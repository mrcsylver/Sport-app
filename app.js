/* =====================================================================
   IRON LEAGUE — app logic
   Plain JavaScript, no build step. Talks to Supabase over HTTPS.
   ===================================================================== */
(function () {
  'use strict';

  var CFG = window.APP_CONFIG || {};
  var TZ = CFG.TIMEZONE || 'Europe/Paris';
  var APP_VERSION = '2.14.0';

  /* ===================================================================
     1. THE POINTS TABLE
     Keep this identical to calc_points() in supabase/schema.sql.
     The server always recalculates, so this is only for the preview.
     =================================================================== */
  /* BODY BEGIN */
  /* GENERATED by tools/build_body.py — do not edit by hand.
     The anatomy is SVG path data from react-native-body-highlighter,
     MIT licensed, (c) 2022 ELABBASSI Hicham. Licence and reasoning in
     vendor/body/. `m` is the region a path answers to; `inert` is the
     rest of the body, drawn but not scored and deaf to taps. */
  var BODY = {"masc":{"box":{"front":"0 0 724 1448","back":"724 0 724 1448"},"front":[{"m":"chest","d":"M272.91 422.84c-18.95-17.19-22-57-12.64-78.79 5.57-12.99 26.54-24.37 39.97-25.87q20.36-2.26 37.02.75c9.74 1.76 16.13 15.64 18.41 25.04 3.99 16.48 3.23 31.38 1.67 48.06q-1.35 14.35-2.05 16.89c-6.52 23.5-38.08 29.23-58.28 24.53-9.12-2.12-17.24-4.38-24.1-10.61z"},{"m":"chest","d":"M416.04 435c-15.12.11-34.46-6.78-41.37-21.48q-1.88-3.99-2.84-12.18c-2.89-24.41-5.9-53.65 8.44-74.79 4.26-6.26 10.49-7.93 18.36-8.56q11.66-.92 23.32-.35c10.58.53 18.02 2.74 26.62 7.87 12.81 7.65 19.73 14.52 22.67 29.75 4.94 25.57.24 64.14-28.21 74.97q-12.26 4.67-26.99 4.77z"},{"m":"obliques","d":"M264.21 435.53c-4.88-3.13-5.75-12.11-5.39-17.36q.03-.53.51-.75 1.8-.84 3.43.85 10.05 10.45 22.57 16.9c3.64 1.89 5.54 3.62 4.79 7.8q-.42 2.35-2.82 1.87-12.45-2.49-23.09-9.31z"},{"m":"obliques","d":"M287.33 452.44c-4.05 4.46-10.38 11.38-16.28 14.3a.84.83 51.1 01-.9-.1c-6.29-5.17-12.54-18.97-14.21-25.09q-.91-3.34.85-8.81.12-.39.35-.05c2.41 3.65 4.59 7.74 8.67 9.76q10.18 5.05 21.27 9.01a.61.61 0 01.25.98z"},{"m":"obliques","d":"M297.3 487.82c-7.36-4.23-16.68-11.37-20.55-17.57q-.32-.5.09-.92 8.72-9.04 19.84-17.87 1.46-1.17 2.81-1.67a.44.44 0 01.59.43c-.28 10.08-.4 20.42.65 30.43q.34 3.26-.68 6.15a1.9 1.9 0 01-2.75 1.02z"},{"m":"obliques","d":"M257.35 456.18l13.68 16.63a1.86 1.82 22.9 01.4.95c.59 5.4-2.02 12.71-3.8 17.56q-.3.84-.84.13-11.85-15.55-9.77-35.17.04-.45.33-.1z"},{"m":"obliques","d":"M271.69 494.07a1.53 1.52-61.8 01-.49-1.64l4.2-13.58a.98.98 0 011.51-.5c3.2 2.32 21.89 14.05 22.26 16.7q1.15 8.32.66 16.79a.9.9 0 01-1.34.73q-14.24-8.05-26.8-18.5z"},{"m":"obliques","d":"M299.35 544.62c-7.52-6.03-16.15-13.43-24.23-21.24-6.93-6.7-6-17.19-4.88-26.06a.44.44 0 01.72-.28q13.31 11.88 28.41 21.38.43.27.6.75c2.33 6.49.95 18.37-.07 25.23q-.09.59-.55.22z"},{"m":"obliques","d":"M299.09 575.53c-7.98-3.65-27.57-15.86-28.06-26.2q-.57-11.91.46-24.3a.36.36 0 01.67-.15q.84 1.36 2.17 2.54 10.59 9.45 21.68 18.31c4.37 3.49 4.34 6.46 4.16 11.74q-.3 8.82-.42 17.64-.01.72-.66.42z"},{"m":"obliques","d":"M308.17 657.58c-7.39-.13-12.41-4.13-17.14-9.39q-11.86-13.22-23.92-26.37-.33-.36-.33-.85.09-23.18 1.81-46.22.53-7.13 2.49-14.41a.71.71 0 011.2-.3q11.54 12.06 25.82 21.1 3.36 2.12 3.62 5.17 2.06 23.67 3.86 47.36c.58 7.62 2.31 13.36 4.43 20.82q.47 1.66-.96 2.79-.39.31-.88.3z"},{"m":"obliques","d":"M438.7 444.36c-2.09-4.03-.13-6.83 3.63-8.81 10.22-5.36 16.79-11 24.23-18.07a1.71 1.71 0 012.89 1.12c.33 4.74-.81 14.39-5.53 17.22-4.68 2.82-18.74 10.02-24.39 9.14q-.57-.09-.83-.6z"},{"m":"obliques","d":"M457.39 466.73c-3.72-1.02-13.2-10.29-16.5-14.49a.52.52 0 01.24-.81q10.94-3.75 21.31-9c3.96-2.01 6.3-5.98 8.57-9.58q.38-.59.55.09c.82 3.33 1.54 6.17.38 9.58-2.55 7.44-7.62 18.79-13.66 24.01a.96.96 0 01-.89.2z"},{"m":"obliques","d":"M428.43 487.22c-1.01-1.79-.82-4.55-.71-6.72q.78-15.08.48-30.27-.01-.59.55-.4 1.72.59 3.02 1.64 11.58 9.37 18.82 16.95c3.86 4.05-16.2 17.42-19.56 19.48a1.87 1.86 59.6 01-2.6-.68z"},{"m":"obliques","d":"M470.76 456.28a.25.25 0 01.44.13q2.03 19.67-9.8 35.22-.37.48-.6-.08c-1.37-3.29-5.86-16.13-3.51-18.91q6.3-7.47 13.47-16.36z"},{"m":"obliques","d":"M452.27 478.5c1.13.49 4.28 12.47 4.78 14.38q.14.5-.23.88-1.29 1.35-2.65 2.41-10.44 8.12-21.76 14.97-1.49.9-2.91 1.33a.81.81 0 01-1.05-.71q-.73-8.62.67-17.15.08-.47.44-.8c1.74-1.6 21.96-15.73 22.34-15.51a.58.03 31 00.37.2z"},{"m":"obliques","d":"M428.22 519.14q.11-.36.43-.56 15.3-9.66 28.83-21.69a.43.42-22.6 01.71.29c.51 8.26 2.25 18.67-4.46 25.4q-11.8 11.84-25.03 22.09-.43.34-.49-.2c-.75-6.82-1.97-18.92.01-25.33z"},{"m":"obliques","d":"M456.54 524.55a.04.04 0 01.07.02q1.52 13.67.41 27.4-.04.47-.28.88c-4.97 8.3-18.23 19.62-27.88 22.63q-.57.17-.58-.43-.05-10.31-.27-20.53-.1-4.8 2.63-7.09c8.54-7.13 18.56-14.62 25.9-22.88z"},{"m":"obliques","d":"M418.89 657.11q-1.12-1.67-.43-3.63 3.27-9.38 4.04-18.23 1.97-22.81 3.58-45.65c.16-2.32.72-6.41 2.84-7.71q14.97-9.23 27.16-21.93.41-.42.71.08 1.29 2.15 1.53 4.2 3.23 27.74 3.13 56.8a1.3 1.28-24.5 01-.33.86q-12.74 13.93-25.55 27.75c-4.8 5.17-9.09 7.87-15.73 7.96q-.61.01-.95-.5z"},{"m":"abs","d":"M311.02 531.71a.23.23 0 01-.19-.21q-.39-10.47 1.9-20.76c1.26-5.69 7.66-9.9 13.1-12.9 9.09-5.01 18.93-11.15 28.56-14.92a1.24 1.21-42.6 01.94.03c3.28 1.52 4.78 3.87 4.82 7.68q.13 13.16-.15 26.31c-.08 3.85.78 8.39-.87 13.1q-.17.46-.59.72-2.65 1.65-4.29 1.82-21.06 2.22-43.23-.87z"},{"m":"abs","d":"M321 577.76c-5.17-.33-8.71-.44-10-6.26q-3.2-14.44-.59-27.83.11-.53.64-.63c7.58-1.44 13.62-2.45 22.45-4.56q11.5-2.76 23.94-1.88c3.67.26 3.3 3.46 3.4 6.21q.46 12.55-.33 26.94-.25 4.41-1.81 8.08-.21.49-.73.6-1.39.28-3.22.29-16.89.14-33.75-.96z"},{"m":"abs","d":"M347.73 429.25c7.46-3.61 10.5 6.27 10.99 11.52.48 5.06 3.46 30.61-2.78 32.93q-4.17 1.55-6.89 3.33-17.56 11.54-35.88 21.46a1.6 1.59-21.9 01-2.3-.98c-2.87-10.41-10.59-43.96 1.66-50.95 11.3-6.45 23.96-11.86 35.2-17.31z"},{"m":"abs","d":"M350.35 712.81c-29.15-9.93-37.98-100.69-39.47-126.61a.99.99 0 01.33-.8c3.58-3.26 27.61-1.47 34.62-.93 4.41.34 15.27 1.31 15.26 7.53-.05 40.77.64 82.05-1.96 122.72a1.29 1.29 0 01-1.86 1.08c-2.3-1.14-4.12-2.04-6.92-2.99z"},{"m":"abs","d":"M371.94 473.31c-5.46-2.59-2.97-24.26-2.77-29.56.25-6.8 2.41-18.63 12.64-13.8q16.26 7.67 32.34 15.72 6.18 3.1 7.13 10.05c.58 4.26 1.35 8.49 1.07 12.72q-.84 12.55-4.33 26.56-.54 2.16-1.1 3.44-.25.58-.81.31c-15.78-7.29-30.79-19.08-44.17-25.44z"},{"m":"abs","d":"M382.57 533.27c-4.17-.18-9.56-.3-13.15-2.69q-.17-.11-.24-.31c-1.82-5.55-.86-11.17-.96-15.66-.18-8.4-.78-17.36.06-25.71.29-2.85 1.88-4.42 4.15-5.79q.42-.26.91-.19 1.71.25 3.21 1.03 12.48 6.44 24.75 13.26c4.96 2.75 12.21 7.02 13.72 12.41q2.93 10.56 2.39 21.49a.77.76-1.8 01-.67.71q-16.89 2.18-34.17 1.45z"},{"m":"abs","d":"M373.75 578.69c-2.47 0-4.31.22-5-2.7-1.8-7.7-3.05-34.29-.19-38.81q.27-.43.77-.47 13.14-1.24 25.77 1.83c8.41 2.04 14.51 3.01 21.85 4.36a1.29 1.28.6 011.05 1.07q2.16 14.12-.73 28.07c-1.08 5.24-5.22 5.26-10.36 5.63q-14.26 1.04-33.16 1.02z"},{"m":"abs","d":"M416.32 584.73q1.14.41 1.07 1.62c-1.62 26.44-9.96 116.68-40.43 126.74-2.27.75-4.15 2.12-6.35 2.73q-1.18.33-1.3-.89-.86-9.2-1.06-17.75c-.83-35.67-.91-71.2-1.01-106.88q0-.5.31-.89c4.95-6.46 41.69-7.25 48.77-4.68z"},{"m":"biceps","d":"M189.52 492.51c-2.43.62-7.38.57-7.51-3.08-.56-16.01-.42-35.49 5.11-50.26 3.19-8.54 13.89-30.22 23.27-32.72 10.08-2.68 12.68 16.59 12.6 22.8-.22 15.98-7.51 34.79-15.05 48.71-4.29 7.94-9.95 12.38-18.42 14.55z"},{"m":"biceps","d":"M526.69 486.31c-9.9-8.61-17.75-33.21-20.65-47.73-1.41-7.06-1.34-29.61 8.58-32.16 10.33-2.66 23.81 25.34 26.6 32.91q2.6 7.04 3.6 16.13 1.62 14.66 1.66 32.28c.03 11.04-16.45 1.48-19.79-1.43z"},{"m":"triceps","d":"M206.2 514.2c-5.41-.67-6.55-7.29-4.69-11.42 11.08-24.55 22.84-50.62 30.54-75.51 1.37-4.41 3.08-8.59 3.95-12.45q2.94-13.12 5.79-26.26.42-1.98 1.82-3.39a.52.52 0 01.81.1q1.04 1.69 1.94 4.56 4.63 14.65 5.15 24.92c.57 11.36-5.11 24.55-8.65 35.5q-7.69 23.78-20.25 45.39c-2.45 4.23-11.51 19.18-16.41 18.56z"},{"m":"triceps","d":"M517.69 512.06c-20.07-22.12-28.95-51.73-38.01-79.03-3.27-9.87-3.58-19.18-1.34-29.38 1.29-5.88 2.49-13.03 5.61-18.52q.32-.57.72-.06 1.35 1.67 1.79 3.69c2.67 12.33 5.14 24.49 9.07 36.52 8.25 25.28 18.58 49.8 31.1 77.2q1.42 3.1 1.05 5.33c-.81 4.89-5.46 9.25-9.99 4.25z"},{"m":"traps","d":"M285.01 307.01a.89.89 0 01-.11-1.64q19.44-9.61 35.65-24.8 1.68-1.57 3.31-.31.4.32.45.82 1.25 12.61-1.57 25.41c-.74 3.32-2.55 4.23-5.9 4.48q-16.02 1.24-31.83-3.96z"},{"m":"traps","d":"M414 311.19c-5.24-.12-7.81-.64-8.9-6.27q-2.33-12.09-1.17-23.94.06-.61.61-.89 1.66-.85 3.65.99 16.12 14.87 33.97 23.63 3.65 1.79-.27 2.89-13.88 3.91-27.89 3.59z"},{"m":"shoulders","d":"M274.06 311.69q3.94 2.77 4.33 8.14.04.48-.38.73c-9.98 5.88-24.35 7.45-28.82 19.75-2.31 6.36-.97 17.35-1.43 23.68q-.55 7.51-5.73 14.07-10.37 13.11-13.81 16.67c-3.41 3.53-6.81 1.76-10.69-.47-15.42-8.87-24.95-25.45-22.52-43.22 2.05-14.92 12.71-25.79 24.06-35.02 16.99-13.82 35.58-17.99 54.99-4.33z"},{"m":"shoulders","d":"M450.39 320.75q-.95-.52-.7-1.58c1.57-6.61 5.8-9.1 12.14-11.9 24.99-11.03 43.76 3.33 60.17 20.74 20.73 21.99 11.81 56.44-14.82 68.19-4.41 1.94-6.79-1.03-9.81-4.51-5.81-6.7-13.46-14.12-15.99-22.8-3.93-13.43 4.32-27.54-9.64-37.62q-8.22-5.93-17.99-9.08-1.84-.59-3.36-1.44z"},{"m":"quads","d":"M292.42 935.6q-.95-.52-1.57-1.4-4.1-5.79-7-13.53-7.8-20.79-13.3-42.33c-9.06-35.53-19.33-71.36-25.03-107.59-5.33-33.86 4-74.19 20.7-103.37q.35-.62.53.07c14.44 55.57 39.03 107.94 41.45 165.34 1.11 26.34.66 52.96-3.6 79.03-.63 3.83-4.73 27.81-12.18 23.78z"},{"m":"quads","d":"M275.11 942.93q-2.42-2.18-3.57-5.24c-3.98-10.61-7.68-21.02-12.81-31.32-7.85-15.76-10.77-34.56-13.2-51.46-2.11-14.63-2.31-31.47-3.93-47.18-.22-2.16-1.04-12.78.46-13.79q1.36-.92 2.08.55c1.5 3.08 3.12 6.12 3.66 9.58q8.21 52.38 26.36 102.15c2.87 7.87 9.98 30.5 1.85 36.74a.71.7-42.5 01-.9-.03z"},{"m":"quads","d":"M322.69 945.72c-3.73 6.14-10.77-2.43-12.6-5.6-3.16-5.47-2.62-14.93-1.78-20.81 4.03-28.09 5.6-52.81 3.48-80.78q-.06-.79.28-.08 15.77 32.83 14.26 68.9c-.4 9.54-2.94 22.48-2.91 34.13q.01 3.02-.73 4.24z"},{"m":"quads","d":"M437.82 933.52c-8.9 14.18-15.15-26.74-15.46-29.25q-5.26-43.04-1.19-86.08c4.9-51.8 26.91-99.32 40.38-150.92q.18-.66.5-.06c17.25 31.67 25.39 68.28 20.54 104.36q-2.29 17.02-8.71 42.76-7.56 30.25-15.2 60.47-6.13 24.25-15.06 47.61-1.83 4.79-5.8 11.11z"},{"m":"quads","d":"M451.79 942.6c-9.95-10.01 4.97-42.91 8.94-55.41q12.55-39.53 19.27-80.47c.49-2.97 2.64-12.34 5.41-13.28a.83.83 0 011.09.64q.74 4 .45 7.92c-1.99 26.52-3.37 58.99-11.01 87.73q-2.53 9.5-7.46 18.8c-4.38 8.24-6.97 16.72-10.08 25.27q-1.66 4.54-4.55 8.63a1.35 1.35 0 01-2.06.17z"},{"m":"quads","d":"M406.69 946.81c-3.24-2.77-1.48-10.64-2.01-14.71q-2.23-17.18-2.57-22.16c-1.75-25.07 3.61-49.11 13.98-71.92q.23-.51.2.05c-1.2 19.15-1.28 38.18.83 57.38q1.68 15.4 3.39 30.8c.43 3.92-.31 9.71-2.09 13.33-1.62 3.28-7.58 10.77-11.73 7.23z"},{"m":"calves","d":"M252.09 1032.57c.24-3.71 2.14-22.17 4.63-24.18a1.03 1.02-17.9 011.67.85c-.45 7.89-1.27 16-1.49 23.45q-.57 18.93-.66 37.88-.02 3.63.34 6.85c2.08 18.76 5.56 37.32 9.3 55.8 3.82 18.84 9.13 37.64 13.11 56.63q2.44 11.68 2.08 17.95c-.32 5.7-3.08 20.49-8.51 23.92a.62.62 0 01-.84-.16q-1.2-1.65-.95-3.55c.92-7.26 1.45-14.15-.3-21.52q-8.25-34.74-13.62-59.06c-1.86-8.44-3.17-17.18-3.93-26.3q-3.69-44.24-.83-88.56z"},{"m":"calves","d":"M315.01 1025.17a.16.16 0 01.32.02c4.06 25.75 8.98 52.72 8.71 77.81q-.13 12.06-5.74 26.31c-7.2 18.3-8.93 38.57-15.95 56.93q-.18.48-.21-.03c-1.87-34.47-5.67-65.91-8.56-103.28q-.97-12.49 4.44-23.14 7.47-14.69 15.14-29.29c.81-1.55 1.35-3.62 1.85-5.33z"},{"m":"calves","d":"M455.5 1231.67c-7.13-5.81-9.23-24.34-8.2-31.86 1.41-10.32 4.63-23.14 7.98-36.33q9.54-37.46 15.15-75.74c2.86-19.5 1.53-40.15.75-59.8-.22-5.67-.98-12.51-1.23-18.75a.97.97 0 011.87-.4c.35.86.92 1.76 1.12 2.68q2.96 14.31 3.31 20.53 2.37 43.28-.49 84.75-1.21 17.42-5.43 35.77-6.33 27.51-12.84 54.98-2.01 8.49-.11 18.36c.36 1.9.11 3.95-.68 5.55a.79.79 0 01-1.2.26z"},{"m":"calves","d":"M412.77 1025.44a.14.14 0 01.27-.04c4.88 11.62 10.93 22.01 17.28 34.78 4.07 8.19 4.71 14.41 4.1 24.25-2.13 34.3-6.27 68.85-8.45 101.59q-.05.69-.31.05-1.48-3.67-2.28-6.75c-4.34-16.75-8.78-38.38-16.39-57.57q-1.4-3.55-2.2-10.11c-1.78-14.73-.2-31.24 2.04-45.88q3.06-20.02 5.94-40.32z"},{"m":"forearms","d":"M127.23 683.05c-4.07-2.12 1.27-27.07 2.25-31.57 4.98-23.03 9.17-46.17 13.91-69.25q1.53-7.47 2.13-15.13c.93-12.09.81-22.15 6.23-31.59 7.1-12.33 13.54-29.16 26.1-36.73a1.98 1.97 62.7 012.84.91c1.92 4.48 1.93 8.28 2.06 14.15.44 19.77-1.3 41.04-8.72 59.67-11 27.62-22.22 55.21-32.62 82.91-4.04 10.76-7.56 20.66-12.82 26.39q-.59.65-1.36.24z"},{"m":"forearms","d":"M201.5 527.4a.84.84 0 01.67.65c3.98 17.15-2.93 39.36-10.95 54.41-4.6 8.63-13.06 20.43-18.21 31.33q-13.21 27.92-24.58 56.64-2.51 6.35-6.61 11.02a1.43 1.43 0 01-2.5-.81q-.36-3.78.84-7.17 10.31-29.18 21.57-57.99c6.32-16.18 14.55-31.65 20.66-47.87 3.69-9.82 5.36-22.36 7.32-30.62 1.49-6.27 4.19-11.06 11.79-9.59z"},{"m":"forearms","d":"M207.33 540.4a.6.59-63.1 011.03-.34l5.38 6.02q.4.45.33 1.06-.52 4.1-1.29 5.84-6.91 15.65-13.69 31.35c-5.41 12.53-16.33 28.4-23.51 44.89-8.3 19.08-16.03 39.32-26.75 57.16a.36.36 0 01-.62 0l-.19-.32q-.17-.28-.06-.59 10.08-29.91 23.05-58.65 2.9-6.42 5.47-11.21c4.62-8.59 10.86-16.17 14.62-23.02q13.23-24.13 16.23-52.19z"},{"m":"forearms","d":"M600.08 683.04c-5-4.14-8.97-15.46-11.29-21.56-5.82-15.25-11.38-30.55-17.58-45.7q-9.15-22.39-18.02-44.89c-5.58-14.19-7.32-31.42-7.99-46.57-.29-6.44-.68-19.43 2.67-25.02a1.71 1.71 0 012.25-.63c6.72 3.52 11.29 9.96 14.87 16.5q6.25 11.38 12.68 22.66c1.97 3.45 2.93 7.66 3.41 12.06 1.16 10.6 1.55 21.29 3.66 31.65 3.93 19.29 7.38 38.63 11.47 57.92 1.5 7.07 9.3 39.08 5.12 43.5a.91.91 0 01-1.25.08z"},{"m":"forearms","d":"M586.58 681.46q-4.35-4.47-6.75-10.61-11.35-28.91-24.59-57.01c-5.72-12.13-14.32-22.86-19.97-35.1-7.1-15.36-12.9-33.32-9.27-50.31a1.44 1.43-87.1 011.23-1.12c7.47-.88 9.29 2.88 11.02 9.2 3.39 12.42 4.76 25.91 9.75 36.7 15.55 33.65 27.61 64.94 39.31 98.42 1.13 3.24 2.05 5.47 1.62 9.04a1.38 1.37 26.3 01-2.35.79z"},{"m":"forearms","d":"M579.58 686.43q-3.92-5.77-6.87-12.13-8.05-17.34-19.75-44.5-2.68-6.24-6.46-13.62c-5.14-10.05-13.15-22.36-17.34-31.85q-9.55-21.68-13.66-31.36-1.09-2.58-1.33-5.87-.04-.61.37-1.07l5.24-5.85a.69.69 0 011.2.4q2.74 27.05 15.49 50.75 1.7 3.17 8.26 12.86 7.02 10.39 12.18 21.88 8.71 19.41 20.19 50.1 2.22 5.92 3.13 9.98a.36.36 0 01-.65.28z"}],"back":[{"m":"traps","d":"M1071.06 308.94c5.6 4.92 6.96 17.83 7.43 24.88q1.5 22.3.93 44.68-1.2 46.76-5.66 94a.57.56 3.7 01-.59.51q-.68-.03-.94-1.01-4.29-15.9-9.79-25.19c-10.24-17.31-18.8-31.84-25.59-49.4-10.19-26.38-15.6-54.28-26.46-80.58q-3.07-7.43-7.61-14.07-.3-.43.2-.6 12.47-4.28 25.48-4.85c5.54-.25 12.15.86 18.32 1.41 9.7.87 16.77 3.6 24.28 10.22z"},{"m":"traps","d":"M1163.98 302.12a.43.43 0 01.22.65q-7.08 10.77-11.41 23.37c-10.53 30.61-17.8 62.94-31.3 91.07-5.11 10.64-15.17 25.22-20.12 36.26q-4.08 9.08-6.59 18.83a.77.77 0 01-1.51-.12q-4.27-45.15-5.52-90.99c-.56-20.28-.74-39.92 2.75-60.43 1.04-6.13 2.77-9.98 7.85-13.85 9.8-7.48 18.02-7.73 30.1-9.11 12.02-1.39 23.92.4 35.53 4.32z"},{"m":"shoulders","d":"M980.66 319.58c.19.14.55.19.65.32a.8.8 0 01-.16 1.15c-6.78 4.75-15.26 9.77-20.03 15.58-6.41 7.78-8.76 16.96-9.44 27.04-.39 5.92-1.68 9.5-5.59 13.43-10.02 10.08-19.04 16.47-31.14 20.41q-.75.25-.75-.55.19-18.4-.09-36.3-.14-9.4 1.07-14.22c4.04-16.07 22.8-33.85 39.68-35.64 9.99-1.06 17.34 2.46 25.8 8.78z"},{"m":"shoulders","d":"M1227.3 316.44c14.62 9.44 25.48 21.03 25.46 39.51q-.02 20.56-.01 41.37a.37.37 0 01-.51.35c-5.08-2.06-10.41-3.98-14.9-6.97-7.84-5.24-21.14-14.95-21.77-24.95-.69-10.75-2.81-20.85-9.76-29.25-4.68-5.65-12.96-10.58-19.6-15.26q-1.23-.87.01-1.71c4.6-3.13 9.91-6.78 15.25-7.98q13.58-3.03 25.83 4.89z"},{"m":"lats","d":"M987.06 381.44c-8.48-5.06-14.14-13.28-18.82-22.92q-5.3-10.92-6.46-14.04c-1.49-4.01 35.14-19.22 39.61-20.97q2.75-1.08 4.33-.72c4.33.96 6.61 9.96 7.46 13.7q5.43 23.89 14.65 55.74.78 2.7-.88 4.39c-5.37 5.5-34.69-12.08-39.89-15.18z"},{"m":"lats","d":"M1017.44 583.31q-9.11-9.57-16.97-22.03-2.28-3.62-2.91-7.25c-3.28-18.82-5.77-38.04-10.52-56.55-3.53-13.73-4.74-25.19-6.61-41.43-.85-7.35-5.67-13.34-8.22-18.75q-4.93-10.47-6.44-22.88-.33-2.72 1.89-1.11c7.25 5.27 16.36 6.16 26.91 7.56 8.86 1.19 23.41-3.18 28.94-10.76 3.34-4.58 4.7-6.5 8.86-8.77a.67.66-26.4 01.92.3q10.02 21.8 19.93 43.78c2.56 5.69 12.11 15.88 10.77 21.83-3.65 16.09-9.88 31.96-16.24 47.13-9.72 23.21-18.61 46.72-27.2 70.36q-.24.67-.88.35-1.03-.52-2.23-1.78z"},{"m":"lats","d":"M1017.71 404.73c-23.86 13.25-54.31 7.11-60.45-22.75-1.2-5.81-2.5-15.84.64-20.55 3.63-5.44 7.17 4.18 8.17 6.14 7.71 15.14 31.62 29.16 48.2 31.13q1.84.21 5.26 2.06.4.21.26.64-.86 2.65-2.08 3.33z"},{"m":"lats","d":"M1141.45 397.63a2.17 2.14-3.6 01-1.88-1.64q-.71-2.97.18-5.95 8.74-29.19 11.75-43.29c1.73-8.11 3.07-16.77 6.94-22.08 1.92-2.62 4.28-2.27 7.19-1.15q20.52 7.9 39.09 18.77a1.37 1.36 25.9 01.58 1.67c-6.05 15.46-12.98 30.84-28.43 39.45-9.45 5.26-25.83 15.17-35.42 14.22z"},{"m":"lats","d":"M1149.69 404.8q-2.04-1.15-2.45-3.5-.09-.53.41-.75c4.64-2.04 9.78-2.51 14.63-3.87 11.01-3.1 22.03-10.83 30.34-18.57q6.33-5.89 7.58-8.93c1.02-2.49 3.79-9.5 7-9.46q.52.01.87.39 2.71 3.01 2.81 7.2c.33 13.77-2.24 26.93-13.26 35.95-13.88 11.36-33.12 9.94-47.93 1.54z"},{"m":"lats","d":"M1161.19 419.98c6.1 1.57 11.6.99 17.75.06 8.36-1.27 14.83-2.76 21.34-7.27a.54.53 74.1 01.84.47q-.64 11.88-5.76 22.85c-2.42 5.2-6.64 10.84-8.04 16.67q-1.02 4.24-1.43 8.92-1.64 18.72-6.34 37.47c-4.73 18.91-7.13 38.67-10.8 57.85q-.24 1.24-2.2 4.3c-4.57 7.14-12.22 19.43-19.34 23.88a.44.43-25.6 01-.64-.22c-8.26-22.57-16.6-45.11-25.91-67.23-6.67-15.85-13.27-32.14-17.27-48.42q-1.58-6.41 2.91-12.01 5.21-6.51 8.57-14.14 9.25-21 19.01-41.64a.47.47 0 01.65-.21q6.17 3.37 9.51 9.64c2.45 4.6 12.22 7.75 17.15 9.03z"},{"m":"triceps","d":"M931.03 442.29c-2.01 2.57-6.52 9.71-10.12 9.17q-.52-.08-.8-.52-1.35-2.09-1.84-4.44c-2.25-10.87-3.28-22.88 1.35-33.38 5.45-12.33 18.27-23.68 29.61-31.2a.47.46 68.7 01.71.32l6.42 38.52q.09.54-.26.97c-.47.58-1.12 1.52-1.71 1.94q-9.11 6.58-18.08 13.36-2.9 2.2-5.28 5.26z"},{"m":"triceps","d":"M958.15 427.11a.41.41 0 01.55.27q4.44 16.16-2.23 31.41-3.37 7.73-5.91 19.98c-1.51 7.28-8.93 12.21-11.81 18.82-2.42 5.56-2.41 12.5-3.51 16.66-2.14 8.06-8.51 14.15-13.91 20.13a.93.93 0 01-1.54-.25q-.57-1.3-.75-2.89c-1.93-16.91 2.52-33.52 5.71-49.99 2.16-11.21-1.54-24.15 9.68-34.59q9.54-8.86 19.55-17.23c1.3-1.08 2.7-1.72 4.17-2.32z"},{"m":"triceps","d":"M903.57 519.67a1.84 1.82-5.4 01-1.12-.92q-3.54-6.97-3.68-15.19c-.37-21.2 3.8-42.53 9.5-63.44q.33-1.23.92-.1 4.64 8.78 8.6 18.67c2.88 7.21 4.19 12.98 1.88 20.57q-6.07 19.96-14.02 39.23-.65 1.58-2.08 1.18z"},{"m":"triceps","d":"M1213.94 424.56q-2.02-1.5-3.08-3.02-.31-.46-.22-1 3.32-19.22 6.42-38.46.09-.56.56-.25 14.9 9.82 24.8 22.71c9.8 12.75 9.72 30.37 5.41 45.13a2.62 2.62 0 01-3.76 1.57c-3.26-1.77-6.22-6.71-8.62-9.67-5.24-6.46-14.75-12-21.51-17.01z"},{"m":"triceps","d":"M1246.2 534.5q-.95-.3-1.75-1.22c-4.65-5.4-9.13-9.88-11.46-15.51-2.96-7.13-1.37-15.5-5.64-22.09-4.06-6.26-8.72-9.91-10.89-17.58-1.62-5.68-2.81-11.46-4.97-17.02-4.56-11.69-6.45-20.86-3.33-33.56a.59.58-74 01.75-.42q1.69.56 3.22 1.79 11.23 9.08 21.54 19.18c5.39 5.28 6.92 10.13 7.24 18.16.9 22.52 10.62 44.97 6.59 67.49a1.01 1 13.9 01-1.3.78z"},{"m":"triceps","d":"M1258.43 439.96q2.01 5.38 3.1 10.68c3.58 17.36 7.13 34.77 6.89 52.61q-.11 8.3-3.94 15.61a1.61 1.6 33.4 01-2.44.5c-1.45-1.19-1.9-3.58-2.43-4.94q-9.23-23.41-13.19-38.15c-2.63-9.81 6.82-27.63 11.53-36.35q.28-.5.48.04z"},{"m":"lowerback","d":"M986.76 627.1c-3.13-13.13-7.31-49.77 7.27-58.07 2.4-1.37 4.8-.82 6.7 1.29 6.15 6.8 16.22 18.56 18.77 28.15a1.35 1.3 52.6 01-.11.98c-2.51 4.53-9.96 8.09-15.83 11.36q-5.47 3.06-11.33 10.52c-1.23 1.56-2.6 4.3-4.5 6.06a.59.58-28.2 01-.97-.29z"},{"m":"lowerback","d":"M1023.15 607.96a2.06 2.04-74.3 01-.94-1.69c-.17-10.98 5.04-24.58 8.79-34.9q15.61-42.83 36-83.59a1.11 1.1-62.5 011.51-.48c1.25.66 3.21 12.98 3.46 15.08q6.94 59.25 2.82 116.88-.62 8.66-3.1 19.37-.13.53-.59.24l-47.95-30.91z"},{"m":"lowerback","d":"M1090.76 581.75q.62-5.16 0-10.27.22-29.79 3.05-59.5 1.1-11.58 3.91-22.88.31-1.27.44-1.43 1.08-1.43 1.88.17 23.38 46.97 40.14 96.18c1.8 5.28 5.84 16.69 4.38 22.96a1.64 1.64 0 01-.71 1.01l-47.63 30.72q-1.12.72-1.34-.6-4.54-28-4.12-56.36z"},{"m":"lowerback","d":"M1151.19 603.31q-5.39-3.38-2.19-9.05 8.03-14.22 17.88-24.62c3.49-3.69 9.04.89 10.97 3.99q2.92 4.66 3.8 10.14 3.5 21.77-1.21 43.02a.96.96 0 01-1.77.28c-6.92-11.85-16.03-16.56-27.48-23.76z"},{"m":"forearms","d":"M878.44 534.38a.15.15 0 01.18-.13c.47.12 6.68 15.77 7.07 17.22q6.66 24.73 5.52 50.29c-.4 8.9-3.45 17.35-6.64 25.55-7.94 20.38-17.41 41.88-29.59 60.09a1.04 1.02-54.2 01-1.49.25c-.34-.26.37-1.45.47-1.83q5.58-20.8 8.97-42.08 8.65-54.15 15.51-109.36z"},{"m":"forearms","d":"M893 518.93a.39.38 24.6 01.69-.25q5.97 7.83 13.11 15.27c8.08 8.4 1.41 28.73-5.88 37.12a1.05 1.05 0 01-1.63-.05c-6.09-7.93-5.41-18.74-4.97-28.44.36-8.12-.76-15.7-1.32-23.65z"},{"m":"forearms","d":"M869.06 547.19c2.16.36 1.67 6.21 1.57 7.8q-2.54 38.84-9.11 77.16c-3.04 17.71-8.47 41.3-22.09 54.09a.38.38 0 01-.62-.41c14.51-40.44 19-84.26 26.8-126.31q.9-4.88 1.48-10.82.18-1.81 1.97-1.51z"},{"m":"forearms","d":"M864.24 682.58q15.09-28.18 25.12-58.55c8.14-24.63 13.67-42.4 20.79-60.35q3.31-8.37 12.08-9.63c1.35-.2 3.68-.75 4.86.21q1.13.93.61 2.3-5.8 15.45-12.04 29.88c-5.79 13.39-14.92 28.68-20.32 40.14-6.12 13-28.07 59.18-31.64 56.64a.21.21 0 01.03-.36q.15-.07.34-.13.12-.04.17-.15z"},{"m":"forearms","d":"M1272.99 519.43c.27-.33.33-.75.75-1.05a.32.32 0 01.5.29c-.7 7.22-1.77 14.33-1.66 21.54.13 8.94 2.13 24-5.35 31.17q-.37.35-.73 0c-7.63-7.55-14.2-28.29-6.52-36.92q6.6-7.41 13.01-15.03z"},{"m":"forearms","d":"M1312.82 688.04c-4.78-6.01-7.2-10.8-11.76-19.56q-12.39-23.79-21.03-47.53c-4.86-13.36-5.22-26.17-3.83-40.19q1.13-11.5 2.69-19.53 2.72-13.98 9.59-26.79a.17.17 0 01.32.06q7.26 63.12 17.22 120.49 2.43 14.04 7.03 30.55c.22.79.74 1.33.36 2.4a.34.34 0 01-.59.1z"},{"m":"forearms","d":"M1296.52 558.51c-.22-2.94-1.44-10.25 2-12.04a.62.61-18.4 01.89.44q6.25 35.69 12.21 71.07c3.88 23 8.77 46.2 16.73 68.19a.29.29 0 01-.47.31c-11.67-10.67-18.09-31.15-20.89-45.98q-7.27-38.55-10.47-81.99z"},{"m":"forearms","d":"M1303.5 683.6c-2.89-.66-10.16-13.21-12.11-17.02-8.8-17.21-16.92-34.81-25.84-51.89-5.36-10.27-10.98-20.49-15.39-30.95q-5.86-13.86-11.07-27.8a1.63 1.62 79.5 011.5-2.2c13.02-.16 15.5 7.18 19.65 18.81q9.04 25.33 17.43 50.89 9.65 29.37 23.82 56.84.87 1.69 2.13 3.12.24.28-.12.2z"},{"m":"glutes","d":"M1045.06 626.19q1.42.61 4.11 4.4.27.39-.19.52c-14.47 4.12-26.13 7.4-38.13 15.77q-15.37 10.71-30.53 21.6a.55.54 74.9 01-.86-.5c1.19-13.13 10.35-35.23 20.46-45.06 9.14-8.88 34.99-1.11 45.14 3.27z"},{"m":"glutes","d":"M1007.94 762.81c-16.94-16.64-29.37-37.66-31.47-61-2.06-22.84 15.63-34.95 32.18-45.71 8.2-5.33 46.51-27.32 54.37-17.65 5.92 7.29 13.38 15.84 15.44 25.21q3.01 13.63 2.44 27.6-.94 22.59-6.27 44.49c-2.43 9.96-2.9 17.16-2.59 26.75.47 14.83-18.52 17.18-29.12 14.07-6.38-1.87-13.79-4.83-21.35-6.25q-7.39-1.38-13.63-7.51z"},{"m":"glutes","d":"M1117.94 631.04q-.13-.03-.27-.06-.12-.02-.06-.13 2.58-4.2 7.05-5.92 12.71-4.87 26.13-5.81c12.93-.91 17.1 3.08 23.28 13.06 5.71 9.22 13.32 24.7 13.44 36.06q.01.76-.61.32-16.65-11.74-33.2-23.51c-10.03-7.14-23.72-10.58-35.76-14.01z"},{"m":"glutes","d":"M1124.12 776.61c-9.28 2.74-26.75 1.29-28.86-10.88-1.05-6.03.27-14.88-1.3-23.27q-.54-2.94-2.15-9.35c-3.2-12.81-4.02-23.33-5.08-35.27-1.07-12.03-.57-22 1.64-33.17q1.1-5.6 4.19-10.41 8.74-13.58 11.87-16.59c4.96-4.77 15.84.18 21.19 2.11q19.7 7.12 40.17 21.43c9.59 6.7 19.29 14.31 22.93 25.17 4.81 14.37-.65 33.88-7.42 46.87q-7.79 14.97-21.39 28.9-6.74 6.9-15.26 8.36c-7.07 1.21-13.68 4.08-20.53 6.1z"},{"m":"hamstrings","d":"M963.27 741.53a.71.7 31.7 011.19-.28q1.51 1.62 2.47 3.99c4.6 11.41 8.93 22.66 11.07 34.72 3.38 19.14 4.84 38.23 3.12 57.74q-1.68 19.06-2.99 38.15c-.51 7.55-.88 15.71.07 23.18q1.08 8.54 1.39 17.57a.52.52 0 01-.98.25q-1.03-2.07-1.8-4.62-5.13-16.92-7.25-34.49-5.01-41.45-6.86-83.17-1.09-24.75-.07-49.51.06-1.59.64-3.53z"},{"m":"hamstrings","d":"M1030.2 791.53q.17-.36.38-.03c5.26 8.11 9.94 16.15 12.47 25.64 3.12 11.72 5.87 24.36 4.31 36.24q-.5 3.8-3.57 14.02c-10.75 35.81-12.83 74.2-18.5 111.1q-.82 5.4-2.55 10.55-.23.68-.59.07c-4.72-8.07-5.18-25.09-5.34-34.81-.7-43.69 1.92-87.82 6.38-131.28 1.41-13.74 1.99-21.15 7.01-31.5z"},{"m":"hamstrings","d":"M998.81 761.94q14.07 14.17 20.1 33.62c.98 3.15-.78 9.61-.93 12.91q-1.3 27.63-2.3 55.27c-.55 15.31-1.54 30.27-5.12 45.26q-8.62 36.18-22.76 68.73-3.65 8.41-10.15 17.19-.45.61-.41-.14c.11-1.93.82-4.15.99-5.71q2.45-22.72 6.08-45.26c2.83-17.66 4.18-35.95 4.33-52.37.33-36.43-.75-73.34 1.47-109.68.33-5.32 1.07-16.16 4.7-20.25q.33-.36.81-.45 1.95-.37 3.19.88z"},{"m":"hamstrings","d":"M1052.52 855.62a.04.04 0 01.08.01q1.07 9.9 2.17 19.87.33 3.04-2.37 14.18c-3.83 15.8-8.15 31.11-8.9 47.47-.99 21.61-3.11 45.66-9.92 66.3q-1.49 4.52-.87-.2 3.38-25.36 3.7-51.99c.05-3.74-.4-10.32.2-15.58 2.19-19.2 7.39-38.25 11.75-57.05 1.78-7.64 2.93-15.21 4.16-23.01z"},{"m":"hamstrings","d":"M1183.25 947.53c2.57 14.85 4.32 31.11 6.22 46.14q.35 2.74-1.11.39c-14.67-23.67-23.34-52.15-30.55-79.32q-5.08-19.14-5.97-39.05-1.36-30.37-2.44-60.74c-.22-6.09-2.56-15.63-.55-21.57q5.87-17.35 18.96-31.07c10.77-11.28 10.17 46.55 10.16 48.97-.13 41.09-.45 74.18 1.91 110.07.57 8.75 1.88 17.53 3.37 26.18z"},{"m":"hamstrings","d":"M1136.43 791.52q.27-.42.49.03c3.12 6.46 4.84 12.26 5.68 19.83 5.07 45.8 8.05 94.61 7.56 140.76-.13 11.8-.46 26.22-5.13 37.08a.44.44 0 01-.83-.06q-2.51-9.14-3.69-18.41-3.54-27.64-7.36-55.24c-2.49-18-5.47-35.67-11.09-52.26q-4.35-12.82-2.08-26.75c1.76-10.77 3.58-21.61 8.46-31.16q3.58-6.99 7.99-13.82z"},{"m":"hamstrings","d":"M1115.03 856.73c2.03 18.72 7.11 37.44 11.47 55.77 2.25 9.46 3.94 19.51 3.95 30.11q.02 31.7 4.08 63.16.16 1.26-.29.07-2.7-7.15-4.19-14.6c-4.44-22.21-5.71-40.52-6.87-61.23-.24-4.24-1.19-9.64-2.23-13.92q-3.94-16.25-7.7-32.55c-2.09-9.04.08-18.69 1.6-27.66q.07-.38.32-.09.16.19.01.4-.19.24-.15.54z"},{"m":"hamstrings","d":"M1202.61 741.08a.44.44 0 01.72.03c.52.82.9 1.86.95 2.91q.73 15.98.37 31.97-1.16 52.95-7.85 105.49-1.88 14.74-5.97 29.04-1 3.52-1.92 4.95-1.57 2.47-1.39-.37c.58-9.44 1.83-19.17 1.71-28.16-.32-24.52-4.94-49.11-3.95-72.75.69-16.54 2.5-33.51 7.54-49.38q2.99-9.4 6.61-18.6.74-1.88 3.18-5.13z"},{"m":"calves","d":"M982.69 1149.31c-3.07-2.23-3.98-6.24-5.24-11.03-7.19-27.14-7.88-53.18-6.67-82.78q1.03-25.29 9.23-47.45c4.77-12.89 15.33-24.77 23.79-36q.82-1.09.74.27c-1.37 22.86-2.72 45.67-3.11 68.49-.52 30.56-1.51 61.11-.42 91.68.24 6.83-2.77 16.29-10.08 18.37q-4.39 1.25-8.24-1.55z"},{"m":"calves","d":"M983.99 1163.56c7.15-5.59 16.16-.63 17 8.23q4.31 45.02 5.22 90.26c.16 8.25-.8 15.79-2.19 23.65q-.45 2.52-1.43 3.66-.95 1.11-1.22-.33c-5.03-26.7-8.28-53.49-11.87-80.36q-1.68-12.52-3.24-18.71-2.04-8.12-5.53-18.24c-1.03-3 .8-6.25 3.26-8.16z"},{"m":"calves","d":"M1013.69 1150.31c-4.8-2.61-4.66-16.17-4.36-20.75 2.34-36.49 3.44-73.94 1.04-110.45-1.03-15.55.02-31.49.62-47.06q.03-.66.25-.03c2.28 6.45 4.52 12.88 7.39 19.11 5.12 11.14 11.5 22.91 14.83 33.92q2.34 7.74 3.97 16.46 5.3 28.43 5.62 56.09c.2 18.32-7.9 40-22.63 51.79q-3.42 2.73-6.73.92z"},{"m":"calves","d":"M1014.14 1164.37c7-1.83 14.1 2.2 14.11 9.95q.06 29.04-5.62 57.41c-3.87 19.28-6.24 38.23-8.43 57.48a.37.37 0 01-.74-.01q-3.12-43.48-3.58-86.64-.15-14.16.76-28.3c.18-2.83.02-8.98 3.5-9.89z"},{"m":"calves","d":"M1172.94 1149.31c-6.06-4.56-6.94-11.4-6.8-19.4.96-52.67-.49-105.31-3.54-157.9q-.04-.72.41-.16 7.96 10.07 15.43 20.44c9.11 12.64 13.61 28.98 15.78 44.21 4.96 34.71 3.75 72.94-5.97 106.5-1.97 6.82-9.18 10.93-15.31 6.31z"},{"m":"calves","d":"M1144.41 1147.33q-17.19-17.37-20.08-40.86-.89-7.22-.13-19.97 1.18-20.06 4.69-41.33c2.33-14.1 5.8-25.22 12.41-38.61q8.19-16.59 14.35-34.15a.14.13-37.7 01.26.03q1.01 15.71 1.26 31.44c.18 11.61-1.34 24.91-1.58 36.43-.72 34.7 1.22 62.05 2.06 93.19.17 6.32-1.1 26.1-13.24 13.83z"},{"m":"calves","d":"M1173.74 1161.73c6.88-2 14.34 3.23 11.98 10.91-2.24 7.3-4.78 14.44-5.99 21.96-5.07 31.52-8.04 63.18-14.13 94.6a.72.71-61.9 01-1.21.37c-.14-.14-.35-.39-.4-.59q-3.53-13.58-3.19-28.23 1.04-44.67 5.06-87.04c.58-6.1 1.93-10.25 7.88-11.98z"},{"m":"calves","d":"M1154.32 1165a1.58 1.57-84.6 01.97 1.18c.79 4.42 1.42 8.78 1.57 13.4.96 29.17-.47 62.66-2.04 90.23q-.78 13.79-1.39 19.52a.23.23 0 01-.45 0c-2.79-21.25-5.41-41.99-9.64-63.03-3.44-17.08-4.29-34.91-4.68-52.3-.19-8.37 8.99-11.61 15.66-9z"}],"inert":{"front":["M354.01 315.07q-3.49-3.65-5.9-8.23c-6.46-12.3-11.03-25.42-16.12-38.77-2.92-7.66-1.98-19.44-1.61-27.6q.03-.58.47-.21c9.06 7.39 11.33 17.46 15.67 27.62 5.4 12.61 15.4 33.31 9.11 46.92a1 .99 35.5 01-1.62.27z","M345.77 316c-4.12-1.96-12.78-6.76-15.07-11.38-4.29-8.65-2.69-16.02-2.28-25.25a1 1 0 011.95-.28c4.29 12.42 10.5 24.4 15.71 36.61q.23.55-.31.3z","M372.75 314.71c-5.78-9.67 1.71-31.17 6.17-40.68 5.95-12.68 8.21-24.68 18.35-33.9a.49.49 0 01.82.35c.28 8.68.84 19.39-1.97 27.72-5.26 15.58-11.39 33.46-21.42 46.62a1.18 1.18 0 01-1.95-.11z","M398.01 278.49a.5.49 35.5 01.87-.14c2.01 2.7 1.62 11.6 1.61 15.13-.04 12.42-8.2 17.45-17.9 22.58a.35.35 0 01-.48-.46c5.51-12.02 11.85-24.46 15.9-37.11z","M362.65 290.52q-1.14-1.37-1.86-3.41-5.33-15.15-12.14-29.75c-2.37-5.06-1.07-9.07-7.92-10.99q-1.01-.28.02-.47c5.98-1.08 15.25.91 21.33 2q2.37.42 4.81-.09 10.09-2.13 20.45-2.12a.37.37 0 01.08.73c-6.34 1.46-5.45 5.64-7.57 10.21q-6.1 13.1-11 26.69-1.3 3.62-2.9 6.81a1.99 1.99 0 01-3.3.39z","M280.26 647.4c11.65 10.74 22.18 21.04 31.02 34.3 15.82 23.72 27.55 49.72 34.01 77.58 1.34 5.79-6.14 20.34-12.62 20.22q-.52-.01-.72-.49-.67-1.59-1.21-3.13c-14.68-41.71-27.96-79.71-46.87-117.01-1.9-3.74-3.05-7.33-4.06-11.2a.27.27 0 01.45-.27z","M331.64 898.32q-.17.57-.23-.02c-2.23-25.01-8.47-50.09-14.25-74.53q-19.4-82.1-42.46-163.69-.58-2.08.33-.13c19.88 42.53 38.94 86.51 51.64 132.07 9.49 34.06 15.59 71.67 4.97 106.3z","M334.46 789.17c1.56-2.63 14.39-20.38 16.2-20.37a1.71 1.7-89.2 011.7 1.76q-1.12 34.88-7.4 68.95c-.38 2.06-1.41 4.27-2.16 6.23q-.24.62-.34-.04-3.68-25.45-8.44-50.7c-.34-1.79-.63-4 .44-5.83z","M395.47 779.4c-5.7 1.33-11.34-11.87-12.46-15.86q-.61-2.18-.02-4.65 10.17-42.64 35.06-78.81c9.47-13.77 18.83-22.36 29.85-32.56q.55-.5.4.22-1.12 5.7-3.73 10.83c-19.44 38.38-33.3 79.2-47.77 119.65a1.84 1.83-86.4 01-1.33 1.18z","M453.65 658.99q.67-1.43.23.09-26.73 93.75-48.63 189.74c-1.98 8.7-3.66 17.9-5.44 26.84q-2.19 11.05-2.78 22.43a.15.15 0 01-.3.04c-8.18-24.48-6.74-51.98-1.87-76.86 11.07-56.49 34.44-110.42 58.79-162.28z","M377.91 768.67c1.49.84 1.76 1.49 2.66 2.66q6.16 8.04 12.23 16.13c1.88 2.52 1.97 4.18 1.38 7.45q-4.57 25.23-8.43 50.57-.11.71-.4.05-1.89-4.29-2.54-8.09-5.57-32.28-6.98-65.01-.09-2 .81-3.44a.95.94 30.8 011.27-.32z","M297.69 1008.37c-7.27 7.29-16.34 3.42-19.64-5.18q-6.18-16.11-9.57-30.68c-1.99-8.6-2.24-19.68 9.72-19.91q13.12-.24 26.05 2.15 1.71.32 3.29 1.02a1.17 1.15 4.2 01.63.72c3.17 10.27 2.5 23.36.05 33.69q-2.37 10.01-10.53 18.19z","M288.03 1059.54c-6.99-5.81 13.75-46.43 17.3-53.91q7.3-15.38 10.9-32.01c.74-3.42 2-6.31 4.18-8.64a1.36 1.35 54.7 012.23.39c3.97 9.09 1.66 13.86-1.67 24.65q-10.23 33.19-27.2 63.57-1.8 3.23-4.2 5.84a1.13 1.12-49 01-1.54.11z","M430.44 1008.31c-12.92-12.62-14.34-33.49-10.92-50.31.31-1.53 1.09-2.53 2.73-2.86q11.44-2.25 23.08-2.59c14.13-.42 17.31 5.67 14.54 18.63q-3.13 14.69-9.12 30.37c-3.45 9.03-11.63 15.25-20.31 6.76z","M438.96 1059.52q-2.25-1.89-3.8-4.64-20.15-35.92-31.06-75.66-2.11-7.68 1.95-14.16a1.16 1.16 0 011.91-.08c2.26 3.06 3.4 5.4 4.26 9.37 3.98 18.54 10.94 32.53 20.07 51.09 3.51 7.14 11.38 26.16 8.5 33.61a1.16 1.16 0 01-1.83.47z","M263.52 973.59a.6.6 0 011.09-.14q1.38 2.22 1.83 5.06c7.87 49.97 18.01 99.59 25 149.68q4.63 33.19 4.31 67.55-.04 3.45-2.15 5.76-.4.44-.75-.03-1.89-2.58-3.08-5.51c-11.63-28.6-20.46-58.12-24.26-88.68q-4.96-39.97-5.72-69.53c-.13-5.27-.17-12.59.35-18.98q1.7-20.77 2.52-41.6c.04-1.16.52-2.43.86-3.58z","M463.39 973.68a.7.7 0 011.25-.1c.27.46.64 1.34.68 1.93q1.26 20.88 2.53 41.76.66 10.82.39 19.98-1.23 40.77-7.51 82.25c-3.91 25.87-12.19 51.55-21.96 75.76q-1.13 2.79-3.27 6.13-.29.44-.71.12c-2.68-2.06-2.32-6.7-2.29-10.32.26-31.03 2.71-55.52 8.76-91.4q9.27-55.06 18.94-110.05c.8-4.5.99-10.52 3.19-16.06z","M100.98 745.85c-9.03-6.62-15.78-13.18-13.3-24.59 2.67-12.29 15.01-20.6 25.37-26.21 7.76-4.21 18.22-1.68 26.15.97 7.14 2.39 11.11 6.16 11.1 13.86q-.04 18.51-4.75 36.37c-5.47 20.76-34.48 6.99-44.57-.4z","M53.81 746.32a.91.91 0 01-.74-.95c.14-2.49-.23-6.34 2.25-7.8 4.66-2.71 11.37-5.53 14.15-10.3q6.32-10.86 16.56-20.3 1.27-1.17.64.44c-1.45 3.73-2.86 7.21-3.87 11.59-2.76 11.9-14.62 30-28.99 27.32z","M87.21 745.05c1.44.46 8.14 2.66 8.61 4.55 1.26 5.12-4.42 8.54-7 12.25-7.73 11.1-15.12 23.38-24.25 33.28a1.22 1.22 0 01-2.11-.86c.11-3.93.38-7.1 2.43-10.65q10.27-17.71 19.31-36.11.32-.65 2.13-2.27.38-.35.88-.19z","M108.11 758.12a2.16 2.16 0 011.07 2.87q-10.49 22.55-19.92 45.81c-1.45 3.56-4.37 5.15-7.82 6.04a1.35 1.34-8.1 01-1.69-1.26c-.11-3.05.37-5.87 1.58-8.9q8.1-20.28 15.15-40.96c.41-1.2.62-3.33 1.69-4.85a1.21 1.21 0 01.91-.49q4.72-.21 9.03 1.74z","M134.09 799.9q-1.16-1.7-1.41-3.73-2.1-17.07-1.18-34.29.03-.6.61-.75l6.93-1.85q.68-.19.65.52-.51 10.9-.85 21.71c-.28 8.58.1 12.65-4.17 18.4a.36.36 0 01-.58-.01z","M108.13 814.65a1.48 1.48 0 01-1.62-1.47c-.02-2.83-.14-5.66.32-8.53q2.9-17.79 5.4-35.65.53-3.84 1.58-7.56a.66.66 0 01.76-.48l7.26 1.24a.97.97 0 01.78 1.14q-4.76 23.96-9.1 46.26-.9 4.64-5.38 5.05z","M591.31 755.99c-8.06-2.93-8.66-9.76-10.28-17.06q-3.22-14.42-3.1-29.3.04-4.06 1.46-6.55c4.34-7.57 18.16-9.91 25.63-10.35 8.75-.51 18.37 6.96 24.99 12.27q8.92 7.17 10.74 17.52c2.45 13.89-12.11 23.41-22.7 29.04-6.95 3.69-18.63 7.39-26.74 4.43z","M641.97 706.78q10.85 9.65 17.61 21.91c1.63 2.97 9.74 6.76 12.87 8.59 2.9 1.7 3.03 4.81 2.55 8.5q-.06.42-.48.49c-8.16 1.32-11.99-1.93-17.72-7.23-10.35-9.58-10.5-20.33-15.33-31.9q-.54-1.29.5-.36z","M638 760.07c-2.54-3.42-7.52-6.03-5.44-11.11q.18-.44.61-.63l7.41-3.3q1.29-.58 2.05.62 3.33 5.23 5.69 10.04 6.84 13.94 14.71 27.33c1.35 2.29 4.28 10.16 2.25 12.11a1.22 1.22 0 01-1.77-.08c-9.43-10.98-16.85-23.36-25.51-34.98z","M647.83 812.68c-4 .24-7.71-2.87-9.11-6.38q-9.28-23.27-19.74-45.33a2.05 2.05 0 01.92-2.71q4.5-2.28 9.62-1.7a1.09 1.07 83.8 01.89.73q7.5 23.06 16.57 45.5 1.8 4.46 1.5 9.24a.7.7 0 01-.65.65z","M596.17 761.18a.84.84 0 01.62.81c-.01 4.86.95 35.3-2.71 37.67q-.49.32-.82-.17-3.41-5.21-3.51-8.49-.45-15.62-1.16-31.23-.03-.72.66-.52l6.92 1.93z","M621.09 814.28c-4.35 1.91-5.92-3.77-6.5-6.56q-4.52-21.91-8.88-43.95a1.41 1.41 0 011.14-1.66l6.8-1.18a.92.92 0 011.06.76q2.79 16.32 5.09 32.91c.85 6.17 2.2 12.25 1.8 18.95q-.03.52-.51.73z","M291.88 1208.11c5.48-1.03 11.85 5.55 13.38 10.37q2.45 7.74 1.47 16.83-.09.83-.45.08c-4.31-9.05-8-16.99-15.39-23.88a1.98 1.98 0 01.99-3.4z","M275.88 1270.94c-4.41-3.87-7.4-7.17-4.91-13.37q4.78-11.92 5.49-21.32.62-8.27 6.22-12.84c9-7.33 20.8 15 23.1 22.1 2.55 7.91 4.83 16.36 4.49 24.5-.31 7.14-2.02 17.4-6.49 23.1q-.3.38-.53-.05c-5.67-10.74-18.6-14.41-27.37-22.12z","M430.92 1209.12c2.24-1.35 10.54-2.02 6.02 2.65q-9.99 10.32-14.82 23.8a.28.28 0 01-.55-.08c-.52-10.27-.48-20.45 9.35-26.37z","M445.01 1223.26c8.45 6.56 6.46 16.66 9.35 25.59q1.76 5.43 3.47 10.88c3.84 12.26-27.75 21.49-32.21 32.42q-1.02 2.51-2.17.05c-6.91-14.82-6.79-29.36-1.78-44.58q2.82-8.57 8.02-16.04c3.02-4.35 9.61-12.76 15.32-8.32z","M264.5 1334.5c-3.98-.34-18.59-4.25-19.04-9.44a1.4 1.4 0 01.27-.94c9.66-13.03 20.9-25.49 28.65-39.78q.25-.47.78-.37 9.76 1.78 17.73 7.65a1.19 1.18 43 01.07 1.86c-1.32 1.11-1.65 2.62-1.06 4.35 2.96 8.57-.92 16.55-4.81 25.34-1.79 4.06-1.76 8.99-2.81 13.62a1.56 1.56 0 01-1.99 1.14q-8.36-2.64-17.79-3.43z","M291.87 1340.12c-2.25-2.64-2.07-5.93-.78-9.35q3.34-8.88 4.02-18.35.43-6.02 1.25-8.74 1.32-4.37 3.45-8.22a.66.65 53.7 011.21.19q1.97 9.26 6.28 17.3c2.59 4.85-.82 11.49-2.92 16.14a1.81 1.78-35.8 00-.16.94q.42 4.3-1.9 7.94-.22.33-.61.43l-8.79 2.06a1.06 1.06 0 01-1.05-.34z","M444.66 1337.65q-1.08-1.3-1.28-3.09c-.52-4.48-.73-8.39-2.77-12.64-3.51-7.31-7.06-16.37-4.43-23.19.77-1.99.92-3.79-.76-5.13a1.29 1.28 46.4 01.04-2.04q7.96-5.76 17.59-7.64.46-.1.69.32c7.25 13.1 17.21 24.83 26.45 36.56q1.11 1.41 2.51 3.8a1.17 1.14-51 01.09.95c-1.75 5.01-12.93 7.89-17.77 8.55q-9.87 1.36-19.54 3.82a.82.8-26.2 01-.82-.27z","M426.94 1338.55c-2.01-.34-2.96-5.48-3-7.12-.15-6.02-6.29-11.65-3.12-17.89q4.35-8.53 6.34-17.75a.78.78 0 011.47-.17c2.12 4.52 4.18 9.08 4.35 14.33q.35 10.43 3.97 20.24c1.19 3.22 1.52 5.83.39 8.78a2.32 2.31 19.3 01-2.87 1.38q-3.44-1.09-7.53-1.8z","M 418.91 167.68 c 3.92 -1.77 6.58 0.47 7.06 4.32 c 1.48 11.93 -4.92 26.67 -11.75 36.45 c -2.21 3.17 -3.86 0.17 -4.74 -1.76 a 0.38 0.38 0 0 0 -0.73 0.16 c 0.02 8.31 1.01 17.01 -3.36 24.53 c -0.167 0.293 -4.39 4.62 -10.799 9.508 c -23.591 18.112 -41.591 16.112 -61.446 -0.797 c -4.736 -3.649 -5.925 -5.041 -8.805 -7.621 c -5.66 -5.07 -5.28 -17.38 -4.47 -24.92 c 0.05 -0.51 -0.468 -0.892 -0.933 -0.687 a 0.653 0.653 0 0 0 -0.357 0.397 c -0.57 1.69 -2.24 4.05 -4.07 1.48 c -6.2 -8.71 -16.02 -28.53 -11.19 -38.98 c 1.68 -3.627 3.733 -3.91 6.16 -0.85 a 182.853 182.853 0 0 1 3.78 23.29 a 1.02 1.02 0 0 0 1.56 0.77 c 2.79 -1.75 2.61 -18.93 2.63 -24.22 c 0.02 -4.53 1.12 -8.94 3.8 -13.1 c 4.36 -6.76 4.86 -11.51 5.57 -19.82 c 0.47 -5.53 4.34 -8.12 9.77 -8.21 c 6.39 -0.12 12.69 -0.07 19 -0.93 c 4.02 -0.55 7.4 -1.43 11.53 -0.75 c 6.7 1.1 13.44 1.64 20.22 1.62 c 4.607 -0.013 7.523 0.227 8.75 0.72 c 5.96 2.37 5.56 9.73 6.11 15.22 c 0.44 4.34 2.097 8.447 4.97 12.32 c 6.57 8.88 2.19 25.6 5.64 36.36 a 1.14 1.14 0 0 0 2.22 -0.23 c 0.887 -8.36 2.18 -16.45 3.88 -24.27 z z z z","M418.91 167.68q-2.55 11.73-3.88 24.27a1.14 1.14 0 01-2.22.23c-3.45-10.76.93-27.48-5.64-36.36q-4.31-5.81-4.97-12.32c-.55-5.49-.15-12.85-6.11-15.22q-1.84-.74-8.75-.72-10.17.03-20.22-1.62c-4.13-.68-7.51.2-11.53.75-6.31.86-12.61.81-19 .93-5.43.09-9.3 2.68-9.77 8.21-.71 8.31-1.21 13.06-5.57 19.82-2.68 4.16-3.78 8.57-3.8 13.1-.02 5.29.16 22.47-2.63 24.22a1.02 1.02 0 01-1.56-.77q-1.14-11.78-3.78-23.29-1.48-6.99-1.9-9.7c-2.49-15.94.13-40.13 13.53-51.15 9.39-7.72 28.53-11.63 40.37-11.51 4.2.05 8.74-.3 12.68.22 13.82 1.82 31.67 5.83 39.42 18.92 9.01 15.21 9.88 35.14 5.33 51.99z"],"back":["M1022.74 290.63a.62.61 25.9 01-.36-1.03q1.71-1.83 4.11-3.11c8.19-4.35 19.4-8.3 23.38-17.48q8.48-19.57 8.22-40.85-.05-4.38.57-5.76c1.98-4.38 9.65-3.66 13.85-2.91 4.3.76 4.71 3.25 4.68 7.3q-.2 24.11-.88 48.2c-.12 4.25 1.6 15.84-4.88 16.32-14.57 1.08-32.6 1.81-48.69-.68z","M1095.75 291.46c-4.3-.25-4.9-3.99-4.95-7.71q-.46-29.47-1-58.94c-.13-7.39 11.74-6.23 15.99-4.85 4.2 1.36 3.01 6.89 2.88 10.79-.28 8.88 5.15 41.1 15.32 46.78q8.6 4.81 17.27 9.51 1.97 1.07 3.26 2.36a.8.79 63.6 01-.45 1.35c-16.12 2.17-33.78 1.56-48.32.71z","M1070.06 785.19c2.95 1.36 1.8 10.43 1.49 13.04q-3.98 33.27-14.66 64.61a.39.39 0 01-.76-.17c.9-7.05 2.31-14.29 2.16-20.92q-.68-30.14-18.71-54.52-.29-.39.18-.49c7.42-1.52 23.53-4.69 30.3-1.55z","M1127.24 787.66c-15.99 21.49-22.3 48.51-16.08 74.83a.47.46-63.2 01-.88.29q-1.99-4.69-3.65-10.24-8.29-27.75-11.6-56.54c-.65-5.71-1.1-11.77 6.87-11.9q13-.19 25.68 2.83a.31.24 41.2 01.1.53q-.12.01-.27.07-.1.04-.17.13z","M998.25 1320.52c-4.62.24-8.17-1.08-8.78-6.28-1.6-13.81-.75-28.85-2.16-42.41q-.39-3.74.24-7.03a.69.69 0 011.23-.28c2.35 3.15 4.22 5.75 5.14 9.66 1.54 6.57 1.91 22.57 9.97 24.09q13.33 2.5 15.93-10.47c.92-4.57 1-12.33 5.05-17.25q.42-.51.42.15c.11 14.39.4 30.86-3.08 44.54-.79 3.13-3.31 4.23-6.51 4.4q-8.73.45-17.45.88z","M1149.5 1319.51c-6.93-.63-6.82-18.08-7.14-23.7q-.73-12.53-.59-25.09.01-.71.45-.15 2.74 3.49 3.29 7.17c1.67 11.25 3.21 25.34 19.7 19.99 4.87-1.58 7.03-18.57 7.89-23.21.79-4.2 2.74-7 5.28-10.13a.56.56 0 01.98.22c1.12 4.6.04 12.39-.37 17.26-.92 10.77-.32 21.48-1.52 32.37q-.7 6.23-7.01 6.18-12.13-.11-20.96-.91z","M962.87 1327.38q-.62-.51-.05-1.07l1.99-1.99q.39-.39.93-.41 25.66-.82 51.26 1 1.34.1 4.43 1.47.46.2.69.64 1.84 3.5 2.87 7.23c2.32 8.38-6.63 7.24-12.23 6.68q-15.37-1.53-30.5-4.56c-8.21-1.65-13.33-3.95-19.39-8.99z","M1154.35 1341.35c-12.48 1.36-13.27-3.88-8.67-13.37 1.82-3.76 12.72-3.65 16.39-3.77q19.44-.63 38.9-.44c2.41.02 3.31 1 4.61 2.76q.32.44-.09.79c-5.43 4.67-10.52 7.17-17.95 8.74q-16.46 3.47-33.19 5.29z","M789.41 726.84c3.98-6.79 9.89-14.6 16.56-20.14a.31.31 0 01.48.35c-4.39 11.06-5.38 21.94-14.02 30.72-5.82 5.93-10.7 9.81-19.04 8.57q-.55-.08-.59-.63c-.24-3.07-.26-7.29 3.1-8.85 4.82-2.26 10.72-5.28 13.51-10.02z","M807.27 745.31c17.61 3.49 2.75 13.52-.73 18.99q-10.05 15.82-21.86 30.37-1.56 1.92-2.52-.58a2.41 2.33-55.4 01-.16-.96q.2-5.26 2.75-9.71c6.94-12.09 13.12-24.52 19.72-36.79q.91-1.7 2.8-1.32z","M819.3 744.82c-7.79-6.06-14.51-12.4-11.88-23.38 3.07-12.83 14.66-20.7 25.14-26.38 9.57-5.18 37.61-.75 37.6 13.68q-.01 16.24-3.67 31.99c-2.38 10.26-4.49 16.44-16.87 16.3-10.71-.13-21.93-5.7-30.32-12.21z","M827.99 758.27a2.08 2.07 26.6 01.91 2.73q-10.47 22.03-19.66 45.04-2.25 5.63-8.23 6.74a1.45 1.44 84.3 01-1.7-1.4q-.1-4.29 1.51-8.31 7.3-18.34 13.86-36.96c.74-2.1 1.53-6.08 2.97-8.96q.26-.5.82-.57 5.05-.64 9.52 1.69z","M841.68 762.32a.76.75-79.1 01.6.89q-4.51 23.14-9.28 45.87c-.73 3.49-2.09 5.73-5.85 5.43q-.52-.04-.61-.56-.74-4.54-.32-7.21 2.89-18.57 5.59-37.18.38-2.65 1.67-8.22.13-.54.68-.44l7.52 1.42z","M854.75 799.53a.78.78 0 01-1.37-.02q-.91-1.75-1.15-4.29-1.62-16.58-1.2-33.25a.84.84 0 01.61-.78l7.09-1.93q.59-.16.56.45-.58 14.77-1.12 29.56c-.14 4.06-1.54 6.86-3.42 10.26z","M1336.39 751.96c-8.72 4.49-29.38 10.28-33.61-3.6q-5.68-18.65-5.83-38.24c-.06-7.59 4.01-11.75 11.09-14.08 8.85-2.92 19.02-5.3 27.54-.35 8.74 5.09 18.39 11.28 22.45 21.01 3.05 7.3 3.34 13.66-1.78 20.01-5.21 6.47-12.49 11.45-19.86 15.25z","M1374.32 737.5c-8.05-8.14-9.61-19.67-13.85-30.75a.22.22 0 01.35-.24q10.3 8.96 17.1 20.77c2.57 4.47 9.08 7.59 13.57 9.79 3.11 1.52 2.96 5.9 2.71 8.73q-.05.52-.57.59c-8.87 1.17-13.48-2.98-19.31-8.89z","M1383.76 795.45c-.59-.21-.96-.17-1.39-.68-8.84-10.3-15.85-21.5-23.44-32.41-2.81-4.02-8.81-7.64-7.45-13.14q.15-.6.7-.84l7.85-3.44q.66-.29 1.13.25 2.36 2.73 4.17 6.49 7.36 15.23 16.89 31.47c2.33 3.96 3.04 7.59 2.32 11.85a.58.58 0 01-.78.45z","M1365.79 812.62c-2.7-.28-6.42-2.66-7.49-5.33q-8.74-21.76-19.85-45.74c-2.12-4.58 6.55-5.17 9.12-5.21 1.8-.03 1.93.71 2.38 2.18q5.72 18.34 15.35 42.12c.74 1.84 4.81 12.43.49 11.98z","M1308.16 759.17l7.44 2.1q.23.07.24.31.75 16.26-.86 32.41-.3 3-1.25 5.48a.79.79 0 01-1.42.12q-3.9-6.58-3.82-13.9.16-13.07-.83-26.11-.05-.57.5-.41z","M1340.07 814.35c-2.7.82-4.99-1.16-5.54-3.71q-5.06-23.49-9.82-47.47a.77.76-10.7 01.62-.9l7.52-1.38q.59-.11.73.47c2.08 8.53 3.26 19.85 4.22 25.75q2.09 12.92 3.19 21.14.34 2.54-.33 5.46a.86.84 88.4 01-.59.64z","M1028.14 166.45c1.03 5.06 1.36 9.61 6.41 11.53 13.06 4.95 16.74 15.51 23.52 27.48 1.387 2.447 3.863 3.623 7.43 3.53a910.025 910.025 0 0136.94-.25c6.23.09 9.27-7.55 11.48-12.3 4.31-9.27 10.37-15.83 20.28-18.94.333-.1.603-.287.81-.56 1.92-2.58 3.043-5.43 3.37-8.55l2.31-1.51a.977.977 0 01.99-.08c11.92 5.42-3.35 35.31-8.21 42.45-.761 1.11-2.423 1.028-3.06-.15l-1.26-2.32c-.133-.253-.32-.297-.56-.13-.34.24-.48.61-.42 1.11.86 7.64.75 16.87-2.96 23.31-.173.3.839.041-3.7 4.71-3.34 3.436-74.18 3.78-75.48-1.38a1.465 1.465 0 00-.55-.82c-4.15-2.97-6.07-7.95-6.16-12.39-.03-1.68.18-14.28-.53-14.63-.207-.1-.33-.037-.37.19-.3 1.553-1.183 2.597-2.65 3.13a.951.951 0 01-1.07-.32c-7.29-9.56-12.32-22.18-12.97-33.54-.34-6.04 1.797-9.23 6.41-9.57zm29.95 61.71c.173 14.187 18.967 14.703 19.1-1.37.03-4.05-.38-6.54-4.68-7.3-4.2-.75-11.87-1.47-13.85 2.91-.413.92-.603 2.84-.57 5.76zm31.71-3.35c.36 19.647 18.59 14.82 18.87 5.94.13-3.9 1.32-9.43-2.88-10.79-4.25-1.38-16.12-2.54-15.99 4.85z","M1138.38 168.39q-.49 4.68-3.37 8.55-.31.41-.81.56c-9.91 3.11-15.97 9.67-20.28 18.94-2.21 4.75-5.25 12.39-11.48 12.3q-18.46-.25-36.94.25-5.35.14-7.43-3.53c-6.78-11.97-10.46-22.53-23.52-27.48-5.05-1.92-5.38-6.47-6.41-11.53q-6.64-26.16 4.43-48.88c8.13-16.7 34.61-21.41 51.58-21.04 4.89.11 9.69-.11 14.42.85 18.79 3.8 33.17 8.5 39.34 28.66q6.38 20.88.47 42.35z"]}},"fem":{"box":{"front":"0 0 724 1448","back":"724 0 724 1448"},"front":[{"m":"traps","d":"m 241.01,294.28 c 12.76,-8.28 25.21,-16.07 39.54,-21.44 2.58,-0.96 6.02,0.04 5.63,3.51 q -0.44,3.92 -1.62,5.3 c -6.48,7.58 -12.74,12.81 -19.24,19.14 -1.03,1.01 -1.87,1.15 -3.25,1.03 q -8.13,-0.69 -18.79,-2.97 -2.99,-0.65 -2.7,-3.86 0.05,-0.46 0.43,-0.71 z"},{"m":"traps","d":"m 366.31,275.29 c 12,4.97 22.67,11.96 33.55,18.94 q 0.51,0.33 0.54,0.94 c 0.08,1.63 -0.71,3.2 -2.44,3.59 q -9.65,2.18 -18.62,2.99 c -1.26,0.11 -2.4,0.24 -3.39,-0.7 q -5.03,-4.78 -10.97,-10.25 -6.53,-6.01 -8.56,-9.16 -1.87,-2.89 -1.63,-6.77 0.03,-0.49 0.35,-0.85 c 3.04,-3.4 7.94,-0.07 11.17,1.27 z"},{"m":"shoulders","d":"m 215.94,289.45 c -21.5,15.97 -35.81,41.69 -45.18,66.64 a 0.54,0.54 0 0 1 -0.75,0.3 c -1.36,-0.68 -2.91,-1.32 -2.88,-3.16 q 0.36,-18.38 4.6,-33.36 c 4.12,-14.55 12.32,-23.54 25.35,-31.51 5.06,-3.09 15.01,-5.79 18.99,0.41 a 0.51,0.51 0 0 1 -0.13,0.68 z"},{"m":"shoulders","d":"m 233,326.79 c -5.53,27.66 -26.78,45.52 -55.22,43.68 a 0.54,0.54 0 0 1 -0.5,-0.61 c 4.48,-32.74 28.09,-58.38 54.53,-76.41 q 2.3,-1.57 4.86,-0.86 a 1.61,1.61 0 0 1 1.18,1.72 Q 236,311.81 233,326.79 Z"},{"m":"shoulders","d":"M 470.13,355.96 C 463.5,338.45 454.92,322.09 443.6,307.63 q -8.16,-10.43 -18.47,-18.02 a 0.95,0.94 33.2 0 1 -0.26,-1.22 c 3.34,-5.94 13.06,-3.27 17.74,-0.68 13.57,7.5 23.02,17.38 27.32,32.28 q 3.99,13.8 4.29,33.5 0.02,0.68 -0.47,1.17 -1.06,1.07 -2.71,1.71 a 0.71,0.71 0 0 1 -0.91,-0.41 z"},{"m":"shoulders","d":"m 463.02,370.48 c -23.85,1.37 -44.22,-11.24 -52.35,-33.91 q -2.31,-6.45 -3.64,-14.34 -2.64,-15.63 -3.89,-27.26 -0.28,-2.56 2.36,-2.63 1.96,-0.05 4.36,1.61 c 25.95,18.03 49.33,43.22 53.78,75.78 q 0.1,0.71 -0.62,0.75 z"},{"m":"chest","d":"m 215.31,402.8 c 3.26,-19.61 9.28,-38.69 19.81,-54.85 q 13.83,-21.23 37.68,-27.16 c 4.19,-1.04 8.16,-1.75 12.65,-1.52 q 12.07,0.63 26.16,3.99 1.78,0.43 1.87,2.25 1.29,25.82 3.16,54.8 0.99,15.38 0.3,27.45 -0.37,6.41 -1.94,10.48 c -1.58,4.08 -6.16,7.97 -9.72,10.84 -4.33,3.5 -10.61,4.16 -16,3.9 q -10.92,-0.53 -23.26,-4.83 c -17.71,-6.16 -33.56,-14.26 -50.09,-24.02 a 1.31,1.29 20.5 0 1 -0.62,-1.33 z"},{"m":"chest","d":"m 363.91,319.81 c 13.87,2.63 26.6,9.35 35.84,20.13 14.46,16.89 22.21,40.73 25.93,63.05 q 0.11,0.67 -0.48,1.03 -23.28,13.86 -44.52,22.12 c -11.73,4.57 -25.54,8.27 -37.94,6.2 -5.84,-0.97 -14.69,-8.68 -16.85,-14.18 q -1.62,-4.13 -2.04,-13.18 -0.45,-9.71 0.29,-21.75 1.69,-27.64 3.41,-58.27 a 1.18,1.16 -22 0 1 0.33,-0.76 q 0.76,-0.78 1.88,-1.05 11.52,-2.78 22.73,-3.71 6.58,-0.54 11.42,0.37 z"},{"m":"biceps","d":"m 155.35,437.19 c 0.88,-13.89 5.09,-26.01 11.66,-38.24 4.28,-7.95 13.01,-13.06 21.68,-14.64 q 5.04,-0.91 8.47,-1.31 c 3.81,-0.44 9.89,1.7 10.16,6.4 q 0.6,10.54 -0.27,19.31 c -1.87,18.75 -10.06,34.69 -22.69,48.75 q -8.64,9.63 -17.84,18.84 a 1.95,1.9 16.8 0 1 -0.98,0.51 q -3.19,0.6 -7.01,-0.16 c -1.52,-0.3 -2.45,-1.78 -2.7,-3.22 -0.63,-3.64 -1.32,-7.25 -1.31,-10.96 q 0.05,-12.9 0.83,-25.28 z"},{"m":"biceps","d":"m 478.52,408.27 q 5.57,13 6.67,24.56 1.21,12.62 1.22,28.51 0,5.95 -1.27,12.12 c -0.81,3.98 -5.47,3.81 -8.54,3.62 q -1.7,-0.11 -3.27,-1.75 c -13.93,-14.57 -28.48,-28.44 -35.35,-48.06 -4.33,-12.36 -5.34,-25.85 -4.21,-39.01 q 0.06,-0.73 0.51,-1.3 c 2.72,-3.44 6.57,-4.44 11.44,-3.76 q 7.25,1.01 11.78,2.52 9.47,3.16 14.86,10.89 3.02,4.33 6.16,11.66 z"},{"m":"triceps","d":"m 203.67,452.99 c -7.1,14.55 -18.61,30.01 -32.29,39.49 q -2.32,1.61 -4.85,0.35 l -0.31,-0.15 q -2.42,-1.22 -0.37,-2.98 c 17.2,-14.83 28.72,-32 37.23,-52.62 q 0.8,-1.93 2.34,-3.04 a 1.28,1.28 0 0 1 1.8,0.31 q 1.35,1.95 0.97,4.41 -1.1,7.22 -4.52,14.23 z"},{"m":"triceps","d":"m 442.12,446.42 q 12.14,25.77 33.26,43.55 1.47,1.24 -0.14,2.3 l -0.63,0.42 a 4.39,4.37 45.8 0 1 -4.92,-0.08 q -8.08,-5.68 -15.21,-13.9 c -9.9,-11.44 -19.12,-25.17 -21.69,-39.38 q -0.62,-3.4 1.39,-5.43 0.44,-0.44 0.99,-0.15 1.54,0.81 2.17,2.22 2.35,5.31 4.78,10.45 z"},{"m":"obliques","d":"m 234.32,446.92 c -0.55,-2.95 -0.73,-5.84 -1.94,-8.63 q -4.84,-11.24 -8.61,-21.89 -1.12,-3.17 1.19,-0.73 c 6.09,6.44 10.93,13.52 16.12,20.74 a 1.71,1.68 28.7 0 1 0.33,1.09 q -0.53,11.15 -1.88,22.64 -0.27,2.38 -1.24,0.18 -2.52,-5.75 -3.97,-13.4 z"},{"m":"obliques","d":"m 242.67,456.5 c 3.65,1.14 7.61,3.78 10.54,6.08 q 2.32,1.82 2.25,4.78 l -0.21,8.13 q -0.04,1.79 -1.45,0.68 -5.7,-4.52 -10.18,-10.21 c -1.95,-2.48 -2.49,-5.57 -2.19,-8.64 a 0.96,0.96 0 0 1 1.24,-0.82 z"},{"m":"obliques","d":"m 246.6,490.48 c -6.03,-4.05 -7.7,-15.15 -7.34,-21.92 a 0.29,0.29 0 0 1 0.5,-0.19 c 4.79,4.87 9.23,10.01 14.82,13.93 a 1.71,1.7 17.7 0 1 0.72,1.39 l -0.03,11.84 a 0.6,0.6 0 0 1 -0.96,0.47 q -4.04,-3.05 -7.71,-5.52 z"},{"m":"obliques","d":"m 252.02,456.3 c -3.08,-1.43 -6.59,-6.27 -8.84,-9.51 q -1.03,-1.49 -0.89,-3.31 a 1.39,1.38 -88.2 0 1 1.35,-1.28 q 2.27,-0.04 4.18,0.73 4.36,1.77 9.23,3.14 0.54,0.15 0.58,0.71 0.4,6.12 -1.21,11.67 a 0.35,0.35 0 0 1 -0.56,0.18 q -1.73,-1.34 -3.84,-2.33 z"},{"m":"obliques","d":"m 257.8,616.66 c -4.79,-0.24 -10.33,-3.88 -14.25,-6.47 -3.22,-2.12 -4.01,-14.52 -4.21,-18.14 q -0.65,-12.12 -0.53,-21.74 0.32,-25.23 0.51,-50.71 c 0.03,-4.43 0.42,-8.84 0.73,-13.1 q 0.3,-4 1.6,-7.53 0.76,-2.05 2.82,-1.27 c 12.29,4.68 12.69,15.09 12.12,26.28 q -0.16,3.03 0.13,5.86 2.02,19.75 1.8,41.33 -0.05,4.7 0.76,9.52 1.4,8.43 2.19,17.36 0.85,9.42 -2.51,17.86 a 1.18,1.17 -77.7 0 1 -1.16,0.75 z"},{"m":"obliques","d":"m 259.53,441.27 c -6.09,-0.94 -15.24,-7.47 -17.83,-12.96 a 1.96,1.95 -46.6 0 1 -0.04,-1.54 c 0.68,-1.67 2.51,-1.45 3.82,-0.88 q 8.66,3.77 17.36,6.42 a 0.45,0.45 0 0 1 0.31,0.35 q 0.79,4.96 -2.71,8.32 -0.38,0.37 -0.91,0.29 z"},{"m":"obliques","d":"m 384.22,445.83 c 4.32,-0.61 8.05,-3.04 12.37,-3.63 q 1.44,-0.19 1.87,1.19 0.52,1.66 -0.54,3.18 c -4.17,5.96 -7.23,9.14 -12.91,11.89 a 0.38,0.38 0 0 1 -0.53,-0.24 q -1.53,-5.28 -1.13,-11.45 a 1.03,1.01 88.3 0 1 0.87,-0.94 z"},{"m":"obliques","d":"m 393.51,426.7 q 1.7,-0.72 3.57,-1.16 a 1.83,1.83 0 0 1 1.91,0.73 c 0.66,0.94 0.41,2.29 -0.25,3.15 q -6.32,8.23 -15.71,11.52 -3.03,1.06 -4.1,-1.97 -1.12,-3.14 -1.26,-3.53 -1.02,-2.87 1.9,-3.71 6.45,-1.87 13.94,-5.03 z"},{"m":"obliques","d":"m 397.15,610.37 c -3.94,2.47 -9.2,6.06 -13.86,6.26 a 1.49,1.48 77.9 0 1 -1.43,-0.93 q -3.33,-8.47 -2.36,-18.41 0.73,-7.6 1.89,-14.77 1,-6.17 0.99,-11.23 c -0.02,-13.26 0.27,-26.67 1.74,-39.85 q 0.47,-4.28 0.19,-8.9 c -0.66,-10.78 0.81,-21.31 12.66,-24.94 a 1.78,1.77 69.5 0 1 2.14,0.98 q 1,2.27 1.34,4.92 1.19,9.27 1.26,20.24 0.16,23.43 0.38,48.3 c 0.09,11.08 -0.18,22.85 -2.37,33.89 -0.34,1.72 -0.99,3.45 -2.57,4.44 z"},{"m":"obliques","d":"m 398.41,456.38 a 0.8,0.8 0 0 1 1.09,0.63 c 0.86,5.62 -1.96,9.16 -5.73,13.05 q -3.65,3.78 -7.3,6.56 a 0.41,0.41 0 0 1 -0.66,-0.3 q -0.29,-4.61 -0.24,-8.98 0.03,-3.34 2.9,-5.44 4.69,-3.42 9.94,-5.52 z"},{"m":"obliques","d":"m 401.76,469.17 c 0.19,6.21 -1.1,14.25 -5.22,19.11 -2.62,3.1 -6.7,5.34 -10.07,7.78 q -0.73,0.53 -0.74,-0.38 l -0.07,-12.12 q 0,-0.59 0.46,-0.96 8.07,-6.31 14.53,-13.82 1.06,-1.24 1.11,0.39 z"},{"m":"obliques","d":"m 417.44,415.21 c -2.14,7.47 -5.47,14.6 -8.52,22.06 q -1.36,3.31 -1.79,6.59 -1.11,8.53 -4.58,16.68 -0.79,1.86 -1.07,-0.14 -1.42,-10.38 -1.9,-22.98 a 1.58,1.57 -28.9 0 1 0.29,-1 q 6.57,-9.46 12.8,-17.21 1.87,-2.32 4.3,-4.31 0.73,-0.59 0.47,0.31 z"},{"m":"abs","d":"m 261.73,482.62 c 0.12,-10.88 -0.33,-22.8 7.93,-31.04 10.43,-10.39 25.75,-12.91 39.95,-10.63 q 0.47,0.08 0.45,0.54 l -0.87,24.33 a 0.96,0.95 88.3 0 1 -0.86,0.92 q -8.51,0.88 -17.53,2.35 c -11.22,1.83 -20.06,5.16 -28.09,13.92 q -1,1.08 -0.98,-0.39 z"},{"m":"abs","d":"m 263.89,560.38 c 18.37,-2.6 32.63,0.35 44.32,15.16 q 0.72,0.91 0.91,2.02 0.98,5.72 1.19,10.92 2.01,48.7 2.07,81.21 0.01,2.91 -2.63,5.03 c -2.77,2.21 -10.61,5.88 -13.52,2.38 -6.1,-7.32 -10.49,-16.08 -14.05,-25.36 q -14.5,-37.75 -18.65,-78.2 -0.6,-5.83 -0.33,-12.39 0.03,-0.68 0.69,-0.77 z"},{"m":"abs","d":"m 283.92,476.78 q 12.55,-5.43 25.84,-3.32 0.36,0.06 0.36,0.42 0.41,19.6 -0.86,38.86 a 0.91,0.91 0 0 1 -1,0.85 c -13.44,-1.32 -28.43,-1.82 -41.54,1.88 q -1.32,0.37 -1.93,-0.86 c -9,-18.41 3.35,-31 19.13,-37.83 z"},{"m":"abs","d":"m 300.31,556.68 q -4.15,-2.21 -8.98,-2.91 -13.66,-2 -27.37,-1.31 a 0.95,0.95 0 0 1 -0.97,-0.72 q -2.73,-10.93 -1.37,-23.2 0.2,-1.78 1.77,-2.92 c 6.36,-4.65 14.69,-8.3 22.33,-8.94 q 12.38,-1.05 23.44,4.26 a 2.06,2.06 0 0 1 1.17,1.89 q -0.3,18.75 -1.38,35.86 -0.03,0.52 -0.55,0.61 c -2.7,0.45 -5.69,-1.34 -8.09,-2.62 z"},{"m":"abs","d":"m 349.65,553.77 c -5.34,0.78 -9.22,3.16 -13.94,5.24 q -1.59,0.69 -3.28,0.24 a 0.64,0.63 -83.8 0 1 -0.47,-0.59 q -1.23,-19.64 -1.28,-36.09 a 1.7,1.68 -13.4 0 1 0.95,-1.53 c 15.64,-7.64 31.83,-5.42 45.86,4.48 0.94,0.67 1.65,1.44 1.78,2.61 q 1.39,12.16 -1.2,23.23 -0.25,1.07 -1.34,1.03 -13.86,-0.54 -27.08,1.38 z"},{"m":"abs","d":"m 354.08,475.63 c 16.7,6.15 32.39,19.94 21.71,39.55 a 0.9,0.9 0 0 1 -1.05,0.43 c -13.38,-3.92 -27.7,-3.22 -41.96,-2.05 a 0.9,0.9 0 0 1 -0.97,-0.78 c -1.62,-12.74 -1.1,-26.27 -1.03,-38.85 a 0.48,0.47 86.4 0 1 0.4,-0.46 q 11.86,-1.91 22.9,2.16 z"},{"m":"abs","d":"m 362.23,642.32 c -4.21,12.26 -9.35,24.52 -17.49,34.79 q -0.9,1.12 -2.2,1.22 -6.54,0.54 -11.69,-3.91 c -1.34,-1.16 -2.37,-2.61 -2.35,-4.44 q 0.27,-32.33 1.7,-73.01 0.36,-10.37 1.51,-19.31 0.12,-0.89 0.67,-1.6 c 11.52,-15 26.64,-18.71 44.72,-15.65 a 0.85,0.84 -86 0 1 0.71,0.83 q 0.06,12.43 -1.93,25.22 -4.56,29.34 -13.65,55.86 z"},{"m":"abs","d":"m 378.34,483.09 c -8.04,-8.87 -17.3,-12.26 -29.19,-14.16 q -8.48,-1.35 -16.49,-2.17 a 1.05,1.04 2.2 0 1 -0.93,-1 l -0.83,-24.24 q -0.02,-0.53 0.51,-0.61 18.35,-2.62 31.52,4.43 14.18,7.59 15.53,22.29 0.76,8.32 0.7,15.15 -0.01,1.2 -0.82,0.31 z"},{"m":"forearms","d":"m 80.93,647.88 c 14.1,-28.26 32.44,-54.05 52.98,-78.01 6.28,-7.32 12.52,-13.04 22.3,-14.76 a 0.44,0.44 0 0 1 0.44,0.67 q -33.59,51.14 -75.12,92.53 -1.62,1.61 -0.6,-0.43 z"},{"m":"forearms","d":"m 85.85,647.42 q 11.51,-11.53 23.19,-23.33 6.63,-6.71 16.74,-19.55 c 13.51,-17.18 26.63,-37.24 39.52,-56.02 a 1.08,1.08 0 0 1 1.85,0.11 q 0.51,0.96 0.26,2.09 c -1.73,8.15 -5.44,14.39 -10.55,22.73 q -11.14,18.18 -25.7,34.1 -18.98,20.74 -39.21,37.61 -2.7,2.25 -6.02,3.82 -0.28,0.13 -0.57,0.24 -1.42,0.54 -1.26,-0.09 0.03,-0.14 0.14,-0.23 0.8,-0.67 1.61,-1.48 z"},{"m":"forearms","d":"m 158.49,493 c -4.72,28.27 -19.05,55.5 -36.58,78.04 q -10.8,13.89 -20.36,28.46 -4.33,6.6 -7.59,13.38 c -5.52,11.5 -11.24,22.8 -19.41,32.11 q -1.49,1.7 -0.81,-0.46 c 3.77,-11.82 9.38,-23.48 14.74,-35.02 3.82,-8.21 7.5,-17.64 10.42,-26.75 7.62,-23.71 16.99,-47.77 29.34,-68.38 7.41,-12.37 17.14,-24.67 29.9,-31.38 a 0.72,0.71 -14.7 0 1 1.05,0.61 q 0.12,4.42 -0.7,9.39 z"},{"m":"forearms","d":"m 176.77,536.74 c -5.58,2.64 -11.34,4.68 -16.45,7.91 -5.83,3.68 -11.93,7.82 -18.84,8.99 a 1.21,1.21 0 0 1 -1.41,-1.13 c -0.29,-5.42 4.57,-12.34 7.66,-16.52 q 10.15,-13.75 23.71,-26.94 c 1.35,-1.32 3.26,-2.11 5.16,-1.83 a 1.02,1.02 0 0 1 0.86,0.86 q 2.03,13.42 -0.05,27.79 a 1.12,1.12 0 0 1 -0.64,0.87 z"},{"m":"forearms","d":"m 478.46,543.49 c -4.04,-2.35 -8.89,-4.3 -13.28,-6.25 -2.13,-0.95 -1.9,-3.18 -2.11,-5.15 q -1.32,-12.65 0.43,-23.9 a 1.38,1.38 0 0 1 1.54,-1.16 c 1.68,0.21 3.26,0.93 4.5,2.12 q 12.39,11.96 22.31,25.05 c 2.67,3.51 10.26,13.94 8.99,18.38 a 1.51,1.51 0 0 1 -1.8,1.06 q -5.3,-1.25 -8.83,-3.31 -5.39,-3.14 -11.75,-6.84 z"},{"m":"forearms","d":"m 480.72,568.04 c -3.26,-5.55 -5.96,-11.12 -7.24,-17.31 q -0.33,-1.61 0.58,-2.51 a 1.03,1.03 0 0 1 1.57,0.16 c 13.24,19.66 27,40.11 41.05,58.18 11.56,14.88 25.47,28 38.8,41.13 q 0.56,0.55 1.06,1.21 0.78,1.05 -0.44,0.58 -3.39,-1.29 -6.75,-4.08 -19.67,-16.3 -37.87,-35.97 -17.53,-18.94 -30.76,-41.39 z"},{"m":"forearms","d":"m 484.61,555.07 c 11.21,1.91 17.39,8.95 24.45,17.26 18.64,21.97 34.4,44.56 47.76,69.37 q 1.9,3.52 3.32,6.62 0.61,1.33 -0.43,0.3 -42.24,-42.1 -75.48,-92.97 -0.48,-0.73 0.38,-0.58 z"},{"m":"forearms","d":"m 540.63,578.72 c 5.89,18.68 13.76,35.38 21.55,52.65 q 2.61,5.79 4.88,12.9 0.81,2.52 -0.89,0.5 c -7.48,-8.88 -12.5,-17.92 -17.97,-29.24 q -6.73,-13.92 -15.23,-25.62 -8.17,-11.25 -17.48,-23.63 c -15.23,-20.26 -27.78,-45.5 -32.6,-70.62 q -1.21,-6.31 -1.13,-11.97 0.01,-1.19 1.06,-0.64 c 10.4,5.42 18.51,14.3 25.31,24.31 13.82,20.33 24.84,47.05 32.5,71.36 z"},{"m":"quads","d":"m 250.69,888.3 q -16.76,-40.15 -28.04,-78.67 -9.66,-32.96 -14.75,-66.84 c -6.22,-41.38 -6.25,-81.5 4.58,-120.54 q 0.12,-0.43 0.43,-0.11 0.42,0.42 0.59,0.99 22.71,76.86 50.06,155.88 0.82,2.37 1.12,4.78 4.02,31.91 4.89,65.81 c 0.88,34.39 -0.14,66.83 -3.55,99.57 -0.42,4.08 -1.48,7.64 -3.4,11.1 a 1.74,1.74 0 0 1 -1.36,0.9 c -3.47,0.33 -9.58,0.21 -9.46,-4.63 q 0.8,-32.72 -0.01,-63.12 -0.07,-2.67 -1.1,-5.12 z"},{"m":"quads","d":"m 237.75,955.7 c -18.56,-47.89 -33.57,-96.23 -39.84,-146.62 -3.58,-28.72 -3.69,-58.65 1.76,-86.95 q 0.62,-3.18 0.81,0.06 0.5,8.53 1.83,17.04 3.42,21.88 10.35,47.5 6.6,24.41 13.5,49.26 2.72,9.81 5.92,19.09 4.05,11.74 7.55,25.02 1.76,6.7 2.16,12.33 2,28.5 1.14,55.08 c -0.12,3.52 -0.98,6.35 -3.98,8.55 a 0.79,0.79 0 0 1 -1.2,-0.36 z"},{"m":"quads","d":"m 301.11,919.16 c -0.02,16.26 -1.13,34.39 -10.87,47.81 -1.99,2.73 -6.98,1.8 -10.02,1.14 -1.09,-0.24 -1.35,-1.1 -1.62,-2.04 -3.49,-11.83 -3.03,-24.65 -1.79,-37.03 q 1.41,-14.09 3.35,-26.37 1.6,-10.13 1.54,-18.61 -0.09,-12.6 -1.48,-24.36 c -1.65,-13.85 -3.88,-27.85 -2.4,-41.77 a 0.32,0.32 0 0 1 0.4,-0.28 q 0.4,0.1 0.57,0.48 c 14.17,32.12 22.37,65.88 22.32,101.03 z"},{"m":"quads","d":"m 390.96,886.41 q -1.98,4.65 -2.04,9.09 -0.45,33.06 0.22,61.74 c 0.09,3.94 -6.78,4.21 -9.43,3.95 a 1.8,1.79 -10.8 0 1 -1.44,-0.97 c -1.51,-2.91 -2.67,-5.71 -3.07,-9.04 q -1.93,-16.27 -2.84,-32.46 c -2.55,-44.99 -1.64,-90.5 3.77,-134.26 q 0.37,-2.95 1.26,-5.54 26.45,-76.29 50.24,-156.54 a 0.39,0.38 21.6 0 1 0.55,-0.23 q 0.31,0.16 0.41,0.51 c 10.18,37.82 10.57,76.54 5.14,115.55 q -4.46,32.04 -13.33,63.85 -12.28,44.03 -29.44,84.35 z"},{"m":"quads","d":"m 441.32,723.15 q 5.4,27.88 3.91,59.43 c -1.5,31.66 -7.16,62.78 -15.56,93.66 q -10.93,40.18 -26.56,79.46 a 0.8,0.8 0 0 1 -1.23,0.34 c -4.09,-3.18 -3.84,-7.55 -3.96,-12.09 q -0.59,-24.2 1.07,-50.23 0.42,-6.4 2.05,-12.77 3.38,-13.22 8.06,-26.98 3.21,-9.44 5.93,-19.09 c 7.96,-28.15 15.34,-53.97 20.99,-81.11 q 3.3,-15.87 4.53,-32.62 0.01,-0.1 0.11,-0.14 l 0.25,-0.09 q 0.11,-0.04 0.12,0.07 0.06,0.98 0.29,2.16 z"},{"m":"quads","d":"m 350.02,966.12 c -9.24,-14.15 -10.21,-31.4 -10.24,-48.19 -0.07,-33.99 8.51,-69.02 22.57,-100.06 a 0.51,0.49 -76.6 0 1 0.45,-0.29 q 0.28,0 0.31,0.28 c 0.83,7.48 0.62,15.88 -0.25,23.21 -1.72,14.59 -3.74,28.99 -3.61,44.11 q 0.06,8 1.44,16.9 1.82,11.87 3.26,24.93 c 1.49,13.54 2.18,28.24 -1.97,40.04 a 1.57,1.55 2.7 0 1 -1.11,1 q -3.84,0.9 -7.13,0.61 -2.19,-0.19 -3.72,-2.54 z"},{"m":"calves","d":"m 273.89,1099.95 c 3.37,-15.16 7.39,-30.47 12.61,-44.88 q 0.67,-1.85 1.28,0.01 4.41,13.38 7.29,29.13 3.29,18.03 6.8,44.66 2.68,20.36 2.76,35.74 0.04,6.36 -1.26,13.77 -3.67,20.9 -7.39,37.94 -6.52,29.95 -12.99,57.95 -0.31,1.31 -0.67,0.01 -3.32,-11.98 -5.27,-24.29 -5.82,-36.85 -4.93,-74.51 c 0.46,-19.33 0.09,-35.61 0.04,-57.98 q -0.03,-9.65 1.73,-17.55 z"},{"m":"calves","d":"m 272.32,1289.43 q 0.14,1.3 -0.7,0.3 -1.96,-2.32 -3.31,-4.94 -4.85,-9.47 -8.79,-20.24 -9.58,-26.21 -15.83,-53.09 -1,-4.3 -2.77,-8.19 c -8,-17.6 -12.77,-36.6 -13.83,-55.89 q -1.45,-26.23 4.89,-54.26 0.41,-1.85 1,-0.05 1.2,3.7 2.35,7.92 c 6.05,22.14 11.41,47.6 16.75,71.19 q 2.42,10.71 3.46,19.48 2.54,21.38 5.09,39.58 c 2.14,15.29 4.57,31.52 9.27,46.7 q 1.76,5.67 2.42,11.49 z"},{"m":"calves","d":"m 351.12,1061.47 q 0.86,-3.24 2.27,-6.95 a 0.37,0.37 0 0 1 0.67,-0.04 q 0.6,1.15 1.07,2.55 4.5,13.38 8.77,29.36 3.17,11.88 4.35,21.44 0.65,5.26 0.58,16.16 c -0.12,17.19 -0.49,33.53 -0.01,50.26 q 0.96,33.46 -3.97,69.04 -2.3,16.62 -6.18,31.23 -0.05,0.18 -0.19,0.29 a 0.25,0.25 0 0 1 -0.39,-0.14 c -7.63,-32.4 -14.66,-63.57 -20.38,-95.86 q -1.46,-8.28 -1.39,-15.2 0.13,-12.7 1.63,-25.36 c 2.86,-24.12 6.38,-51.37 13.17,-76.78 z"},{"m":"calves","d":"m 400.56,1202.06 c -1.03,2.29 -2.11,4.68 -2.7,7.11 -5.58,23.36 -12.43,46.15 -21.19,67.15 q -3.13,7.49 -7.08,13.12 -0.19,0.27 -0.47,0.44 -0.68,0.4 -0.57,-0.38 0.79,-5.61 2.43,-11.29 5.47,-18.91 8.27,-39.41 3.36,-24.7 6.32,-48.64 1.04,-8.33 3.58,-19.59 5.82,-25.8 11.4,-49.84 3.49,-14.99 7.21,-27.24 0.18,-0.58 0.63,-1.01 0.33,-0.31 0.43,0.13 c 8.56,36.96 7.32,74.95 -8.26,109.45 z"}],"back":[{"m":"traps","d":"m 1135.46,406.92 c -18.92,-8.6 -32.13,-23.67 -41.02,-42.55 q -5.92,-12.58 -12.01,-29.57 c -8.17,-22.8 -18.79,-46.58 -45.58,-51.11 q -2.73,-0.46 -0.13,-1.41 4.88,-1.81 11.29,-2.14 31.93,-1.68 62.2,8.18 c 8.7,2.84 17.39,6.76 24.41,12.87 q 2.26,1.96 2.37,4.56 c 1.34,31.32 1.02,63.51 1.17,95.68 q 0.01,2.48 -0.9,4.77 -0.52,1.31 -1.8,0.72 z"},{"m":"traps","d":"m 1207.43,323.66 c -6.47,16.09 -12.05,33.31 -20.26,48.12 q -13.47,24.27 -37.07,35.13 a 1.54,1.53 -20.3 0 1 -2.12,-0.97 q -0.48,-1.66 -0.5,-3.37 -0.3,-43.8 0.71,-95.35 c 0.05,-2.69 0.7,-4.25 2.8,-6.09 q 6.04,-5.32 13.64,-8.64 c 21.78,-9.51 47.77,-13.62 71.23,-12.54 4.41,0.2 9.17,0.92 13.49,2.46 q 1.76,0.62 -0.05,1.06 c -2.97,0.73 -5.89,1.25 -8.8,2.31 -17.14,6.22 -26.43,21.36 -33.07,37.88 z"},{"m":"shoulders","d":"m 1015.26,291.32 c 13.82,-8.28 31.54,1.05 42.43,10.31 a 0.79,0.78 46.2 0 1 -0.02,1.22 c -24.49,19.25 -48.03,41.91 -64.52,68.41 q -0.81,1.29 -0.91,-0.22 c -0.55,-8.02 -1.66,-16.16 -1.47,-24.04 q 0.34,-14.94 4.17,-28.72 4.91,-17.72 20.32,-26.96 z"},{"m":"shoulders","d":"m 1269.98,290.8 c 13.79,8.38 20.48,19.55 22.26,34.89 1.11,9.5 1.29,18.77 0.46,28.54 q -0.73,8.56 -1.29,17.08 -0.08,1.29 -0.8,0.22 c -16.86,-25.21 -37.38,-47.58 -59.38,-68.88 q -0.46,-0.45 0,-0.88 c 9.46,-8.91 18.65,-15.05 32.34,-13.43 2.34,0.28 4.42,1.25 6.41,2.46 z"},{"m":"lats","d":"m 1076.09,338.19 q 6.08,14 8.04,30.55 0.06,0.58 -0.52,0.57 -11.77,-0.05 -23.32,-2.12 -15.59,-2.79 -31.04,-5.99 a 1.37,1.36 12.9 0 1 -1.05,-1.66 c 4.67,-18.66 16.53,-35.54 33.45,-45.02 a 0.84,0.83 59.5 0 1 1.11,0.29 c 4.8,7.66 9.75,15.13 13.33,23.38 z"},{"m":"lats","d":"m 1136.59,472.49 c -4.61,16.03 -15.02,30.53 -26.92,42.6 q -13.41,13.6 -29.37,25.62 -0.65,0.5 -0.73,-0.32 -5.92,-63.46 -22.92,-123.09 -4.96,-17.4 -9.99,-34.68 a 0.7,0.7 0 0 1 0.74,-0.89 q 8.92,0.83 16.32,1.52 10.81,1.01 19.97,-4.41 0.54,-0.32 1,0.1 1.78,1.59 3.48,3.41 22.52,24.07 30.7,32.06 7.98,7.79 16.19,14.02 0.79,0.6 1.05,1.52 6.33,22.2 0.48,42.54 z"},{"m":"lats","d":"m 1208.41,340.35 c 3.56,-8.92 9.08,-17.44 14.24,-25.41 a 0.92,0.91 29.5 0 1 1.18,-0.32 c 16.77,8.66 28.69,26.73 33.4,44.65 q 0.44,1.67 -1.25,2 c -18.03,3.6 -36.04,7.81 -54.02,8.1 q -0.68,0.01 -0.6,-0.67 1.86,-15.33 7.05,-28.35 z"},{"m":"lats","d":"m 1238.86,382.59 c -6.36,21.96 -12.97,44.37 -18.07,65.96 q -10.8,45.75 -14.79,91.85 a 0.46,0.46 0 0 1 -0.74,0.33 Q 1184.28,525.02 1169.34,508 c -8.23,-9.38 -15.46,-20.54 -19.54,-32.61 q -6.78,-20.06 -1.17,-43.01 c 0.49,-1.99 0.83,-3.24 2.53,-4.57 17.75,-13.89 33.26,-32.01 49.33,-48.67 q 0.64,-0.67 1.43,-0.19 9.01,5.38 20.51,4.24 7.75,-0.76 15.78,-1.39 a 0.62,0.62 0 0 1 0.65,0.79 z"},{"m":"lowerback","d":"m 1068.62,544.21 c 7.96,11.18 10.18,25.23 10.98,38.79 a 1.16,1.16 0 0 1 -0.56,1.06 l -18.13,10.62 a 0.4,0.39 -13.5 0 1 -0.6,-0.36 q 1.04,-16.62 2.69,-32.09 0.79,-7.45 2.8,-14.26 0.6,-2.03 1.74,-3.74 a 0.66,0.65 -45.9 0 1 1.08,-0.02 z"},{"m":"lowerback","d":"m 1133.23,596.5 c -0.34,6.54 -1.25,13 -1.82,19.57 q -0.07,0.85 -0.6,0.17 -3.62,-4.73 -6.59,-7.98 -9.72,-10.63 -18.63,-20.4 -12.89,-14.11 -23.33,-31.15 a 0.57,0.57 0 0 1 0.2,-0.78 q 7.87,-4.59 14.81,-11.15 c 12.33,-11.64 23.63,-24.56 35.33,-36.99 q 0.87,-0.93 0.88,0.34 c 0.19,29.32 1.27,58.9 -0.25,88.37 z"},{"m":"lowerback","d":"m 1199.62,562.36 c -10.91,16.62 -23.7,30.13 -37.46,44.84 q -3.74,4 -7.34,8.9 -0.68,0.93 -0.82,-0.22 c -1.8,-15.45 -2.6,-31.26 -2.53,-47.01 q 0.14,-30.97 0.58,-60.87 0.02,-1.16 0.8,-0.31 13.63,14.85 25.73,27.71 7.18,7.63 15.56,14.48 4.25,3.47 8.77,5.95 0.51,0.27 0.23,0.78 -1.61,2.85 -3.52,5.75 z"},{"m":"lowerback","d":"m 1205.91,583.12 c 0.67,-13.86 3,-27.66 11.11,-39.08 a 0.57,0.57 0 0 1 0.96,0.06 q 1.93,3.49 2.75,7.64 c 2.78,13.99 3.44,28.55 4.52,42.69 a 0.37,0.36 13.1 0 1 -0.55,0.34 l -18.27,-10.7 a 1.05,1.04 16.6 0 1 -0.52,-0.95 z"},{"m":"triceps","d":"m 976.72,479.34 q -0.46,-17.73 -0.59,-32.11 c -0.22,-24.28 5.62,-47.43 19.17,-67.7 q 0.38,-0.55 0.24,0.1 -9.84,49.08 -17.78,99.12 a 0.83,0.82 -4 0 1 -0.58,0.66 l -0.25,0.08 q -0.21,0.06 -0.21,-0.15 z"},{"m":"triceps","d":"m 990.45,481.03 c -2.39,-3.13 -3.03,-8.64 -3.15,-12.61 q -0.81,-26.18 5.96,-50.71 a 0.37,0.37 0 0 1 0.61,-0.17 q 0.16,0.15 0.19,0.37 c 2.18,16.06 4.1,31.34 3.14,46.56 -0.33,5.2 -1.01,12.19 -4.17,16.53 a 1.61,1.61 0 0 1 -2.58,0.03 z"},{"m":"triceps","d":"m 1028.38,422.24 c 0.27,25.48 -7.52,49.09 -23.37,68.41 q -1.16,1.41 -0.93,-0.4 0.44,-3.53 0.41,-7.83 -0.06,-10.3 -1.3,-22.14 -2.2,-20.98 -3.39,-41.99 -0.61,-10.99 0.7,-23.78 c 1.65,-15.98 6.95,-30.6 19.9,-39.63 a 0.42,0.41 73.8 0 1 0.65,0.36 q -0.32,8.22 1.06,16.55 c 2.81,16.86 6.09,33.63 6.27,50.45 z"},{"m":"triceps","d":"m 1281,491.32 c -15.93,-19.27 -23.98,-42.53 -23.89,-67.9 q 0.04,-11.8 1.5,-21.9 2.13,-14.76 4.78,-30.03 1.39,-8.03 1.05,-16.2 a 0.44,0.44 0 0 1 0.7,-0.38 c 15.59,11.17 19.78,29.71 20.57,48.58 q 0.43,10.46 -0.35,21.17 -1.37,18.51 -3.48,40.31 -1.27,13.12 -0.53,26.21 0.03,0.6 -0.35,0.14 z"},{"m":"triceps","d":"m 1290.47,379.83 c 11.24,17.52 17.33,36.08 18.47,57.41 0.75,14.09 0.07,27.95 0.05,41.97 a 0.28,0.28 0 0 1 -0.41,0.24 l -0.26,-0.14 q -0.65,-0.34 -0.76,-1.06 -7.8,-49.99 -17.59,-98.21 -0.29,-1.43 0.5,-0.21 z"},{"m":"triceps","d":"m 1291.39,418.15 q 0.03,-0.24 0.18,-0.46 0.48,-0.71 0.7,0.12 6.89,26.33 5.89,50.76 c -0.16,3.99 -0.69,8.84 -2.91,12.24 a 1.69,1.68 -51.8 0 1 -2.56,0.31 c -2.76,-2.6 -3.77,-9.43 -4.13,-13.42 -1.44,-16.19 0.36,-32.39 2.83,-49.55 z"},{"m":"forearms","d":"m 930.6,575.85 q 12.51,-34.52 28.14,-65.44 0.43,-0.86 0.47,0.1 c 0.49,12.97 -1.54,25.43 -3.91,38.74 -2.51,14.09 -8.58,26.84 -13.42,40.42 q -1.72,4.85 -6.72,12.65 -6.65,10.39 -13.96,19.24 -0.97,1.19 -0.94,-0.34 0.15,-6.74 1.54,-13.78 3.2,-16.15 8.8,-31.59 z"},{"m":"forearms","d":"m 941.59,538.06 c -10.38,28.73 -19.89,57.37 -27.03,86.93 q -2.23,9.27 -3.43,18.85 -0.08,0.65 -0.71,0.78 -2.96,0.63 -5.97,-0.23 c -6.64,-1.91 -6.12,-8.24 -4.18,-13.33 q 16.12,-42.22 33.46,-84.93 5.37,-13.2 12.03,-26.13 2.79,-5.42 6.2,-10.06 1.45,-1.95 0.5,0.29 -5.71,13.57 -10.87,27.83 z"},{"m":"forearms","d":"m 975.21,499.01 c 1.84,-4.34 5.3,-10.94 10.55,-11.69 3.15,-0.45 8.93,-1.45 11.43,0.58 q 3.23,2.63 1.33,6.34 -3.58,7.04 -8.39,12.41 -7.93,8.86 -17.96,19.2 a 0.33,0.32 -34.8 0 1 -0.53,-0.09 q -1.15,-2.76 -1.06,-6.04 0.31,-10.51 4.63,-20.71 z"},{"m":"forearms","d":"m 999.09,525.32 q -17.79,35.36 -41.48,67.79 c -7.52,10.28 -15.96,20.81 -26,28.74 a 0.38,0.38 0 0 1 -0.53,-0.06 q -0.12,-0.14 -0.06,-0.3 11.88,-29.53 23.3,-54.73 6.13,-13.51 13.08,-26.59 3.39,-6.37 7.64,-12.19 c 1.13,-1.54 2.35,-1.94 4.17,-1.93 q 4.02,0.02 8.07,0.22 c 3.63,0.18 9.88,-4.42 13.11,-6.8 0.2,-0.14 0.64,-0.23 0.88,-0.2 a 0.39,0.39 0 0 1 0.32,0.53 q -1.15,2.82 -2.5,5.52 z"},{"m":"forearms","d":"m 1287.36,495.1 q -0.66,-1.22 -1,-2.61 c -0.91,-3.66 3.19,-5.9 6.21,-5.78 q 4.25,0.16 7.87,0.8 c 8.25,1.44 13.07,19.85 14.04,26.73 q 0.78,5.48 -0.26,11.26 a 0.58,0.57 -61.9 0 1 -0.98,0.3 q -8.13,-8.44 -16.02,-16.95 -5.71,-6.16 -9.86,-13.75 z"},{"m":"forearms","d":"m 1374.91,644.61 a 0.63,0.62 -88.1 0 1 -0.49,-0.53 q -1.21,-9.55 -3.48,-19.07 c -6.9,-28.95 -17.02,-59.28 -27.49,-88.05 q -4.81,-13.23 -10.53,-26.85 -0.87,-2.08 0.48,-0.27 2.91,3.89 5.26,8.23 6.11,11.31 10.9,22.92 19.34,46.92 36.37,92.34 1.24,3.29 0.04,6.8 c -1.45,4.24 -7.09,5.35 -11.06,4.48 z"},{"m":"forearms","d":"m 1326.82,510.35 c 12.89,26.28 24.62,53.47 32.98,80.83 q 4.59,15.02 5.5,30.08 0.09,1.54 -0.91,0.36 -9.8,-11.56 -17.28,-24.72 -2.96,-5.2 -5.36,-12.12 -2.76,-7.98 -6.01,-15.96 c -4.29,-10.55 -5.9,-21.36 -7.75,-32.59 q -2.18,-13.15 -1.71,-25.77 0.04,-1.12 0.54,-0.11 z"},{"m":"forearms","d":"m 1354.08,620.73 q 0.96,2.42 -1,0.71 -9.65,-8.41 -17.52,-18.38 -24.99,-31.7 -45.4,-70.39 -3.25,-6.15 -6.17,-12.68 -0.09,-0.2 -0.03,-0.4 a 0.51,0.51 0 0 1 0.78,-0.29 c 4.23,2.88 9.9,7.54 15.41,6.95 q 3.07,-0.34 6.17,-0.24 c 1.9,0.05 3.28,0.57 4.42,2.2 7.5,10.74 13.36,23.21 18.79,35.06 q 12.91,28.18 24.55,57.46 z"},{"m":"glutes","d":"m 1031.34,630.56 q 8.17,-17.96 22.15,-29.52 0.57,-0.47 1.35,-0.78 c 0.63,-0.25 1.17,-0.54 1.88,-0.51 14.27,0.79 25.72,4.26 35.26,15.37 q 6.45,7.51 12.43,13.9 a 0.36,0.36 0 0 1 -0.21,0.6 c -34.24,5.14 -63.6,20.7 -83.41,49.12 q -0.77,1.1 -0.69,-0.24 1.54,-26.6 11.24,-47.94 z"},{"m":"glutes","d":"m 1092.12,758.09 q -11.32,3.91 -20.99,7.71 c -12.16,4.78 -21.24,13.16 -25.53,25.74 a 0.48,0.48 0 0 1 -0.84,0.14 c -13.74,-17.9 -25.37,-38.01 -28.48,-60.45 -4.82,-34.78 16.06,-62.1 45.13,-78.08 q 32.39,-17.79 71.37,-20.24 0.77,-0.05 0.81,0.72 0.34,5.98 0.51,12.21 0.73,27.38 0.54,74.62 0,0.87 -0.42,1.68 -13.36,26.01 -42.1,35.95 z"},{"m":"glutes","d":"m 1186.16,755.14 q -22.34,-9.6 -34.37,-32.01 -0.93,-1.73 -0.95,-3.5 -0.44,-43.95 0.95,-86.05 a 0.58,0.58 0 0 1 0.61,-0.56 q 41.96,2.14 76.79,23.16 c 13.08,7.9 25.57,19.74 32.87,33.53 18.71,35.37 0.74,73.49 -21.32,101.95 a 0.51,0.51 0 0 1 -0.88,-0.15 c -5.18,-15.06 -16.82,-23.21 -31.43,-28.03 q -12.38,-4.09 -22.27,-8.34 z"},{"m":"glutes","d":"m 1264.84,679.01 c -19.47,-28.27 -49.51,-44.62 -83.31,-49.28 a 0.48,0.48 0 0 1 -0.29,-0.79 q 6.39,-7.08 11.97,-13.48 c 9.56,-10.98 20.54,-14.92 34.76,-15.73 q 2.05,-0.11 3.56,1.07 c 22.85,17.9 32.44,49.93 33.85,78.03 q 0.06,1.04 -0.54,0.18 z"},{"m":"hamstrings","d":"m 1016.46,760.39 c 9.44,12.24 16.13,25.63 21.41,40.04 q 0.82,2.23 0.84,4.98 0.07,11.27 -2.12,20.02 -2.75,11.03 -6.86,22.65 -0.52,1.46 -0.85,-0.05 -5.74,-25.82 -9.75,-52.59 -2.71,-18.1 -3.52,-34.73 -0.08,-1.52 0.85,-0.32 z"},{"m":"hamstrings","d":"m 1070.52,797.23 c -11.68,27.55 -15.75,60.62 -14.27,91.04 q 0.39,8.01 2.12,18.82 4.13,25.86 6.66,45.29 c 2.03,15.61 1.65,31.91 -2.1,47.3 q -0.46,1.88 -0.63,-0.05 -1.63,-18.54 -5.82,-37.03 c -4.65,-20.57 -11.75,-43.58 -16.62,-63.37 -3.73,-15.15 -6.46,-30.31 -6.22,-45.8 q 0.49,-31.41 19.87,-55.13 c 2.88,-3.51 6.39,-5.98 9.88,-8.87 q 4.53,-3.74 10.3,-2.59 a 1.34,1.33 19.9 0 1 0.91,1.94 q -2.27,4.18 -4.08,8.45 z"},{"m":"hamstrings","d":"m 1098.11,859.08 c 1.35,28.16 0.76,58.11 -0.79,88.31 q -0.29,5.81 -1.41,12.09 -3.22,18.13 0.59,38.78 0.25,1.36 -0.6,0.27 c -20.01,-25.52 -30.66,-57.27 -32.98,-89.64 -3.21,-44.66 6.94,-89.32 20.75,-132.24 q 0.55,-1.71 1.12,0 4.19,12.73 6.41,24.46 5.53,29.26 6.91,57.97 z"},{"m":"hamstrings","d":"m 1219.83,958.26 c 1.6,-16.19 4.52,-34.12 7.27,-51.04 q 1.66,-10.21 2.07,-18.85 1.54,-32.19 -5.11,-62.59 -4.38,-20.02 -13.16,-37.05 -0.74,-1.43 0.83,-1.82 c 6.74,-1.68 10.82,2.92 15.54,6.72 q 2.63,2.11 4.95,4.97 22.46,27.73 19.33,65.93 c -1.28,15.6 -4.64,30.53 -8.67,45.57 q -4.34,16.19 -8.51,30.94 -8.04,28.43 -11.39,59.02 -0.17,1.55 -0.51,0.03 -4.7,-20.89 -2.64,-41.83 z"},{"m":"hamstrings","d":"m 1222.33,911.18 c -2.52,31.89 -13.09,61.94 -32.73,87.27 q -0.72,0.94 -0.52,-0.23 2.27,-12.72 1.81,-27.27 -0.2,-6.27 -1.31,-11.44 -1.06,-4.94 -1.37,-9.68 -2.26,-34.73 -1.33,-78.46 0.82,-38.23 7.92,-73.18 2.24,-11.02 6.07,-21.84 0.39,-1.11 0.77,0.01 c 14.36,43.28 24.25,89.74 20.69,134.82 z"},{"m":"hamstrings","d":"m 1252.18,789.16 c 4.38,-10.33 10.1,-20.26 17.11,-29.06 q 0.66,-0.83 0.64,0.22 -0.05,2.47 -0.26,5.11 c -2.25,28.2 -7.17,55.27 -13.07,82.76 q -0.28,1.33 -0.76,0.06 -5.28,-13.81 -7.95,-27.73 c -1.27,-6.66 -2.21,-15.94 0.54,-22.48 q 1.91,-4.56 3.75,-8.88 z"},{"m":"calves","d":"m 1058.29,1085.75 c 6.08,-36.26 10.83,-64.98 13.27,-97.84 a 0.64,0.63 12.9 0 1 0.87,-0.54 c 4.34,1.73 9.74,5.17 11.34,9.62 q 0.48,1.32 0.33,3.57 c -2.18,33.02 -2.33,66.33 -2.21,98.92 q 0.04,10.61 0.89,19.22 2.51,25.4 2.71,52.08 c 0.09,12.28 -0.31,25.64 -3.38,37.51 q -0.44,1.7 -1.2,3.04 a 1.46,1.45 41.5 0 1 -2.43,0.15 c -11.56,-15.48 -22.57,-30.58 -27.04,-49.16 -1.94,-8.07 -1.22,-16.06 -0.32,-24.08 q 3.25,-29.05 7.17,-52.49 z"},{"m":"calves","d":"m 1065.65,1205.72 q 0.18,-0.49 0.65,-0.28 0.57,0.24 0.99,0.74 c 7.66,9.08 12.41,22.27 15.49,34.01 q 4.92,18.73 7.73,38.56 5.06,35.68 6.15,72.03 0.14,4.75 0.25,9.3 0.09,3.29 -3.11,2.5 -0.29,-0.07 -0.59,-0.13 -3.27,-0.66 -3.31,-4 c -0.46,-33.25 -5.05,-66.1 -13.91,-98.69 q -4.29,-15.78 -9.2,-30.12 c -2.89,-8.45 -4,-15.96 -1.14,-23.92 z"},{"m":"calves","d":"m 1092.9,1005.49 c 6.21,14.55 10.89,30.05 15.1,45.29 q 10.42,37.71 15.89,75.17 c 2.5,17.09 4.13,34.52 2.57,51.57 -0.69,7.55 -2.44,16.36 -7.25,22.3 a 0.72,0.71 31.3 0 1 -0.84,0.2 q -15.39,-6.63 -22.38,-22.48 -0.37,-0.84 -0.47,-1.77 -7.84,-73.34 -8.63,-146.47 -0.15,-13.23 1,-26.05 0.18,-2.03 0.73,-4.04 a 0.43,0.43 0 0 1 0.78,-0.13 q 2.25,3.5 3.5,6.41 z"},{"m":"calves","d":"m 1105.01,1296.15 c 0.31,-3.79 0.81,-7.66 0.78,-11.4 -0.13,-20.92 -0.61,-43.07 2,-63.38 0.83,-6.47 2.37,-14.24 6.84,-19.26 a 0.48,0.48 0 0 1 0.83,0.26 q 2,15.5 0.91,32.56 -1.2,18.89 -3.49,38.19 -1.15,9.79 -1.38,19.57 -0.5,21.82 -2.41,44.66 -0.8,9.48 -2.65,18.21 a 0.62,0.61 -66.5 0 1 -0.96,0.38 q -3.82,-2.71 -4.14,-6.62 -0.59,-7.27 0.39,-17.04 1.85,-18.46 3.28,-36.13 z"},{"m":"calves","d":"m 1195.51,999.93 q 1.11,-2 1.54,0.25 0.83,4.43 1.06,9.07 0.79,16.08 0.37,32.68 -1.79,71.08 -8.41,133.14 c -0.54,5.01 -5.87,11.84 -9.39,15.76 -3.68,4.08 -8.74,6.74 -13.52,9.33 q -0.36,0.2 -0.63,-0.11 c -4.85,-5.52 -6.81,-14.78 -7.44,-21.9 -1.74,-19.58 0.51,-41.13 3.73,-60.68 q 6.55,-39.63 18.22,-79.21 3.88,-13.17 8.74,-26 2.41,-6.37 5.73,-12.33 z"},{"m":"calves","d":"m 1170.98,1202.22 q 2.43,2.66 3.69,6.26 c 3.13,8.93 4.01,18.89 4.48,28.32 q 1.05,21.18 0.46,40.21 -0.29,9.19 0.45,16.73 1.84,18.75 3.59,37.51 0.69,7.41 0.77,15.52 0.06,5.8 -4.1,9 a 0.89,0.89 0 0 1 -1.42,-0.54 q -2.23,-11.91 -3.17,-25.71 -1.35,-19.53 -1.75,-36.62 -0.22,-9.41 -1.32,-18.68 -2.7,-22.94 -3.76,-42.69 -0.78,-14.6 1.04,-28.98 a 0.6,0.6 0 0 1 1.04,-0.33 z"},{"m":"calves","d":"m 1209.54,1220.37 q 3.46,-8.19 9.41,-14.95 a 0.5,0.49 56.8 0 1 0.84,0.18 q 1.93,5.91 1.63,12.13 -0.21,4.19 -2.73,11.81 -8.63,26.04 -13.9,49.21 -8.52,37.45 -9.27,80.13 -0.04,2.72 -2.68,3.4 -0.94,0.24 -1.91,0.38 -2.41,0.33 -2.37,-2.1 0.65,-35.68 4.84,-70.31 c 2.84,-23.48 7.15,-48.63 16.14,-69.88 z"},{"m":"calves","d":"m 1234.23,1161.52 c -4.22,19.14 -15.78,34.23 -26.98,49.71 a 1.72,1.72 0 0 1 -3.01,-0.42 q -1.93,-5.29 -2.63,-11.11 -1.51,-12.55 -1.6,-22.65 -0.27,-28.3 2.59,-57.1 0.96,-9.64 1.02,-20.11 0.27,-52.48 -2.22,-101.41 c -0.04,-0.76 0.35,-1.89 0.76,-2.54 q 3.68,-5.85 11.13,-8.53 0.53,-0.19 0.58,0.37 2.93,37.23 10.25,79.5 c 4.59,26.56 9.04,54.75 11.1,81.03 q 0.51,6.47 -0.99,13.26 z"}],"inert":{"front":["m 290.15,290.93 c 3.28,4.65 5.62,9.8 7.15,15.32 0.7,2.53 -1.37,5.04 -3.97,5.05 q -6.56,0.04 -11.88,-2.03 a 2.07,2.06 14.4 0 1 -1.3,-2.18 c 0.77,-5.99 4.87,-11.91 8.89,-16.23 a 0.72,0.71 -41.3 0 1 1.11,0.07 z","m 301.06,294.13 c -3.51,-6.07 -6.56,-12.38 -9.23,-18.87 q -1.42,-3.46 -1.56,-6.85 -0.36,-8.56 0.86,-17.74 a 0.44,0.44 0 0 1 0.53,-0.37 q 0.46,0.1 0.79,0.48 4.65,5.35 7.86,11.31 c 6.42,11.93 11.65,25.19 18.44,37.61 a 5.28,5.26 30.8 0 1 0.66,2.59 L 319.3,321 a 0.79,0.79 0 0 1 -0.79,0.79 c -1.83,0 -3.44,-1.1 -4.21,-2.73 q -5.68,-11.89 -13.24,-24.93 z","m 328.11,315.98 c -1.39,2.8 -1.85,5.31 -5.45,5.84 a 0.92,0.91 85.6 0 1 -1.04,-0.9 q -0.09,-9.72 -0.08,-18.23 0,-1.57 0.75,-3.05 6.71,-13.16 12.25,-25.58 c 3.49,-7.85 8.12,-17.17 14.42,-23.56 q 0.19,-0.18 0.45,-0.19 a 0.43,0.42 -3.6 0 1 0.44,0.38 q 0.95,7.84 0.93,15.61 -0.02,4.3 -1.34,7.91 -3.03,8.27 -7.56,16.2 -8.05,14.11 -13.77,25.57 z","m 343.58,306.54 c 1.42,-5.58 3.9,-10.86 7.19,-15.57 a 0.69,0.68 -50.1 0 1 1.04,-0.1 c 3.9,3.84 9.01,11.14 8.89,17.13 a 1.07,1.06 79.1 0 1 -0.65,0.97 q -5.44,2.34 -12.5,2.38 c -2.39,0.01 -4.57,-2.43 -3.97,-4.81 z","m 332.05,262.18 c -0.78,8.99 -5.96,18.06 -11.27,26.44 a 0.35,0.35 0 0 1 -0.59,0.01 q -6.05,-9.29 -9.58,-18.59 -1.68,-4.42 -1.62,-8.04 0.06,-3.36 4.03,-3.55 6.44,-0.31 14.09,-0.13 c 2.41,0.05 5.19,0.99 4.94,3.86 z","m 274.27,0 h 7.5 q 18.53,1.85 32.68,8.03 c 12.49,5.46 24.03,14.94 27.55,28.42 a 2.51,2.49 89.9 0 0 1.78,1.79 c 62.05,16.11 59.99,92.38 46.77,140.06 q -2.56,9.24 -6.49,17.96 c -2.53,5.64 -4.61,7.93 -10.53,9.62 l -0.26,-0.1 q -0.2,-0.09 -0.11,-0.29 3.73,-8.25 6.16,-16.1 a 1.18,1.17 -83.3 0 1 1.03,-0.82 c 9.73,-0.81 11.84,-26.25 11.81,-33.17 q 0,-2.26 -1.89,-3.46 -0.54,-0.35 -1.17,-0.24 -2.13,0.37 -4.08,0.23 c -5.7,-0.39 -8.68,-13.45 -9.55,-17.9 q -2.57,-13.24 -5.09,-28.6 -0.65,-3.97 -2.37,-7.72 a 2.7,2.65 86.8 0 0 -1.54,-1.4 C 357.04,93.05 347.03,89.65 337,88.5 q -7.76,-0.88 -14.28,3.52 c -7.75,5.24 -15.65,10.37 -23.95,14.33 q -15.27,7.3 -31.5,12.13 a 1.86,1.85 -0.3 0 0 -1.25,1.26 c -2.08,7.3 -3.02,14.59 -4.33,22.04 -0.77,4.35 -2.6,10.1 -8.02,10.27 q -1.4,0.05 -2.82,-0.07 -0.58,-0.04 -0.95,0.42 -1.31,1.64 -1.26,3.95 0.23,12.21 3.9,23.14 c 1.3,3.87 3.66,8.51 8.11,9.16 a 1.22,1.22 0 0 1 0.99,0.84 q 5.67,17.91 15.65,33.72 0.59,0.93 0.76,2 0.62,15.66 -0.31,31.34 -0.33,0.59 -0.86,1.05 -10.81,9.38 -25.26,12.16 -3.21,-0.66 -6.4,-1 -2.96,-0.31 -6.35,-1.14 -4.1,-0.99 -8.5,-0.85 -19.12,0.63 -38.2,1.5 -7.58,0.35 -14.57,-0.86 a 1.74,1.73 -87.9 0 1 -1.44,-1.56 c -0.81,-8.45 2.45,-17.97 5.45,-25.54 7.91,-19.94 16.52,-39.52 21.72,-59.8 q 3.8,-14.84 3.36,-29.24 -0.62,-20.32 -5.16,-56.52 -0.99,-7.83 -0.58,-13.57 1.97,-27.81 13.7,-48.73 C 220.38,22.24 229.09,15.56 238.9,9.81 Q 254.97,0.39 274.27,0 Z m 61.27,35.35 c -9.93,-7.61 -23.32,-7.23 -32.78,1.35 q -1.11,1.01 0.36,0.73 c 10.93,-2.09 20.77,-2.59 31.88,-0.82 q 2.75,0.43 0.54,-1.26 z","m 302.76,36.7 c 9.46,-8.58 22.85,-8.96 32.78,-1.35 q 2.21,1.69 -0.54,1.26 c -11.11,-1.77 -20.95,-1.27 -31.88,0.82 q -1.47,0.28 -0.36,-0.73 z","m 333.98559,84.905706 c 18.21278,1.634629 31.69605,8.136487 37.2945,12.907787 5.05164,4.305277 7.19576,45.031667 12.65256,51.693107 1.8636,2.27501 8.59028,-1.54865 8.92968,2.02054 0.963,10.12704 -1.13955,23.87461 -5.01774,34.09876 -2.76635,7.29296 -5.63968,2.08994 -7.32662,6.61326 -4.20041,11.26291 -13.37526,37.43537 -39.7435,47.10729 -18.23028,5.87857 -27.73171,4.95957 -42.38166,-0.61061 -10.53425,-7.41801 -26.32238,-15.61151 -35.48901,-45.82179 -2.15595,-7.10532 -5.07953,-2.63217 -8.24698,-7.1806 -8.2816,-11.89228 -6.24233,-24.37427 -6.11257,-33.09312 1.24002,-3.08768 8.39807,0.42491 9.86574,-4.98496 1.75809,-6.48033 5.74751,-21.52642 6.67934,-30.11758 42.80886,-10.2119 49.53647,-34.194904 68.89626,-32.632084 z","m 76.9,699.46 c -1.68,6.93 -4.86,14.23 -9.75,19.73 -3.68,4.16 -10.21,-0.6 -12.84,-3.77 q -4.85,-5.86 -10.18,-10.8 c -2.03,-1.88 -9.58,-5.18 -8.37,-9.02 2,-6.4 4.37,-11.1 8.21,-16.74 q 5.74,-8.42 12.12,-17.03 2.99,-4.03 5.79,-6.44 1.65,-1.41 4.25,-0.76 7.51,1.87 13.62,4.2 a 0.47,0.47 0 0 1 0.29,0.4 q 1.61,20.71 -3.14,40.23 z","m 22.64,686.61 q -4.35,2.68 -8.97,5.43 -2,1.2 -3.76,0.73 -2.04,-0.55 -3.53,-1.72 a 1.59,1.59 0 0 1 -0.52,-1.77 q 0.73,-2.04 2.74,-3 5.14,-2.46 10.34,-5.1 a 13.93,13.75 -80.7 0 0 3.47,-2.53 c 3.04,-3.03 6.28,-5.7 9.81,-8.15 q 6.86,-4.77 13.58,-9.74 c 1.68,-1.24 2.71,-1.36 4.54,-0.38 4.66,2.47 -4.59,11.2 -6.57,12.67 -6.61,4.92 -13.95,9.14 -21.13,13.56 z","m 32.82,715.26 q -5.43,14.93 -12.06,27.84 -1.98,3.85 -4.71,6.75 a 1.67,1.66 51.8 0 1 -2.66,-0.32 c -2.02,-3.56 1,-13.05 2.6,-16.84 q 6.53,-15.37 13.87,-30.34 1.12,-2.28 2.93,-0.51 l 2.38,2.35 q 1.73,1.69 0.66,3.87 -1.5,3.03 -3.01,7.2 z","m 32.18,751.27 q -3.61,7.78 -7.04,13.77 -0.8,1.41 -2.48,1.29 a 2.04,2.04 0 0 1 -1.82,-1.48 q -0.59,-2.11 -0.16,-4.36 1.46,-7.75 4.24,-15.75 5.55,-15.95 13.53,-31.52 a 0.92,0.91 -74.1 0 1 0.89,-0.5 c 2.55,0.22 5.63,1.06 5.19,4.36 q -0.48,3.64 -1.61,7.19 -3.97,12.44 -10.74,27 z","m 54.56,726.38 c -4.87,12.51 -10.55,24.74 -16.83,36.91 q -1.47,2.85 -4.02,0.9 -0.2,-0.16 -0.33,-0.36 -1.89,-2.86 -0.7,-5.81 c 4.9,-12.05 9.63,-24.27 15.68,-36.05 q 0.94,-1.84 2.66,-0.68 l 2.47,1.68 q 1.89,1.28 1.07,3.41 z","m 53.04,752.14 c 1.93,-10.58 3.66,-20.18 11.74,-27.88 a 0.88,0.88 0 0 1 1.43,0.33 q 0.18,0.48 0.07,0.95 -3.65,14.78 -9.97,27.42 -0.75,1.49 -1.97,2.17 a 1.06,1.06 0 0 1 -1.57,-1.02 q 0.09,-0.98 0.27,-1.97 z","m 572.57,717.95 c -4.99,-6.62 -7.75,-14.53 -9.42,-22.45 q -3.74,-17.75 -2.3,-36.07 0.04,-0.54 0.55,-0.72 6.91,-2.55 14.55,-4.31 a 3.01,2.98 -33.7 0 1 2.4,0.47 q 2.39,1.67 4.36,4.26 11.12,14.61 17.14,24.22 2.19,3.48 3.91,8.26 c 1.42,3.93 2.9,6.43 -1.05,8.84 -6.91,4.23 -11.6,9.71 -16.7,15.64 q -2.2,2.56 -5.58,4.02 -4.71,2.02 -7.86,-2.16 z","m 629.7,692.91 q -0.88,0.05 -1.63,-0.39 -16.99,-9.79 -30.39,-19.09 -5.44,-3.78 -8.34,-10.22 a 1.71,1.66 45.9 0 1 0,-1.38 c 0.82,-1.77 3.4,-2.74 5.14,-1.47 6.84,4.98 13.95,9.43 20.22,14.76 2.49,2.13 4.62,4.61 7.54,6.13 q 5.16,2.68 10.13,5.06 1.59,0.76 2.51,2.34 a 1.98,1.97 56.7 0 1 -0.53,2.58 q -2.03,1.52 -4.65,1.68 z","m 619.38,741.59 c -5.55,-10.95 -9.91,-22.4 -14.42,-34.16 q -0.76,-1.97 0.82,-3.38 1.39,-1.24 2.48,-2.21 1.76,-1.58 2.8,0.54 8.21,16.69 14.65,32.42 2.73,6.68 2.32,13.6 -0.1,1.72 -1.83,1.98 a 1.61,1.58 -24.2 0 1 -1.45,-0.54 q -2.91,-3.38 -5.37,-8.25 z","m 615.42,764.65 c -7.37,-14.18 -14.47,-29.6 -18.64,-45 q -0.59,-2.21 -0.29,-4.52 0.07,-0.54 0.54,-0.83 1.9,-1.18 4.13,-1.51 1,-0.15 1.46,0.75 7.03,13.62 12.86,29.78 3.2,8.87 4.64,16.66 0.36,1.96 0.12,4.04 c -0.34,2.92 -3.74,2.72 -4.82,0.63 z","M 603.2,763.32 C 597.32,751.95 590.92,738.6 586.42,726.4 q -0.8,-2.15 1.08,-3.48 l 2.22,-1.57 q 1.83,-1.29 2.86,0.69 c 6.13,11.73 10.79,24.28 15.74,36.17 q 1.16,2.78 -0.76,5.72 -0.12,0.18 -0.3,0.32 -2.58,1.93 -4.06,-0.93 z","m 585.43,754.04 c -5.15,-8.61 -8.65,-19.26 -10.81,-28.78 q -0.46,-2.03 1.32,-0.96 1.2,0.73 2.2,1.88 c 6.71,7.78 8.51,18.11 10.03,28.15 a 0.6,0.59 -12.9 0 1 -0.42,0.66 q -1.5,0.43 -2.32,-0.95 z","m 286.21,731.89 c 5.1,-12.47 11.15,-24.68 16.82,-36.79 a 1.17,1.17 0 0 1 1.84,-0.38 q 0.8,0.71 0.75,1.93 c -0.46,12.38 -1.19,24.44 -2.76,37.71 -1.13,9.56 -1.91,19.14 -3.49,28.56 -0.69,4.17 -2.41,8.04 -4.32,11.69 a 2.22,2.21 29.3 0 1 -3.12,0.87 q -1.32,-0.8 -1.28,-2.41 0.2,-8.82 0.13,-17.67 -0.01,-1.4 -0.62,-2.66 c -2.16,-4.44 -4.98,-9.87 -5.17,-14.91 q -0.11,-2.7 1.22,-5.94 z","m 292.87,682.52 c -3.12,14.57 -9.61,27.39 -18.76,39.63 q -0.68,0.91 -1.28,-0.05 c -2.93,-4.74 -4.21,-9.67 -4.63,-15.36 q -0.45,-5.9 -1.09,-8.14 -1.89,-6.56 -4.75,-13.01 c -2.76,-6.23 -4.28,-13.06 -7.14,-19.37 -7.35,-16.19 -16.26,-33.86 -22.42,-51.48 q -0.08,-0.21 -0.01,-0.42 a 0.26,0.26 0 0 1 0.46,-0.06 q 10.77,14.73 21.98,28.77 11.5,14.4 17.6,19.88 7.55,6.77 14.86,13.4 2.74,2.48 4.97,5.3 a 1.11,1.09 32.6 0 1 0.21,0.91 z","m 300.1,836.83 q 1.64,19.12 2.22,36.73 0.11,3.47 -0.89,0.14 -2.42,-8.03 -4.58,-16.34 c -4.22,-16.26 -9.63,-31.97 -14.75,-48.34 q -2.58,-8.25 -6.48,-18.58 -14.03,-37.14 -28.05,-75.59 -3.75,-10.3 -6.6,-19.52 -5.15,-16.64 -9.78,-32.48 c -4.96,-16.95 -8.01,-35.46 -8.1,-52.91 q -0.02,-2.71 0.71,-0.1 9.47,34.09 24.55,65.57 c 6.56,13.69 13.54,27.25 19.02,40.89 q 15.49,38.57 24.83,67.42 2.41,7.47 3.31,17.16 c 1.12,12.19 3.56,24.02 4.59,35.95 z","m 338.67,872.66 c 0.73,-20.39 1.92,-38.3 4.91,-57.16 1.83,-11.53 2.14,-23.33 6.03,-34.79 q 10.93,-32.22 23.86,-64.19 c 5.7,-14.09 12.7,-27.77 19.13,-41.2 q 15.32,-32.04 24.53,-65.61 0.65,-2.39 0.64,0.08 c -0.12,28.68 -7.98,55.21 -17.34,83.63 -1.78,5.41 -3.2,10.88 -5.19,16.32 -10.25,27.99 -20.2,55.76 -30.81,83.12 q -2.8,7.2 -4.91,13.95 -5.59,17.81 -11.19,35.6 c -3.34,10.63 -5.96,21.88 -9.13,32.74 q -0.27,0.92 -0.4,-0.03 -0.18,-1.22 -0.13,-2.46 z","m 394.37,647.3 c -4.46,9.4 -9.38,19.33 -12.44,29.17 q -1.3,4.17 -2.83,7.95 c -2.26,5.61 -4.97,10.94 -5.74,16.89 -0.99,7.53 -1.06,14.75 -5.53,21.2 a 0.5,0.5 0 0 1 -0.83,0.01 q -4.34,-6.22 -7.72,-11.72 -7.96,-12.97 -11.16,-28.21 a 1.61,1.61 0 0 1 0.35,-1.37 q 2.44,-2.82 5.14,-5.21 c 9.88,-8.72 18.7,-16.83 27.48,-27.39 q 13.43,-16.14 25.97,-33.56 0.32,-0.44 0.43,-0.49 0.87,-0.39 0.54,0.51 -5.81,15.68 -13.66,32.22 z","m 352.83,748.3 c -2.02,3.67 -2.95,6.58 -2.78,11.03 q 0.26,6.53 0.21,13.72 c -0.01,1.58 -1.13,2.86 -2.75,2.9 q -0.58,0.01 -0.9,-0.48 C 342.49,769 341.27,763.08 340.41,755 q -1.04,-9.79 -2.26,-19.97 c -1.6,-13.31 -2.43,-25.65 -2.79,-38.57 q -0.03,-1.03 0.53,-1.68 a 1.21,1.2 52.6 0 1 2.01,0.27 q 6.87,14.67 13.39,28.8 2.2,4.77 3.93,9.5 1.07,2.92 0.68,5.46 -0.83,5.42 -3.07,9.49 z","m 258.39,1012.52 q -5.64,-13.48 -7.66,-29.52 -0.05,-0.4 0.28,-0.64 10.7,-7.7 22.82,-4.11 c 5.29,1.57 4.59,5.57 4.3,9.79 -0.9,12.75 -3.27,25.99 -10.59,36.26 a 1.05,1.05 0 0 1 -1.64,0.09 q -4.73,-5.21 -7.51,-11.87 z","m 258.71,1030.63 c 3.61,6.97 7.98,15.68 6.95,23.65 a 0.48,0.48 0 0 1 -0.72,0.35 l -18.34,-11.02 q -0.38,-0.23 -0.55,-0.64 -1,-2.44 -1.32,-4.76 -3.61,-26.35 -4.29,-51.87 -0.06,-2.2 0.39,-0.04 c 3.24,15.52 10.35,29.77 17.88,44.33 z","m 288.4,985.22 q 1.73,22.58 1.26,41.7 -0.04,1.47 -0.76,3.07 -4.6,10.16 -10.25,24.15 c -2.27,5.6 -3.1,11.54 -5.89,17 q -0.2,0.39 -0.5,0.07 -0.38,-0.42 -0.45,-1 c -1.47,-12.75 1.68,-26.74 4.89,-38.94 4.36,-16.65 9.53,-33.85 10.59,-51.47 q 0.01,-0.22 0.19,-0.33 0.05,-0.02 0.1,-0.02 0.14,0.02 0.16,0.16 0.44,2.76 0.66,5.61 z","m 367.17,978.19 c 7.47,-2.33 16.25,-0.86 22.63,4.13 q 0.45,0.35 0.38,0.92 -1.87,15.08 -7.33,28.55 -2.94,7.24 -7.89,12.74 a 0.87,0.87 0 0 1 -1.34,-0.05 c -8.34,-10.84 -10.82,-27.73 -10.91,-41.25 -0.01,-2.65 2.18,-4.32 4.46,-5.04 z","m 369.06,1070.47 q -0.22,1.65 -1.01,0.18 c -2.79,-5.17 -3.42,-10.89 -5.57,-16.11 q -5.2,-12.65 -10.53,-24.91 c -0.38,-0.88 -0.8,-1.93 -0.81,-2.92 q -0.26,-23.13 1.85,-46.89 a 0.27,0.26 -77 0 1 0.36,-0.23 l 0.25,0.11 q 0.13,0.05 0.14,0.19 0.36,9.87 2.31,18.26 4.17,17.95 9.24,37.46 c 3.02,11.63 5.3,23.46 3.77,34.86 z","m 378.49,1038.47 q 5.25,-11 11.25,-23 6.8,-13.59 10.56,-29.93 0.03,-0.15 0.18,-0.19 0.05,-0.01 0.1,0 0.1,0.01 0.09,0.11 -0.84,19 -2.62,37.31 -1.13,11.63 -2.64,19.1 -0.25,1.26 -1.49,1.99 -10.01,5.93 -17.84,10.76 a 0.53,0.53 0 0 1 -0.8,-0.37 c -0.79,-5.16 1.07,-11.29 3.21,-15.78 z","m 260.21,1135.29 c 4.48,29.68 7.59,58.82 9.36,87.72 0.53,8.66 1.47,17.39 2.79,25.97 q 1.84,11.96 7.34,33.23 0.5,1.94 1.04,7.01 0.48,4.54 -2.34,7.74 a 0.6,0.6 0 0 1 -0.99,-0.15 q -1.61,-3.47 -2.33,-7.07 -5.47,-27.17 -8.25,-45.03 -4.18,-26.84 -7.6,-47.71 -0.97,-5.95 -2.27,-12 -7.08,-33.02 -14.69,-65.86 -1.12,-4.84 -2.44,-9.69 -1.98,-7.22 -2.28,-9.76 -2.41,-20.6 1.44,-43.08 0.25,-1.46 0.73,-0.06 7.12,20.57 13.32,44.54 4.83,18.73 7.17,34.2 z","m 367.02,1257.45 q 3.27,-15.94 4.47,-35.75 2.4,-39.61 7.94,-77.98 2.44,-16.87 5.47,-30.17 6.3,-27.69 16.13,-56.67 0.69,-2.03 1.04,0.08 2.43,14.55 2.2,31.01 -0.15,10.91 -2.78,20.08 -1.29,4.53 -2.34,9.1 -7.34,31.94 -13.88,61.81 -2.12,9.66 -3.56,18.43 c -4.93,29.99 -9.41,60.77 -15.76,91.85 q -0.74,3.61 -2.23,7.21 a 0.86,0.86 0 0 1 -1.48,0.19 q -2.35,-3.11 -2.05,-7.14 0.36,-4.83 1.56,-9.43 3.01,-11.58 5.27,-22.62 z","m 279.42,1326.07 q -2.66,-6.02 -11.2,-17.08 -1.56,-2.02 -1.72,-4 c -0.22,-2.73 1.78,-5.75 4.12,-7.13 q 0.49,-0.29 0.97,0.02 8.39,5.34 14.89,11.86 1.95,1.97 2.11,4.51 0.86,13.41 -1.46,27.53 a 0.31,0.31 0 0 1 -0.45,0.22 q -0.14,-0.08 -0.27,-0.19 -0.12,-0.1 -0.18,-0.24 -3.39,-7.76 -6.81,-15.5 z","m 284.58,1349.02 q 5.05,11.98 7,21.38 3.39,16.32 -0.69,32.76 a 0.43,0.43 0 0 1 -0.76,0.16 c -10.12,-13.11 -21.17,-25.72 -34.11,-35.39 a 1.63,1.62 16.1 0 1 -0.65,-1.17 q -0.75,-8.89 2.46,-14.38 0.98,-1.67 1.18,-3.55 1.38,-12.95 2.87,-26.6 a 0.7,0.7 0 0 1 0.91,-0.59 c 11.21,3.6 17.28,16.67 21.79,27.38 z","m 353.36,1339.65 q -1.77,-13.46 -1.06,-24.75 0.06,-0.96 0.22,-1.92 c 0.7,-4.2 12.74,-12.42 16.33,-14.86 q 1.07,-0.72 2.08,0.08 c 3.24,2.56 4.92,6.49 2.19,10.15 -5.11,6.84 -9.99,13.59 -13.3,21.5 q -2.27,5.41 -5.06,11.31 -0.76,1.62 -1.15,-0.12 -0.15,-0.68 -0.25,-1.39 z","m 379.01,1322.5 c 1.17,9.01 1.97,18.19 3.1,26.72 0.13,0.98 0.26,1.92 0.78,2.79 q 3.21,5.33 2.84,13.44 -0.1,2.05 -1.78,3.29 c -12.07,8.93 -22.71,21.31 -32.45,33.67 q -0.48,0.61 -1.15,0.95 a 0.24,0.24 0 0 1 -0.34,-0.16 q -5.23,-21.38 1.99,-42.64 2.82,-8.31 6.7,-16.77 c 4.2,-9.17 9.45,-18.49 19,-22.07 a 0.98,0.97 -14.1 0 1 1.31,0.78 z","m 268.26,1427.94 c -13.55,0.97 -28,-0.1 -40.94,-5.21 a 0.76,0.76 0 0 1 -0.35,-1.14 c 8.83,-12.76 18.97,-25.24 27.65,-37.39 q 0.72,-1 1.89,-0.61 11.12,3.76 18.81,14.19 c 1.66,2.25 0.2,9.63 -0.4,12.23 q -2.17,9.25 -5.67,17.23 a 1.19,1.17 -79.8 0 1 -0.99,0.7 z","m 279.15,1427.43 c -2.43,-4.43 -2.1,-22.42 2.82,-25.6 q 0.48,-0.31 0.93,0.02 c 4.5,3.21 11.09,10.03 10.33,15.88 q -0.43,3.39 -0.54,7.02 a 3.3,3.3 0 0 1 -3.32,3.21 l -9.41,-0.05 q -0.55,0 -0.81,-0.48 z","m 382.04,1384.42 q 1.51,-0.66 3.03,-0.87 0.57,-0.07 0.91,0.39 l 27.73,37.1 a 1.23,1.22 61 0 1 -0.53,1.87 c -12.37,4.87 -27.24,6.04 -40.29,5.05 a 1.5,1.49 81 0 1 -1.27,-0.93 c -3.14,-7.8 -6.11,-16.5 -6.82,-25.44 q -0.2,-2.47 0.96,-4.02 6.68,-8.97 16.28,-13.15 z","m 347.64,1416.45 c -0.34,-4.76 6.41,-11.73 10.12,-14.46 a 1.41,1.4 -37.2 0 1 1.94,0.26 c 2.44,3.09 2.86,7.16 3.19,10.93 q 0.6,6.77 -0.53,13.21 a 1.96,1.95 5.2 0 1 -1.95,1.62 l -9.39,-0.1 a 2.74,2.74 0 0 1 -2.7,-2.53 q -0.35,-4.33 -0.68,-8.93 z"],"back":["m 1096.04,0 h 7.79 c 22.49,2.45 53.18,10.99 60.21,36.27 q 0.46,1.66 2,2.05 c 64.38,16.44 60.01,99.49 44.24,147.61 q -2.62,7.99 -6.55,14.81 -0.51,0.89 -1.22,1.68 -0.97,1.07 -1.52,1.32 -3.63,1.58 -6.55,2.8 -14.77,6.1 -29.14,11 -0.74,0.26 -0.59,1.19 -0.52,-0.76 -1.47,-0.46 -4.4,1.42 -10.86,2.97 -3.97,1 -6.62,1.12 -5.28,0.24 -12.66,-0.36 -5.76,-0.52 -11.15,-2.21 -0.54,-0.17 -0.85,-0.03 -0.45,0.2 -0.58,0.77 0.26,-1.02 -0.52,-1.24 -9.76,-2.78 -19.21,-7.04 a 0.46,0.46 0 0 0 -0.65,0.43 l 0.39,12.23 q 0.49,15.83 -0.42,30.63 l -0.91,9.16 a 2.42,2.42 0 0 1 -1.99,2.15 q -11.5,2.06 -22.86,2.63 -5.84,0.05 -10.69,-1.12 -4.96,-1.19 -7,-1.31 -3.7,-0.23 -7.48,-0.12 -17.27,0.5 -33.97,1.33 -7.72,0.38 -15.15,-0.74 a 2.13,2.12 0.2 0 1 -1.78,-1.78 c -1,-6.77 1.75,-15.8 4.22,-22.15 q 5.88,-15.15 12.89,-32.55 c 7.85,-19.49 14.27,-41.02 13.35,-61.3 q -1.03,-22.68 -5.37,-57.36 -0.67,-5.33 -0.4,-10.31 0.98,-18.63 7.84,-37.04 5.87,-15.78 15.35,-24.54 c 7.92,-7.32 19.98,-14.55 31.09,-17.67 q 9.1,-2.55 18.79,-2.82 z m 40.66,79.56 c 15.08,3.29 29.45,-7.8 30.48,-23.26 q 0.15,-2.22 -0.63,-0.14 -6.61,17.62 -24.72,17.97 c -5.78,0.12 -11.81,-2.36 -16.39,-6.42 -1.53,-1.35 -3.74,-3.16 -5.84,-2.98 a 0.62,0.62 0 0 0 -0.5,0.9 q 5.86,11.37 17.6,13.93 z","m 1167.18,56.3 c -1.03,15.46 -15.4,26.55 -30.48,23.26 Q 1124.96,77 1119.1,65.63 a 0.62,0.62 0 0 1 0.5,-0.9 c 2.1,-0.18 4.31,1.63 5.84,2.98 4.58,4.06 10.61,6.54 16.39,6.42 q 18.11,-0.35 24.72,-17.97 0.78,-2.08 0.63,0.14 z","m 1133.1,222 -1.89,61.8 q -0.02,0.66 -0.62,0.4 -22.28,-9.35 -45.91,-9.91 a 0.77,0.77 0 0 1 -0.6,-1.22 q 1.38,-1.89 3.31,-2.48 c 6.78,-2.07 12.31,-3.9 18.36,-8.33 q 5.08,-3.71 7.66,-8.78 c 5.15,-10.11 6.3,-21.91 7.11,-32.95 q 0.13,-0.57 0.58,-0.77 0.31,-0.14 0.85,0.03 5.39,1.69 11.15,2.21 z","m 1164.71,218.73 c 1.42,12.25 1.86,24.62 7.81,35.59 3.46,6.38 11.4,11.41 17.38,13.62 q 4.31,1.6 8.68,2.96 1.85,0.58 2.75,2.35 a 0.65,0.65 0 0 1 -0.56,0.94 q -23.87,0.79 -45.98,10.03 a 0.34,0.34 0 0 1 -0.47,-0.3 l -1.94,-62.68 q 6.46,-1.55 10.86,-2.97 0.95,-0.3 1.47,0.46 z","m 848.29,759.36 c -5.06,1.08 -7.66,-1.55 -5.81,-6.56 6.01,-16.27 11.17,-30.37 18.43,-44.8 q 1.12,-2.22 3.5,-1.5 l 0.45,0.14 a 3.88,3.87 16.7 0 1 2.59,4.81 c -4.79,16.16 -11.42,31.25 -18.02,46.96 a 1.59,1.57 -84.6 0 1 -1.14,0.95 z","m 847.54,670.63 q 10.11,-7.1 20.43,-14.71 c 1.82,-1.34 3.93,-1.24 5.61,0.25 q 1.28,1.14 0.18,2.44 -14.63,17.34 -36.12,26.57 -2.85,1.23 -6.59,0.27 -0.66,-0.17 -1.22,-0.56 -2.78,-1.93 0.1,-3.71 c 5.74,-3.54 11.89,-6.53 17.61,-10.55 z","m 857.95,703.24 c -4.4,13.3 -9.63,25.8 -16.79,37.33 q -1.72,2.78 -4.23,4.64 a 1.09,1.09 0 0 1 -1.74,-0.77 c -1.48,-16.04 8.92,-33.06 16.88,-46.35 q 0.57,-0.95 1.66,-0.8 c 2.95,0.39 5.23,2.88 4.22,5.95 z","m 875.79,722.55 c -4.07,11.58 -8.41,22.82 -15.01,33 q -1.32,2.03 -3.6,3.48 -0.5,0.32 -0.97,-0.05 c -2.61,-2.08 -1.53,-6.69 -0.45,-9.36 q 5.27,-13.01 10.45,-25.43 1.81,-4.36 4.29,-8.03 0.62,-0.92 1.74,-0.88 c 4.7,0.15 4.82,3.66 3.55,7.27 z","m 887.56,727.64 q -3.99,9.72 -9.96,19.13 -0.8,1.27 -2.1,1.97 a 0.74,0.74 0 0 1 -1.08,-0.76 q 1.91,-12.47 8.14,-23.5 2.02,-3.58 4.82,-5.9 a 0.94,0.94 0 0 1 1.47,0.36 c 1.15,2.7 -0.24,6.15 -1.29,8.7 z","m 902.05,654.63 c 1.72,20.81 -0.74,43.2 -13.83,59.63 -1.5,1.87 -3.8,1.35 -5.78,0.55 q -7.06,-2.82 -10.22,-9.71 c -0.79,-1.73 -2.34,-3.24 -3.92,-4.25 q -5.99,-3.87 -10.35,-8.32 a 2.19,2.18 28.4 0 1 -0.59,-1.97 q 1.69,-8.62 7.46,-16.29 7.41,-9.85 18.48,-23.61 0.37,-0.46 0.96,-0.45 8.59,0.19 16.75,3.06 a 1.58,1.57 -82.5 0 1 1.04,1.36 z","m 1390.47,703.5 c -7.03,-15.23 -8.6,-32.38 -6.98,-49.44 a 0.84,0.84 0 0 1 0.55,-0.71 q 8.24,-2.9 16.94,-3.13 a 1.84,1.82 -19.6 0 1 1.51,0.71 q 7.66,9.84 15.58,19.76 7.56,9.46 9.92,18.85 c 0.44,1.72 -0.22,3.09 -1.51,4.23 q -4.73,4.18 -10.02,7.65 -2.13,1.39 -3.17,3.63 c -2.09,4.49 -5.23,7.65 -10.01,9.66 q -2.32,0.97 -4.45,0.69 -0.49,-0.06 -0.83,-0.42 -4.41,-4.73 -7.53,-11.48 z","m 1434.6,668.37 q 8.2,5.82 20.84,12.66 3.02,1.63 0.3,3.74 -0.49,0.38 -1.08,0.56 -3.68,1.1 -6.72,-0.17 -21.61,-9.06 -36.36,-26.82 -0.91,-1.09 0.16,-2.02 c 2.56,-2.23 4.17,-1.59 6.69,0.28 q 8.2,6.11 16.17,11.77 z","m 1440.18,733.51 c -5.02,-9.9 -10.12,-20.82 -13.01,-31.69 q -0.15,-0.58 0.1,-1.13 1.52,-3.24 4.84,-3.48 0.7,-0.05 1.06,0.55 c 8.34,13.8 18.29,30.28 17.05,46.84 a 0.97,0.97 0 0 1 -1.49,0.74 c -3.56,-2.27 -6.64,-8.06 -8.55,-11.83 z","m 1431.61,747.81 q -8.05,-18.9 -13.4,-35.42 -0.77,-2.34 0.9,-4.93 0.26,-0.41 0.73,-0.64 3.35,-1.63 4.67,0.91 3.49,6.76 6.36,13.86 6.23,15.39 12.52,32.24 c 1.04,2.81 0.09,4.43 -2.57,5.47 q -3.87,1.52 -5.42,-2.33 -1.86,-4.6 -3.79,-9.16 z","m 1409.03,720.33 c -1.15,-3.85 4.48,-7.35 6.5,-3.44 1.02,1.99 2.25,3.91 3.11,5.95 q 5.89,13.98 11.33,27.6 1.3,3.26 0.57,7.15 a 1.77,1.77 0 0 1 -2.76,1.12 c -3.33,-2.36 -6.22,-7.81 -8.08,-11.63 -4.21,-8.63 -7.92,-17.55 -10.67,-26.75 z","m 1407.92,746.8 c -4.19,-6.47 -7.65,-13.37 -10.63,-20.61 q -1.4,-3.4 -0.83,-7.06 a 0.95,0.94 -66.3 0 1 1.52,-0.6 q 2.67,2.14 4.37,5.02 6.61,11.16 8.73,24.5 a 0.6,0.6 0 0 1 -0.81,0.65 q -1.51,-0.6 -2.35,-1.9 z","m 1105.13,759.58 c 5.8,-4.09 12.22,-8.33 18.77,-11.59 a 0.54,0.54 0 0 1 0.76,0.34 q 6.8,24.08 7.01,48.83 0.14,15.6 -0.3,31.35 -0.18,6.57 -1.58,13.23 -3.79,18 -7.08,34.37 -5.52,27.39 -7.25,53.63 -0.01,0.2 -0.21,0.19 h -0.01 q -0.21,0 -0.21,-0.21 0.06,-11.05 -1.13,-21.98 -3.47,-31.95 -10.99,-67.96 c -3.17,-15.15 -5.96,-29.39 -7.82,-44.53 q -1.41,-11.59 -0.64,-23.56 c 0.24,-3.74 7.85,-10.11 10.68,-12.11 z","m 1103.25,880.79 q 0.13,-2.03 0.52,-4.03 0.12,-0.59 0.23,0 6.54,34.5 9.4,71 1.16,14.9 0.83,30.46 c -0.35,16.42 -2.06,31.59 -8.52,45.43 q -0.65,1.38 -0.93,-0.12 -1.07,-5.81 -1.74,-12.63 c -4.13,-42 -2.61,-84.7 0.21,-130.11 z","m 1170.31,929.79 q -0.01,0.14 -0.14,0.14 h -0.01 q -0.15,0 -0.16,-0.15 c -1.65,-27.23 -6.7,-52.91 -12.71,-80.29 q -1.04,-4.77 -2.03,-9.9 -0.97,-5.04 -1.11,-10.04 -0.71,-25 0,-41.29 0.88,-20.1 6.78,-40.2 a 0.39,0.39 0 0 1 0.58,-0.23 c 9.66,5.72 22.47,11.93 28.7,21.66 q 0.95,1.49 1.03,4.47 c 0.23,9.46 -0.26,19.48 -1.78,29.2 q -3.09,19.91 -8.67,45.97 -4.33,20.25 -8.36,50.7 c -1.32,9.97 -1.83,19.78 -2.12,29.96 z","m 1182.66,888.38 c 3.02,46.17 3.96,90.2 -1.62,134.34 q -0.38,3.02 -1.62,0.24 -5.13,-11.5 -6.75,-24.8 -2.53,-20.86 -1.07,-43.33 2.55,-39.34 9.73,-77.33 0.48,-2.54 0.64,0.04 0.36,5.65 0.69,10.84 z","m 1079.17,1372.43 q 2.14,-5.13 4.33,-10.15 0.64,-1.46 0.77,0.13 0.19,2.38 0.04,5.19 -0.48,8.92 -1.17,17.73 c -0.55,6.98 -0.51,14.62 0.97,21.25 q 0.81,3.6 3.17,7.55 2.42,4.04 6.98,2.53 2.09,-0.69 2.69,-2.94 c 5.05,-18.81 8.53,-32.57 10.08,-47.69 q 0.17,-1.71 0.65,-0.06 c 4.88,16.71 7.77,34.11 9.71,51.52 a 2.81,2.81 0 0 1 -0.55,2.02 c -5.24,7.01 -14.76,8.75 -23.07,8.71 -8.31,-0.03 -16.89,-1.11 -24.67,-3.97 -3.23,-1.19 -5.27,-5.26 -4.38,-8.25 4.42,-14.8 8.65,-29.67 14.45,-43.57 z","m 1206.8,1373.7 q 8.72,22.21 13.81,42.33 0.94,3.7 -1.71,6.53 -1.44,1.54 -5.28,2.63 -14.71,4.18 -29.01,2.49 -9.46,-1.11 -14.96,-6.81 -1.68,-1.75 -1.46,-3.83 2.75,-26.35 9.42,-50.74 0.68,-2.5 0.89,0.09 0.63,7.86 2.59,16.87 2.96,13.59 7.32,30.23 0.79,2.98 3.86,3.47 3.52,0.56 5.1,-1.58 c 2.84,-3.86 4.42,-8.89 4.83,-13.69 0.96,-11.15 -0.35,-22.32 -1,-33 q -0.2,-3.25 0.17,-6.56 0.09,-0.79 0.42,-0.07 2.74,5.83 5.01,11.64 z"]}}};
  /* BODY END */
  /* BANK BEGIN */
  /* GENERATED by tools/build_exercises.py — do not edit by hand.
     [key, name, category, search aliases, note, modes, muscles]
     A number is points per unit; an object is a gym lift priced from
     bodyweight and load. `muscles` is the share of the points each
     region gets, adding to 1. Mirrors public.exercises. */
  var EX_BANK = [
  ['wallpush','Wall Push-up','PUSH','wall easy beginner','',{reps:0.25},{chest:0.5,shoulders:0.2,triceps:0.3}],
  ['kneepush','Knee Push-up','PUSH','knees modified beginner','',{reps:0.75},{chest:0.5,shoulders:0.2,triceps:0.3}],
  ['inclinepush','Incline Push-up','PUSH','bench elevated hands raised','',{reps:0.75},{chest:0.5,shoulders:0.2,triceps:0.3}],
  ['pushups','Push-ups','PUSH','pushup press floor','Any hand position. Wide, diamond and decline have their own entries.',{reps:1.0},{chest:0.45,shoulders:0.15,triceps:0.3,abs:0.1}],
  ['widepush','Wide Push-up','PUSH','wide grip chest','',{reps:1.0},{chest:0.6,shoulders:0.2,triceps:0.2}],
  ['diamondpush','Diamond Push-up','PUSH','triceps close narrow','',{reps:1.0},{chest:0.35,shoulders:0.15,triceps:0.5}],
  ['declinepush','Decline Push-up','PUSH','feet elevated','',{reps:1.25},{chest:0.4,shoulders:0.3,triceps:0.25,abs:0.05}],
  ['pikepush','Pike Push-up','PUSH','shoulders delts','',{reps:1.25},{chest:0.15,shoulders:0.55,triceps:0.3}],
  ['clappush','Clap Push-up','PUSH','explosive plyo power','',{reps:1.5},{chest:0.4,shoulders:0.2,triceps:0.3,abs:0.1}],
  ['archerpush','Archer Push-up','PUSH','one side unilateral','',{reps:1.5},{chest:0.45,shoulders:0.2,triceps:0.25,abs:0.1}],
  ['planchepush','Pseudo Planche Push-up','PUSH','lean planche straight arm','',{reps:1.5},{chest:0.3,shoulders:0.35,triceps:0.15,abs:0.2}],
  ['benchdips','Bench Dips','PUSH','tricep chair','',{reps:0.75},{chest:0.2,shoulders:0.2,triceps:0.6}],
  ['dips','Dips','PUSH','parallel bars triceps','Parallel bars, rings or between two chairs',{reps:1.5},{chest:0.4,shoulders:0.2,triceps:0.4}],
  ['ringdips','Ring Dips','PUSH','rings unstable','',{reps:1.75},{chest:0.35,shoulders:0.2,triceps:0.35,abs:0.1}],
  ['onearmpush','One-arm Push-up','PUSH','single arm','',{reps:2.0},{chest:0.4,shoulders:0.15,triceps:0.25,abs:0.2}],
  ['sphinxpush','Sphinx Push-up','PUSH','sphinx forearm tricep','',{reps:0.75},{chest:0.15,triceps:0.7,abs:0.15}],
  ['handstand','Handstand Push-up','PUSH','hspu wall overhead invert','Against a wall, freestanding, or hanging from a bar',{reps:2.5,seconds:0.3},{shoulders:0.55,triceps:0.3,traps:0.1,abs:0.05}],
  ['rows','Inverted Rows','PULL','australian bodyweight row horizontal','',{reps:1.0},{biceps:0.25,forearms:0.15,traps:0.2,lats:0.4}],
  ['scapulapull','Scapular Pull-up','PULL','scap shrug','',{reps:0.75},{forearms:0.2,traps:0.5,lats:0.3}],
  ['bandpullup','Assisted Pull-up','PULL','band assisted machine','',{reps:1.25},{biceps:0.3,forearms:0.15,traps:0.1,lats:0.45}],
  ['chinups','Chin-ups','PULL','supinated underhand biceps','',{reps:2.0},{biceps:0.4,forearms:0.15,traps:0.1,lats:0.35}],
  ['pullups','Pull-ups','PULL','pullup overhand lats','Overhand grip. Kipping counts, but be honest.',{reps:2.0},{biceps:0.25,forearms:0.15,traps:0.15,lats:0.45}],
  ['widepullup','Wide-grip Pull-up','PULL','wide lats','',{reps:2.25},{biceps:0.15,forearms:0.15,traps:0.15,lats:0.55}],
  ['commandopull','Commando Pull-up','PULL','mixed grip','',{reps:2.25},{biceps:0.3,forearms:0.15,lats:0.4,obliques:0.15}],
  ['lsitpullup','L-sit Pull-up','PULL','lsit legs out','',{reps:2.5},{biceps:0.2,forearms:0.2,lats:0.35,abs:0.25}],
  ['typewriter','Typewriter Pull-up','PULL','side to side','',{reps:2.5},{biceps:0.2,forearms:0.2,lats:0.4,obliques:0.2}],
  ['archerpull','Archer Pull-up','PULL','one side unilateral','',{reps:2.5},{biceps:0.25,forearms:0.2,lats:0.4,obliques:0.15}],
  ['muscleup','Muscle-up / Flag','PULL','muscleup bar ring humanflag','Bar or rings. The human flag scores here too.',{reps:3.5,seconds:2.0},{shoulders:0.15,biceps:0.2,triceps:0.2,forearms:0.15,lats:0.3}],
  ['deadhang','Dead Hang','PULL','hang grip forearm','',{seconds:0.2},{forearms:0.6,traps:0.2,lats:0.2}],
  ['frontlever','Front Lever','PULL','lever static hold','',{seconds:0.75},{forearms:0.2,lats:0.35,lowerback:0.15,abs:0.3}],
  ['calves','Calf Raises','LEGS','calf standing seated','',{reps:0.2},{calves:1.0}],
  ['airsquats','Air Squats','LEGS','bodyweight squat','',{reps:0.5},{glutes:0.3,quads:0.5,hamstrings:0.2}],
  ['jumpsquats','Jump Squats','LEGS','plyo explosive','',{reps:0.75},{glutes:0.25,quads:0.45,hamstrings:0.1,calves:0.2}],
  ['lunges','Lunges','LEGS','walking reverse forward','',{reps:0.5},{glutes:0.35,quads:0.4,hamstrings:0.25}],
  ['splitsquat','Bulgarian Split Squat','LEGS','bulgarian rear foot elevated','',{reps:0.75},{glutes:0.35,quads:0.4,hamstrings:0.25}],
  ['stepups','Step-ups','LEGS','box bench','',{reps:0.5},{glutes:0.35,quads:0.4,hamstrings:0.15,calves:0.1}],
  ['gluteBridge','Glute Bridge','LEGS','hip thrust glutes','',{reps:0.25},{lowerback:0.1,glutes:0.6,hamstrings:0.3}],
  ['nordic','Nordic Curl','LEGS','hamstring eccentric','',{reps:1.0},{glutes:0.15,hamstrings:0.75,calves:0.1}],
  ['sissy','Sissy Squat','LEGS','quads','',{reps:0.75},{abs:0.1,quads:0.8,calves:0.1}],
  ['shrimp','Shrimp Squat','LEGS','advanced single leg','',{reps:1.0},{glutes:0.3,quads:0.45,hamstrings:0.15,calves:0.1}],
  ['pistols','Pistol Squats','LEGS','single leg one','',{reps:2.0},{glutes:0.3,quads:0.45,hamstrings:0.15,calves:0.1}],
  ['wallsit','Wall Sit','LEGS','isometric quads','',{seconds:0.15},{glutes:0.2,quads:0.7,calves:0.1}],
  ['crunches','Crunches','CORE','crunch sit abs','',{reps:0.25},{abs:1.0}],
  ['situps','Sit-ups','CORE','situp full','',{reps:0.5},{abs:0.8,obliques:0.1,quads:0.1}],
  ['twists','Russian Twists','CORE','oblique twist side','',{reps:0.25},{abs:0.3,obliques:0.7}],
  ['legraises','Lying Leg Raises','CORE','floor lying','',{reps:0.5},{abs:0.8,quads:0.2}],
  ['kneeraises','Hanging Knee Raises','CORE','hanging knee tuck','',{reps:1.0},{forearms:0.15,abs:0.7,obliques:0.15}],
  ['hangingleg','Hanging Leg Raises','CORE','straight leg toes','',{reps:1.5},{forearms:0.15,abs:0.65,obliques:0.1,quads:0.1}],
  ['toestobar','Toes to Bar','CORE','ttb crossfit','',{reps:2.0},{forearms:0.15,lats:0.15,abs:0.6,obliques:0.1}],
  ['dragonflag','Dragon Flag','CORE','dragon advanced','',{reps:3.0},{lats:0.15,lowerback:0.2,abs:0.55,obliques:0.1}],
  ['vups','V-ups','CORE','v up jackknife','',{reps:0.75},{abs:0.75,obliques:0.1,quads:0.15}],
  ['supermans','Supermans','CORE','lower back extension','',{reps:0.25},{traps:0.15,lowerback:0.6,glutes:0.25}],
  ['sidecrunch','Lateral Crunches','CORE','side oblique lateral crunch','',{reps:0.25},{abs:0.2,obliques:0.8}],
  ['bicycle','Bicycle Crunches','CORE','bicycle cycling abs','',{reps:0.25},{abs:0.5,obliques:0.5}],
  ['deadbug','Dead Bug','CORE','deadbug stability','',{reps:0.5},{lowerback:0.2,abs:0.8}],
  ['birddog','Bird Dog','CORE','birddog stability back','',{reps:0.5},{lowerback:0.5,abs:0.25,glutes:0.25}],
  ['flutterkick','Flutter Kicks','CORE','flutter scissor kicks','',{reps:0.25},{abs:0.7,quads:0.3}],
  ['mountainclimb','Mountain Climbers','CORE','mountain climber cardio abs','',{reps:0.25},{shoulders:0.2,abs:0.5,obliques:0.1,quads:0.2}],
  ['rollout','Ab Wheel Rollout','CORE','ab wheel rollout barbell','',{reps:2.0},{shoulders:0.1,lats:0.2,lowerback:0.1,abs:0.6}],
  ['plank','Plank','CORE','front elbow forearm','',{minutes:10.0},{shoulders:0.2,lowerback:0.2,abs:0.6}],
  ['sideplank','Side Plank','CORE','oblique side','',{minutes:12.0},{shoulders:0.2,abs:0.1,obliques:0.7}],
  ['hollowhold','Hollow Hold','CORE','hollow body','',{seconds:0.25},{abs:0.8,quads:0.2}],
  ['lsit','L-sit','CORE','lsit legs parallel','',{seconds:0.4},{triceps:0.2,abs:0.6,quads:0.2}],
  ['run','Run','CARDIO','running jog jogging','Outdoor or treadmill',{km:5.0},{glutes:0.15,quads:0.3,hamstrings:0.25,calves:0.3}],
  ['sprints','Sprint Intervals','CARDIO','sprint interval hiit','One sprint = 15 sec flat out, 100 m minimum',{reps:2.0},{glutes:0.2,quads:0.3,hamstrings:0.3,calves:0.2}],
  ['bike','Biking','CARDIO','cycling bicycle spin','Road / Trail / Stationary',{km:1.5},{glutes:0.2,quads:0.5,hamstrings:0.1,calves:0.2}],
  ['swim','Swim','CARDIO','swimming pool crawl','Any stroke, active swim time',{minutes:0.5},{chest:0.15,shoulders:0.3,triceps:0.1,lats:0.3,abs:0.15}],
  ['walk','Walking','CARDIO','walk hike hiking steps','Hiking counts too',{km:2.5},{glutes:0.15,quads:0.3,hamstrings:0.2,calves:0.35}],
  ['row','Rowing Machine','CARDIO','erg ergometer concept2','Indoor erg',{minutes:0.5},{biceps:0.15,traps:0.15,lats:0.3,lowerback:0.15,quads:0.25}],
  ['jumprope','Jump Rope','CARDIO','skipping rope','Skipping',{minutes:0.55},{shoulders:0.15,quads:0.2,hamstrings:0.1,calves:0.55}],
  ['stairs','Stair Climbing','CARDIO','stairmaster steps','Real stairs or machine',{minutes:0.5},{glutes:0.3,quads:0.4,hamstrings:0.1,calves:0.2}],
  ['football','Football / Soccer','SPORT','soccer foot futbol match','Actual playing time, not time at the venue',{minutes:0.25},{shoulders:0.1,abs:0.1,glutes:0.15,quads:0.25,hamstrings:0.2,calves:0.2}],
  ['basketball','Basketball','SPORT','basket hoops ball','Actual playing time, not time at the venue',{minutes:0.25},{shoulders:0.1,abs:0.1,glutes:0.15,quads:0.25,hamstrings:0.15,calves:0.25}],
  ['rugby','Rugby','SPORT','rugby union league','Actual playing time, not time at the venue',{minutes:0.25},{shoulders:0.15,abs:0.1,glutes:0.15,quads:0.25,hamstrings:0.2,calves:0.15}],
  ['handball','Handball','SPORT','hand ball','Actual playing time, not time at the venue',{minutes:0.25},{shoulders:0.2,abs:0.15,obliques:0.1,quads:0.25,hamstrings:0.1,calves:0.2}],
  ['hockey','Hockey','SPORT','ice field puck','Actual playing time, not time at the venue',{minutes:0.25},{forearms:0.1,obliques:0.15,glutes:0.2,quads:0.3,hamstrings:0.15,calves:0.1}],
  ['squash','Squash','SPORT','squash racket court','Actual playing time, not time at the venue',{minutes:0.25},{shoulders:0.15,forearms:0.1,obliques:0.15,quads:0.3,hamstrings:0.1,calves:0.2}],
  ['boxing','Boxing / Martial Arts','SPORT','box mma judo bjj karate muay sparring','Actual playing time, not time at the venue',{minutes:0.25},{chest:0.1,shoulders:0.25,triceps:0.15,abs:0.2,obliques:0.15,calves:0.15}],
  ['climbing','Climbing','SPORT','bouldering rock wall','Actual playing time, not time at the venue',{minutes:0.25},{shoulders:0.1,biceps:0.15,forearms:0.3,lats:0.3,abs:0.15}],
  ['tennis','Tennis','SPORT','tennis racket court','Actual playing time, not time at the venue',{minutes:0.166667},{shoulders:0.2,forearms:0.1,obliques:0.15,quads:0.25,hamstrings:0.1,calves:0.2}],
  ['padel','Padel','SPORT','padel paddle','Actual playing time, not time at the venue',{minutes:0.166667},{shoulders:0.2,forearms:0.1,obliques:0.15,quads:0.25,hamstrings:0.1,calves:0.2}],
  ['volleyball','Volleyball','SPORT','volley beach net','Actual playing time, not time at the venue',{minutes:0.166667},{shoulders:0.25,abs:0.1,quads:0.3,hamstrings:0.1,calves:0.25}],
  ['badminton','Badminton','SPORT','badminton shuttle','Actual playing time, not time at the venue',{minutes:0.166667},{shoulders:0.2,forearms:0.15,obliques:0.15,quads:0.25,calves:0.25}],
  ['tabletennis','Table Tennis','SPORT','ping pong','Actual playing time, not time at the venue',{minutes:0.166667},{shoulders:0.25,forearms:0.2,obliques:0.2,quads:0.2,calves:0.15}],
  ['othersport','Other Sport','SPORT','other misc game match','Actual playing time, not time at the venue',{minutes:0.166667},{chest:0.075,shoulders:0.15,lats:0.075,abs:0.15,glutes:0.1,quads:0.2,hamstrings:0.1,calves:0.15}],
  ['gymbench','Bench Press','GYM','bench barbell chest press flat','Type the total on the bar, not counting your own weight',{reps:{k:1.5625,equip:1.0,legs:false}},{chest:0.5,shoulders:0.2,triceps:0.3}],
  ['gymdbbench','Dumbbell Bench Press','GYM','dumbbell db incline chest','Type the total on the bar, not counting your own weight',{reps:{k:1.5625,equip:1.0,legs:false}},{chest:0.5,shoulders:0.25,triceps:0.25}],
  ['gymohp','Overhead Press','GYM','ohp military shoulder press standing','Type the total on the bar, not counting your own weight',{reps:{k:1.5625,equip:1.0,legs:false}},{shoulders:0.55,triceps:0.3,traps:0.15}],
  ['gymdip','Weighted Dips','GYM','weighted dip belt','Type the total on the bar, not counting your own weight',{reps:{k:1.5625,equip:1.0,legs:false}},{chest:0.4,shoulders:0.2,triceps:0.4}],
  ['gymchestmach','Chest Press (Machine)','GYM','machine chest press pec','Type the total on the bar, not counting your own weight',{reps:{k:1.5625,equip:0.75,legs:false}},{chest:0.6,shoulders:0.15,triceps:0.25}],
  ['gymtricep','Tricep Pushdown','GYM','cable pushdown tricep rope','One side at a time — type the weight of the single dumbbell',{reps:{k:1.5625,equip:0.6,legs:false}},{triceps:1.0}],
  ['gymlatraise','Lateral Raise','GYM','side delt raise shoulder','One side at a time — type the weight of the single dumbbell',{reps:{k:1.5625,equip:1.0,legs:false}},{shoulders:0.85,traps:0.15}],
  ['gymdeadlift','Deadlift','GYM','deadlift conventional sumo barbell','Type the total on the bar, not counting your own weight',{reps:{k:2.0,equip:1.0,legs:false}},{forearms:0.1,traps:0.15,lowerback:0.25,glutes:0.25,hamstrings:0.25}],
  ['gymbarbellrow','Barbell Row','GYM','bent over row pendlay','Type the total on the bar, not counting your own weight',{reps:{k:2.0,equip:1.0,legs:false}},{biceps:0.2,forearms:0.1,traps:0.2,lats:0.4,lowerback:0.1}],
  ['gymdbrow','Dumbbell Row','GYM','one arm db row','One side at a time — type the weight of the single dumbbell',{reps:{k:2.0,equip:1.0,legs:false}},{biceps:0.2,forearms:0.15,traps:0.2,lats:0.45}],
  ['gymlatpull','Lat Pulldown','GYM','pulldown machine lats','Type the total on the bar, not counting your own weight',{reps:{k:2.0,equip:0.75,legs:false}},{biceps:0.3,forearms:0.15,lats:0.55}],
  ['gymcablerow','Seated Cable Row','GYM','cable row seated','Type the total on the bar, not counting your own weight',{reps:{k:2.0,equip:0.6,legs:false}},{biceps:0.2,forearms:0.1,traps:0.25,lats:0.45}],
  ['gymweightpull','Weighted Pull-up','GYM','weighted pullup belt','Type the total on the bar, not counting your own weight',{reps:{k:2.0,equip:1.0,legs:false}},{biceps:0.25,forearms:0.15,traps:0.15,lats:0.45}],
  ['gymcurl','Bicep Curl','GYM','curl barbell dumbbell biceps','One side at a time — type the weight of the single dumbbell',{reps:{k:2.0,equip:1.0,legs:false}},{biceps:0.8,forearms:0.2}],
  ['gymfacepull','Face Pull','GYM','cable rear delt','Type the total on the bar, not counting your own weight',{reps:{k:2.0,equip:0.6,legs:false}},{shoulders:0.5,biceps:0.15,traps:0.35}],
  ['gymsquat','Back Squat','GYM','squat barbell back high bar','Type the total on the bar, not counting your own weight',{reps:{k:0.588,equip:1.0,legs:true}},{lowerback:0.1,glutes:0.3,quads:0.45,hamstrings:0.15}],
  ['gymfrontsquat','Front Squat','GYM','front squat clean grip','Type the total on the bar, not counting your own weight',{reps:{k:0.588,equip:1.0,legs:true}},{lowerback:0.1,abs:0.15,glutes:0.2,quads:0.55}],
  ['gymlegpress','Leg Press','GYM','leg press machine','Type the total on the bar, not counting your own weight',{reps:{k:0.588,equip:0.75,legs:true}},{glutes:0.3,quads:0.55,hamstrings:0.15}],
  ['gymrdl','Romanian Deadlift','GYM','rdl stiff leg hamstring','Type the total on the bar, not counting your own weight',{reps:{k:0.588,equip:1.0,legs:true}},{lowerback:0.25,glutes:0.3,hamstrings:0.45}],
  ['gymhipthrust','Hip Thrust','GYM','glute bridge barbell','Type the total on the bar, not counting your own weight',{reps:{k:0.588,equip:1.0,legs:true}},{lowerback:0.05,glutes:0.7,hamstrings:0.25}],
  ['gymlegcurl','Leg Curl','GYM','hamstring machine curl','Type the total on the bar, not counting your own weight',{reps:{k:0.588,equip:0.75,legs:false}},{hamstrings:0.9,calves:0.1}],
  ['gymlegext','Leg Extension','GYM','quad machine extension','Type the total on the bar, not counting your own weight',{reps:{k:0.588,equip:0.75,legs:false}},{quads:1.0}],
  ['gymlunge','Weighted Lunge','GYM','dumbbell lunge walking','Type the total on the bar, not counting your own weight',{reps:{k:0.588,equip:1.0,legs:true}},{glutes:0.35,quads:0.4,hamstrings:0.25}],
  ['gymcalf','Weighted Calf Raise','GYM','calf machine standing','Type the total on the bar, not counting your own weight',{reps:{k:0.588,equip:0.75,legs:false}},{calves:1.0}],
  ['gymcablecrunch','Cable Crunch','GYM','cable crunch kneeling abs','Type the total on the bar, not counting your own weight',{reps:{k:1.0,equip:0.6,legs:false}},{abs:0.9,obliques:0.1}],
  ['gymwoodchop','Woodchoppers','GYM','woodchop cable oblique rotation','One side at a time — type the weight of the single dumbbell',{reps:{k:1.0,equip:0.6,legs:false}},{shoulders:0.1,abs:0.2,obliques:0.7}],
  ['gympullover','Dumbbell Pullover','GYM','pullover lats chest','Type the total on the bar, not counting your own weight',{reps:{k:2.0,equip:1.0,legs:false}},{chest:0.25,triceps:0.2,lats:0.55}],
  ['gymshrug','Shrug','GYM','shrug traps barbell','Type the total on the bar, not counting your own weight',{reps:{k:2.0,equip:1.0,legs:false}},{forearms:0.15,traps:0.85}],
  ['gymincline','Incline Bench Press','GYM','incline bench upper chest','Type the total on the bar, not counting your own weight',{reps:{k:1.5625,equip:1.0,legs:false}},{chest:0.45,shoulders:0.3,triceps:0.25}],
  ['gymgoblet','Goblet Squat','GYM','goblet kettlebell squat','Type the total on the bar, not counting your own weight',{reps:{k:0.588,equip:1.0,legs:true}},{abs:0.15,glutes:0.3,quads:0.45,hamstrings:0.1}],
  ['gymstepup','Weighted Step-up','GYM','step up box weighted','Type the total on the bar, not counting your own weight',{reps:{k:0.588,equip:1.0,legs:true}},{glutes:0.35,quads:0.4,hamstrings:0.15,calves:0.1}],
  ['stretch','Stretching Session','RECOVERY','stretch mobility yoga flexibility','At least 10 minutes of stretching, mobility or yoga',{flat:5.0},{}],
  ['sauna','Sauna / Cold Plunge','RECOVERY','sauna ice bath cold recovery','',{flat:3.0},{}]
  ];
  var MUSCLE_META = [{"key":"chest","name":"Chest","view":"front","target":70},{"key":"shoulders","name":"Shoulders","view":"both","target":55},{"key":"biceps","name":"Biceps","view":"front","target":30},{"key":"triceps","name":"Triceps","view":"both","target":40},{"key":"forearms","name":"Forearms","view":"both","target":25},{"key":"traps","name":"Traps","view":"both","target":30},{"key":"lats","name":"Lats","view":"back","target":70},{"key":"lowerback","name":"Lower back","view":"back","target":30},{"key":"abs","name":"Abs","view":"front","target":45},{"key":"obliques","name":"Obliques","view":"front","target":30},{"key":"glutes","name":"Glutes","view":"back","target":55},{"key":"quads","name":"Quads","view":"front","target":85},{"key":"hamstrings","name":"Hamstrings","view":"back","target":55},{"key":"calves","name":"Calves","view":"both","target":25}];
  /* BANK END */


  /* UI-only tweaks. The rate is never here — only how a unit is named,
     stepped and pre-filled, because "6 sprints" reads better than "6 reps". */
  var EX_UI = {
    sprints:  { reps: { unitLabel: 'SPRINTS', short: 'sprints', def: 6, quick: [4, 6, 8, 12] } },
    stretch:  { flat: { unitLabel: 'SESSIONS', short: 'sessions', def: 1, quick: [1, 2] } },
    sauna:    { flat: { unitLabel: 'SESSIONS', short: 'sessions', def: 1, quick: [1, 2] } },
    plank:    { minutes: { def: 2, quick: [1, 2, 3, 5] } },
    sideplank:{ minutes: { def: 2, quick: [1, 2, 3, 5] } },
    swim:     { minutes: { def: 30, quick: [20, 30, 45, 60] } },
    lsit:     { seconds: { def: 30, quick: [15, 30, 45, 60] } },
    deadhang: { seconds: { def: 60, quick: [30, 60, 90, 120] } },
    wallsit:  { seconds: { def: 60, quick: [30, 60, 90, 120] } }
  };

  /* Sports and gym sets are logged in the units people actually speak in. */
  var SPORT_UI  = { minutes: { def: 60, quick: [30, 45, 60, 90] } };
  var GYM_UI    = { reps:    { def: 8,  quick: [5, 8, 10, 12] } };


  /* Say the rate the way a person would. Time-based rates read per hour,
     and a slow drip like the L-sit reads "1 pt / 3 sec" rather than "0.33". */
  function rateLabel(mode, rate, key) {
    function n(x) { return String(Math.round(x * 100) / 100); }
    if (mode === 'minutes') return n(rate * 60) + ' pts / hour';
    if (mode === 'seconds') {
      return rate >= 1 ? n(rate) + ' pts / sec' : '1 pt / ' + n(1 / rate) + ' sec';
    }
    if (mode === 'km')   return n(rate) + ' pts / km';
    if (mode === 'flat') return n(rate) + ' pts / session';
    if (key === 'sprints') return n(rate) + ' pts / sprint';
    return (rate === 1 ? '1 pt' : n(rate) + ' pts') + ' / rep';
  }

  /* Expand the generated bank into the shape the rest of the app uses. */
  var EXERCISES = EX_BANK.map(function (r) {
    var key = r[0], cat = r[2], raw = r[5], muscles = r[6] || {};
    var modes = Object.keys(raw).map(function (mode) {
      var v = raw[mode];
      var ui = (EX_UI[key] && EX_UI[key][mode]) ||
               (cat === 'SPORT' ? SPORT_UI[mode] : cat === 'GYM' ? GYM_UI[mode] : null) || {};
      var m = {
        mode: mode,
        gym: typeof v === 'object',
        rate: typeof v === 'object' ? 0 : v,
        k: typeof v === 'object' ? v.k : 0,
        equip: typeof v === 'object' ? v.equip : 0,
        legs: typeof v === 'object' ? !!v.legs : false
      };
      m.label = m.gym ? 'from your bodyweight and the bar'
                      : rateLabel(mode, m.rate, key);
      ['unitLabel', 'short', 'step', 'def', 'quick'].forEach(function (f) {
        if (ui[f] !== undefined) m[f] = ui[f];
      });
      return m;
    });
    return { key: key, name: r[1], cat: cat, aliases: r[3], variants: r[4],
             modes: modes, muscles: muscles };
  });

  /* Order the categories appear in menus and on the scoring card. */
  var CATEGORIES = ['PUSH', 'PULL', 'LEGS', 'CORE', 'CARDIO', 'SPORT', 'GYM', 'RECOVERY'];

  /* Menu grouping is not training grouping. A bench press trains PUSH and a
     football match is cardio, so combos and the stats bars stay at five
     groups however many menu sections exist. Mirrors exercise_category() in SQL. */
  function trainingCat(key) {
    var ex = exercise(key);
    if (!ex) return null;
    if (ex.cat === 'SPORT') return 'CARDIO';
    if (ex.cat === 'GYM') {
      var m = ex.modes[0];
      return m.legs ? 'LEGS' : m.k >= 2 ? 'PULL' : m.k >= 1.5 ? 'PUSH' : 'LEGS';
    }
    return ex.cat;
  }

  /* Muscle groups that count towards the daily combo (recovery is excluded
     so a stretch cannot buy a group). Mirrors week_combo_bonus() in SQL. */
  var COMBO_CATS = ['PUSH', 'PULL', 'LEGS', 'CORE', 'CARDIO'];
  var COMBO_MIN = 10;
  var COMBO_TIERS = [{ n: 5, pts: 12 }, { n: 4, pts: 8 }, { n: 3, pts: 5 }];

  /* Divisions. Where you are is where you finished, so it maintains itself
     with no bookkeeping. Each one is a place with a name and a crest, not a
     colour with a number — you are in the VANGUARD, not in "tier 2".
     The sizes narrow sharply at the top on purpose: two in the ROYAL GUARD
     and three in the APEX means the top of the table is somewhere you can be
     pushed out of by one good evening, which is the whole point of it.

     Nothing here is a god or a throne. They are posts in an order: you start
     outside it, you are made in the forge, you fight in the line, and two
     people stand at the crown. Every one of them is somewhere you can be
     moved out of by Sunday.

     `rank` is which numbering a division belongs to. The Royal Guard and the
     Apex share one, so the top five read 1 to 5 straight through — they are
     the top of the league, not two separate races. Everything below counts
     from 1 again: your number is your place among the people you are
     actually fighting. */
  var TIERS = [
    { key: 'DIAMOND', name: 'ROYAL GUARD', sub: 'The two who stand closest to the crown',
      icon: 'crown',              size: 2,  rank: 'top' },
    { key: 'GOLD',    name: 'APEX',        sub: 'Three deep, and one push off the front',
      icon: 'crossed-swords',     size: 3,  rank: 'top' },
    { key: 'SILVER',  name: 'VANGUARD',    sub: 'The line that holds. Chasing the Apex',
      icon: 'crenulated-shield',  size: 10, rank: 'vanguard' },
    { key: 'BRONZE',  name: 'FORGE',       sub: 'Where a fighter is hammered into shape',
      icon: 'anvil-impact',       size: 15, rank: 'forge' },
    { key: 'STONE',   name: 'COMMONER',    sub: 'Everyone starts here. Nobody stays',
      icon: 'triple-gate',        size: 6,  rank: 'commoner' }
  ];
  /* 2 + 3 + 10 + 15 + 6 = 36, and 36 is even on purpose: the Monday rivalry
     pairs the table off two by two, and an odd league always leaves the last
     person without an opponent. */
  /* Below this many people a division is just the table with headings in it. */
  var TIER_MINIMUM = 10;

  /* The rank ladder, all the way to 50,000 lifetime points. Named grades with
     numerals inside them, so there is always a next step close enough to want.
     `tier` is the banner colour a grade flies on the board. */
  var MILESTONES = (function () {
    var GRADES = [
      ['SPARK',     'i',   [25]],
      ['ROOKIE',    'i',   [50]],
      ['REGULAR',   'ii',  [100]],
      ['GRINDER',   'ii',  [200, 350, 550]],
      ['MACHINE',   'iii', [800, 1100, 1500]],
      ['BEAST',     'iii', [2000, 2600, 3300]],
      ['WARLORD',   'iv',  [4200, 5300, 6600]],
      ['TITAN',     'iv',  [8200, 10000, 12500]],
      ['IMMORTAL',  'v',   [15500, 19000, 23000]],
      ['ASCENDANT', 'v',   [28000, 34000, 41000]],
      ['ETERNAL',   'vi',  [50000]]
    ];
    var ROMAN = ['I', 'II', 'III'], out = [];
    GRADES.forEach(function (g) {
      g[2].forEach(function (at, i) {
        out.push({
          at: at, tier: g[1], grade: g[0],
          name: g[2].length > 1 ? g[0] + ' ' + ROMAN[i] : g[0]
        });
      });
    });
    return out;
  })();

  /* ------------------------------------------------------------------
     Avatars and badges.

     Everything visual is one monochrome game-icons path drawn with
     currentColor, so the SAME drawing renders gold, silver or bronze just by
     changing CSS colour — a division tint costs nothing and no image is
     fetched. Emoji picked before the icons existed still render, so nobody
     loses the animal they chose.
     ------------------------------------------------------------------ */
  var ICON_PREFIX = 'gi:';

  /* Emblem tints. CLEAR drops the disc entirely and leaves the bare mark,
     which is the closest thing to transparent a small round avatar can be. */
  var AV_COLORS = [
    { key: 'clear',   name: 'CLEAR',   css: 'var(--txt)' },
    { key: 'crimson', name: 'CRIMSON', css: '#ff2e2e' },
    { key: 'ember',   name: 'EMBER',   css: '#ff6a1f' },
    { key: 'gold',    name: 'GOLD',    css: '#ffc93c' },
    { key: 'jade',    name: 'JADE',    css: '#26d07c' },
    { key: 'azure',   name: 'AZURE',   css: '#3ba6ff' },
    { key: 'violet',  name: 'VIOLET',  css: '#a86bff' },
    { key: 'steel',   name: 'STEEL',   css: '#c9d3e2' },
    { key: 'bone',    name: 'BONE',    css: '#e8e2d4' },
    { key: 'toxic',   name: 'TOXIC',   css: '#b6ff2e' },
    { key: 'rose',    name: 'ROSE',    css: '#ff5c9d' },
    { key: 'cyan',    name: 'CYAN',    css: '#22e0e0' },
    { key: 'copper',  name: 'COPPER',  css: '#d08442' },
    { key: 'ink',     name: 'INK',     css: '#8792a8' }
  ];

  /* Text colours for a name. Deliberately a short, high-contrast list: this
     exists so a banner never swallows the name, not to be a paint box. */
  var NAME_COLORS = [
    { key: '',        name: 'DEFAULT', css: 'var(--txt)' },
    { key: 'white',   name: 'WHITE',   css: '#ffffff' },
    { key: 'black',   name: 'BLACK',   css: '#101216' },
    { key: 'gold',    name: 'GOLD',    css: '#ffc93c' },
    { key: 'crimson', name: 'CRIMSON', css: '#ff5555' },
    { key: 'jade',    name: 'JADE',    css: '#3ce68f' },
    { key: 'azure',   name: 'AZURE',   css: '#5cb8ff' },
    { key: 'violet',  name: 'VIOLET',  css: '#c08cff' },
    { key: 'toxic',   name: 'TOXIC',   css: '#c8ff4a' }
  ];
  function nameColor(key) {
    for (var i = 0; i < NAME_COLORS.length; i++) {
      if (NAME_COLORS[i].key === key) return NAME_COLORS[i].css;
    }
    return null;
  }
  function avColor(key) {
    for (var i = 0; i < AV_COLORS.length; i++) if (AV_COLORS[i].key === key) return AV_COLORS[i];
    return null;
  }

  /* An avatar is one mark, never two: either an emblem or an animal.
       "gi:wolf-head|c=crimson"   an emblem, tinted
       "🦍"                       an animal
     The older "|p=" pinned-emoji form still parses so nothing breaks, but the
     pin is no longer offered and no longer drawn. */
  function parseAvatar(spec) {
    var out = { icon: null, color: null, emoji: null };
    if (!spec) return out;
    if (spec.indexOf(ICON_PREFIX) !== 0) { out.emoji = spec; return out; }
    var parts = String(spec).split('|');
    out.icon = parts[0].slice(ICON_PREFIX.length);
    parts.slice(1).forEach(function (bit) {
      if (bit.indexOf('c=') === 0) out.color = bit.slice(2);
      if (bit.indexOf('p=') === 0) out.emoji = bit.slice(2);
    });
    return out;
  }
  function buildAvatar(a) {
    if (!a.icon) return a.emoji || '';
    return ICON_PREFIX + a.icon + (a.color ? '|c=' + a.color : '');
  }

  function iconSvg(name, cls) {
    var body = (typeof GI_ICONS !== 'undefined') && GI_ICONS[name];
    if (!body) return '';
    return '<svg class="' + (cls || 'gi') + '" viewBox="0 0 512 512" ' +
           'aria-hidden="true" focusable="false">' + body + '</svg>';
  }

  /* One place decides how a fighter is drawn — rows, duels, bounties, hall.
     Swapping the art later is this function, not a hunt through the file. */
  function avatarHtml(spec, opts) {
    opts = opts || {};
    var a = parseAvatar(spec);
    var cls = 'av';
    if (opts.size) cls += ' av-' + opts.size;
    /* A chosen tint wins; otherwise the division metal shows through. */
    var c = a.color && avColor(a.color);
    if (c) cls += ' av-tint' + (a.color === 'clear' ? ' av-clear' : '');
    else if (opts.tier) cls += ' av-' + String(opts.tier).toLowerCase();
    var style = c ? ' style="color:' + c.css + '"' : '';

    var inner;
    if (a.icon) {
      inner = iconSvg(a.icon) || '<span class="av-fb">?</span>';
    } else if (a.emoji) {
      inner = '<span class="av-emoji">' + esc(a.emoji) + '</span>';
    } else {
      inner = '<span class="av-fb">' + esc((opts.name || '?').slice(0, 1)) + '</span>';
    }
    /* Emblem and animal together: the emoji rides as a small pin on the rim. */
    return '<span class="' + cls + '"' + style + '>' + inner + '</span>';
  }

  /* Plain-text avatar for the few places that are one line of text. */
  function avatarText(spec) {
    return spec && spec.indexOf(ICON_PREFIX) !== 0 ? spec + ' ' : '';
  }

  var BADGE_SHAPES = ['heater', 'spade', 'round', 'pointed', 'banner', 'hex', 'kite', 'blade'];
  var BADGE_COLORS = [
    { key: 'crimson', a: '#ff2e2e', b: '#7a0d0d' }, { key: 'gold',   a: '#ffc93c', b: '#8a5a00' },
    { key: 'steel',   a: '#c9d3e2', b: '#4a5364' }, { key: 'jade',   a: '#26d07c', b: '#0b5c37' },
    { key: 'azure',   a: '#3ba6ff', b: '#0d3f7a' }, { key: 'violet', a: '#a86bff', b: '#3d1a7a' },
    { key: 'ember',   a: '#ff6a1f', b: '#7a2c05' }, { key: 'bone',   a: '#e8e2d4', b: '#5a5346' }
  ];
  var SHAPE_PATH = {
    heater:  'M8 6h84v40c0 26-19 42-42 52C27 88 8 72 8 46Z',
    spade:   'M8 6h84v52L50 98 8 58Z',
    round:   'M50 4a46 46 0 1 1 0 92a46 46 0 1 1 0-92Z',
    pointed: 'M50 3l42 15v34c0 24-18 38-42 46C26 90 8 76 8 52V18Z',
    banner:  'M12 4h76v78L50 96 12 82Z',
    hex:     'M50 3l40 23v48L50 97 10 74V26Z',
    kite:    'M50 2l44 34-44 62L6 36Z',
    blade:   'M14 6h72l-8 46c-3 20-14 32-28 46-14-14-25-26-28-46Z'
  };

  function badgeOf(league) {
    var b = (league && league.badge) || {};
    var seed = 0, id = (league && (league.id || league.name)) || '';
    for (var i = 0; i < id.length; i++) seed = (seed * 31 + id.charCodeAt(i)) >>> 0;
    /* Unsigned shifts: the hash is a full 32-bit value, and a signed >> turns
       anything past 2^31 negative, which indexes off the front of the array. */
    return {
      shape:  b.shape  || BADGE_SHAPES[seed % BADGE_SHAPES.length],
      color:  b.color  || BADGE_COLORS[(seed >>> 3) % BADGE_COLORS.length].key,
      emblem: b.emblem || GI_CRESTS[(seed >>> 6) % GI_CRESTS.length]
    };
  }

  /* Shield + emblem, composed at render time. Eight shapes x eight colours x
     56 emblems is ~3,500 distinct badges from a handful of paths. */
  function leagueBadgeHtml(league, size) {
    return badgeHtml(badgeOf(league), size);
  }

  /* The same drawing from a plain {shape,color,emblem}, so the shop can show
     a choice before it is made. */
  function badgeHtml(b, size) {
    var c = BADGE_COLORS.filter(function (x) { return x.key === b.color; })[0] || BADGE_COLORS[0];
    var gid = 'bg' + b.color;
    return '<span class="lbadge lbadge-' + (size || 'md') + '">' +
      '<svg viewBox="0 0 100 100" aria-hidden="true" focusable="false">' +
        '<defs><linearGradient id="' + gid + '" x1="0" y1="0" x2="0" y2="1">' +
          '<stop offset="0" stop-color="' + c.a + '"/>' +
          '<stop offset="1" stop-color="' + c.b + '"/></linearGradient></defs>' +
        '<path d="' + SHAPE_PATH[b.shape] + '" fill="url(#' + gid + ')" ' +
          'stroke="rgba(0,0,0,.45)" stroke-width="3"/>' +
        '<path d="' + SHAPE_PATH[b.shape] + '" fill="none" ' +
          'stroke="rgba(255,255,255,.35)" stroke-width="1.5"/>' +
      '</svg>' +
      '<span class="lbadge-em">' + iconSvg(b.emblem) + '</span>' +
    '</span>';
  }

  /* A fighter's banner is their lifetime rank — derived from points they
     already have, so there is nothing to store and nothing to award. */
  /* The raid, bounty and combo used to push the table below the fold. They
     fold into one strip now, and the strip says enough when shut that you only
     open it if something needs doing. */
  function paintWeekChips() {
    var c = [];
    if (state.chip.raid)   c.push(state.chip.raid);
    if (state.chip.bounty) c.push(state.chip.bounty);
    if (state.chip.combo)  c.push(state.chip.combo);
    var box = $('#weekChips'); if (!box) return;
    var wb = $('#weekBox');
    wb.hidden = !c.length;
    /* Open itself when today actually needs something — a live bounty you have
       not done. Otherwise it stays folded and the table starts at the top. */
    if (!state.weekBoxTouched && state.chip.bounty && state.chip.bounty.urgent) wb.open = true;
    box.innerHTML = c.map(function (x) {
      return '<span class="chip' + (x.on ? ' on' : '') + '">' + x.t + '</span>';
    }).join('');
  }

  /* A raid is the one job the whole league carries together for a week. The
     target scales with headcount, so a group of 8 and a group of 28 both get
     something that needs everybody rather than one strong person. */
  async function loadRaid() {
    var box = $('#raidCard'); if (!box) return;
    var r = await sb.rpc('current_raid', { p_league: state.leagueId });
    var d = r.error ? null : (r.data && r.data[0]);
    if (!d) { box.hidden = true; state.chip.raid = null; paintWeekChips(); return; }
    var pct = Math.min(100, Number(d.progress) / Number(d.target) * 100);
    var left = Math.max(0, Number(d.target) - Number(d.progress));
    box.hidden = false;
    box.className = 'raid' + (d.done ? ' done' : '');
    box.innerHTML =
      '<div class="raid-h"><span class="raid-t">LEAGUE RAID</span>' +
        '<span class="raid-n">' + esc(d.name) + '</span>' +
        (d.done ? '<span class="raid-b">CLEARED</span>' : '') + '</div>' +
      '<p class="raid-d">' + esc(d.descr) + ' — together, before Sunday.</p>' +
      '<div class="raid-bar"><span style="width:' + pct.toFixed(1) + '%"></span></div>' +
      '<div class="raid-f">' +
        '<span><b>' + num(d.progress) + '</b> / ' + num(d.target) + ' ' + esc(d.unit) + '</span>' +
        '<span>' + (d.done
          ? 'The whole league scores'
          : num(left) + ' to go · ' + d.members + ' of you') + '</span>' +
      '</div>' +
      (d.top_name ? '<p class="raid-top">Carrying it: <b>' + esc(d.top_name) +
        '</b> with ' + num(d.top_amount) + '</p>' : '');
    state.chip.raid = { on: d.done,
      t: 'RAID ' + Math.round(Number(d.progress) / Number(d.target) * 100) + '%' };
    paintWeekChips();
  }

  /* Badges. Earned in a league, derived from what already happened, and worn
     three at a time by choice — the board stays readable and the choice is
     itself a small statement. */
  function badgeArt(key) { return (GI_BADGE_ART && GI_BADGE_ART[key]) || 'medal'; }

  async function loadBadges() {
    var box = $('#badges'); if (!box) return;
    var r = await sb.rpc('my_badges', { p_league: state.leagueId });
    if (r.error || !r.data) { box.innerHTML = ''; return; }
    state.badges = r.data;
    var pinned = (state.profile && state.profile.pinned_badges) || [];
    var got = r.data.filter(function (b) { return b.earned; }).length;
    $('#badgeCount').textContent = got + ' / ' + r.data.length;
    box.innerHTML = r.data.map(function (b) {
      var on = pinned.indexOf(b.key) >= 0;
      var pct = Math.min(100, Number(b.progress) / Number(b.target) * 100);
      return '<button type="button" class="bdg' + (b.earned ? ' got' : '') +
        (on ? ' pinned' : '') + '" data-badge="' + b.key + '"' +
        (b.earned ? '' : ' disabled') + '>' +
        '<span class="bdg-i">' + iconSvg(badgeArt(b.key)) + '</span>' +
        '<span class="bdg-n">' + esc(b.name) + '</span>' +
        '<span class="bdg-d">' + esc(b.descr) + '</span>' +
        (b.earned ? '<span class="bdg-on">' + (on ? 'ON SHOW' : 'TAP TO WEAR') + '</span>'
          : '<span class="bdg-bar"><i style="width:' + pct.toFixed(0) + '%"></i></span>' +
            '<span class="bdg-p">' + num(b.progress) + ' / ' + num(b.target) + '</span>') +
      '</button>';
    }).join('');
  }


  /* Rivalries. Nobody at rank 12 is chasing rank 1, but they very much do not
     want to lose to rank 13. Pairings come from last week's finish so they hold
     still all week; the score is this week's live points. */
  function rivalCard(r, big) {
    var meId = state.profile ? state.profile.id : null;
    var iAmA = r.a_id === meId;
    var meSide  = iAmA ? { n: r.a_name, av: r.a_avatar, p: Number(r.a_points) }
                       : { n: r.b_name, av: r.b_avatar, p: Number(r.b_points) };
    var foeSide = iAmA ? { n: r.b_name, av: r.b_avatar, p: Number(r.b_points) }
                       : { n: r.a_name, av: r.a_avatar, p: Number(r.a_points) };
    var L = big ? meSide : { n: r.a_name, av: r.a_avatar, p: Number(r.a_points) };
    var R = big ? foeSide : { n: r.b_name, av: r.b_avatar, p: Number(r.b_points) };
    var gap = L.p - R.p;
    var cls = 'riv' + (big ? ' riv-big' : '') + (r.mine && !big ? ' mine' : '');
    return '<div class="' + cls + '">' +
      '<span class="riv-s' + (gap > 0 ? ' up' : '') + '">' +
        avatarHtml(L.av, { name: L.n, size: big ? 'lg' : 'sm' }) +
        '<b>' + esc(L.n) + '</b><i>' + num(L.p) + '</i></span>' +
      '<span class="riv-v">' + (big
        ? (gap === 0 ? 'LEVEL' : (gap > 0 ? '+' + num(gap) : num(gap)))
        : 'vs') + '</span>' +
      '<span class="riv-s' + (gap < 0 ? ' up' : '') + '">' +
        avatarHtml(R.av, { name: R.n, size: big ? 'lg' : 'sm' }) +
        '<b>' + esc(R.n) + '</b><i>' + num(R.p) + '</i></span>' +
    '</div>';
  }

  async function loadRivalries() {
    var mine = $('#myRival'), all = $('#rivalList');
    if (!mine) return;
    var r = await sb.rpc('league_rivalries', { p_league: state.leagueId });
    if (r.error || !r.data || !r.data.length) {
      mine.innerHTML = '<div class="empty">Pairings start once the league has ' +
        'a full week behind it.</div>';
      all.innerHTML = '';
      return;
    }
    var m = r.data.filter(function (x) { return x.mine; })[0];
    mine.innerHTML = m ? rivalCard(m, true)
      : '<div class="empty">You are the odd one out this week — no rival drawn.</div>';
    all.innerHTML = r.data.map(function (x) { return rivalCard(x, false); }).join('');
  }

  /* Consistency, not volume: the one table a beginner can win. */
  async function loadStreaks() {
    var box = $('#streaks'); if (!box) return;
    var r = await sb.rpc('league_streaks', { p_league: state.leagueId });
    if (r.error || !r.data) { box.innerHTML = ''; return; }
    var rows = r.data.filter(function (x) { return x.best_streak > 0; });
    var me = state.profile ? state.profile.id : null;
    /* The summary is what you see with the section shut, so it carries your
       own run rather than the league's — the leader's streak is not news
       every time you open the tab. */
    var mine = rows.filter(function (x) { return x.profile_id === me; })[0];
    $('#streakCount').textContent = !mine ? 'NO STREAK YET'
      : mine.current_streak > 0 ? 'YOU ' + mine.current_streak + ' DAYS'
      : 'YOUR BEST ' + mine.best_streak;
    if (!rows.length) {
      box.innerHTML = '<div class="empty">No streak going yet. Score 20 points two days running to start one.</div>';
      return;
    }
    box.innerHTML = rows.slice(0, 10).map(function (x, i) {
      var live = x.current_streak > 0;
      return '<div class="stk' + (x.profile_id === me ? ' me' : '') +
               (skinClass(x.banner) ? ' skin ' + skinClass(x.banner) : '') + '">' +
        '<span class="stk-r">' + (i + 1) + '</span>' +
        avatarHtml(x.avatar, { name: x.display_name, size: 'sm' }) +
        '<span class="stk-n">' + esc(x.display_name) + '</span>' +
        '<span class="stk-d' + (live ? ' live' : '') + '">' +
          (live ? x.current_streak : x.best_streak) +
          '<small>' + (live ? 'DAYS' : 'BEST') + '</small></span>' +
      '</div>';
    }).join('');
  }

  function bannerOf(lifetime) {
    /* Below the first grade you are UNRANKED rather than nothing — a row with
       no edge at all looks like a rendering fault next to rows that have one. */
    var got = { at: 0, tier: 'o', grade: 'UNRANKED', name: 'UNRANKED' };
    for (var i = 0; i < MILESTONES.length; i++) {
      if (Number(lifetime) >= MILESTONES[i].at) got = MILESTONES[i];
    }
    return got;
  }

  var UNITS = {
    reps:    { label: 'REPS',       short: 'reps', step: 1,   def: 10, quick: [5, 10, 20, 50] },
    seconds: { label: 'SECONDS',    short: 'sec',  step: 5,   def: 30, quick: [15, 30, 60, 120] },
    minutes: { label: 'MINUTES',    short: 'min',  step: 1,   def: 10, quick: [5, 10, 20, 30] },
    km:      { label: 'KILOMETRES', short: 'km',   step: 0.5, def: 5,  quick: [1, 3, 5, 10] },
    flat:    { label: 'SESSIONS',   short: 'sessions', step: 1, def: 1, quick: [1, 2, 3] }
  };

  function exercise(key) {
    for (var i = 0; i < EXERCISES.length; i++) if (EXERCISES[i].key === key) return EXERCISES[i];
    return null;
  }
  function modeOf(key, mode) {
    var ex = exercise(key); if (!ex) return null;
    for (var i = 0; i < ex.modes.length; i++) if (ex.modes[i].mode === mode) return ex.modes[i];
    return null;
  }
  /* Must agree with calc_points() in SQL to the last decimal — this only
     draws the preview, the server is what actually scores. */
  function pointsFor(key, mode, amount, bw, load) {
    var m = modeOf(key, mode); if (!m) return 0;
    if (m.gym) {
      bw = Number(bw); load = Number(load);
      if (!(bw >= 30 && bw <= 250) || !(load >= 0 && load <= 500)) return 0;
      var r = (load * m.equip + (m.legs ? 0.85 * bw : 0)) / bw;
      return Math.round(amount * m.k * r * 100) / 100;
    }
    return Math.round(amount * m.rate * 100) / 100;
  }

  /* ===================================================================
     2. Small helpers
     =================================================================== */
  var $ = function (s) { return document.querySelector(s); };
  function esc(s) {
    return String(s == null ? '' : s).replace(/[&<>"']/g, function (c) {
      return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c];
    });
  }
  function num(n) {
    n = Math.round(Number(n) * 100) / 100;
    return (n % 1 === 0) ? String(n) : String(Math.round(n * 10) / 10);
  }
  var toastTimer;
  function toast(msg, bad) {
    var t = $('#toast');
    t.textContent = msg;
    t.className = 'toast' + (bad ? ' bad' : '');
    t.hidden = false;
    clearTimeout(toastTimer);
    toastTimer = setTimeout(function () { t.hidden = true; }, bad ? 4200 : 2400);
  }
  var FRIENDLY = {
    LEAGUE_FULL: 'That league is full — 30 fighters maximum.',
    NO_SUCH_LEAGUE: 'No league found with that code. Check the 6 characters.',
    BAD_CODE: 'That restore code does not match any profile.',
    DEVICE_HAS_DATA: 'This phone already has a profile with workouts on it.',
    NO_PROFILE: 'Pick your name first.',
    TOO_MANY_LEAGUES: 'You already own 10 leagues.',
    NOT_SIGNED_IN: 'Connection lost — reload the page.',
    WEEK_CLOSED: 'That week is over — its entries are locked.',
    ALREADY_IN_CHALLENGE: 'You already have a duel open. Finish or cancel it first.',
    OWN_CHALLENGE: 'That is your own duel code.',
    NO_SUCH_CHALLENGE: 'No duel found with that code.',
    CHALLENGE_UNAVAILABLE: 'That duel is no longer open.',
    NOT_A_MEMBER: 'That duel belongs to a league you are not in.',
    REST_DAY: 'Sunday is a rest day — a stretching session is the only thing that counts.',
    REST_DAY_DONE: 'You already logged your recovery today. Rest.'
  };
  function niceError(e) {
    if (!e) return 'Something went wrong.';
    var m = e.message || e.error_description || String(e);
    for (var k in FRIENDLY) if (m.indexOf(k) !== -1) return FRIENDLY[k];
    if (/display_name_check|char_length/.test(m)) return 'Names must be 2 to 18 characters.';
    if (/Anonymous sign-ins are disabled/i.test(m)) {
      return 'Anonymous sign-ins are still OFF in Supabase (Authentication → Sign In / Providers).';
    }
    if (/Failed to fetch|NetworkError|load failed/i.test(m)) return 'No connection. Try again.';
    return m;
  }

  /* ===================================================================
     3. Weeks and the Sunday 23:59 deadline (timezone aware)
     =================================================================== */
  function tzOffsetMs(date, tz) {
    var dtf = new Intl.DateTimeFormat('en-US', {
      timeZone: tz, hourCycle: 'h23', year: 'numeric', month: '2-digit', day: '2-digit',
      hour: '2-digit', minute: '2-digit', second: '2-digit'
    });
    var p = {}, parts = dtf.formatToParts(date), i;
    for (i = 0; i < parts.length; i++) p[parts[i].type] = parts[i].value;
    var asUTC = Date.UTC(+p.year, +p.month - 1, +p.day, +p.hour, +p.minute, +p.second);
    return asUTC - Math.floor(date.getTime() / 1000) * 1000;
  }
  /* "Wall clock" date: a Date whose UTC fields hold the local time in TZ. */
  function wallNow() { var n = new Date(); return new Date(n.getTime() + tzOffsetMs(n, TZ)); }
  function wallToUtcMs(wallMs) {
    var g = wallMs - tzOffsetMs(new Date(wallMs), TZ);
    return wallMs - tzOffsetMs(new Date(g), TZ);
  }
  function iso(d) {
    return d.getUTCFullYear() + '-' +
      String(d.getUTCMonth() + 1).padStart(2, '0') + '-' +
      String(d.getUTCDate()).padStart(2, '0');
  }
  /* Monday of the current week, as YYYY-MM-DD (matches Postgres date_trunc). */
  function currentWeekStart() {
    var w = wallNow();
    var back = (w.getUTCDay() + 6) % 7;
    return iso(new Date(Date.UTC(w.getUTCFullYear(), w.getUTCMonth(), w.getUTCDate() - back)));
  }
  /* Sunday is a rest day: the league is closed and only recovery counts. */
  function isRestDay() { return wallNow().getUTCDay() === 0; }

  /* Instant of Saturday 23:59:59.999 — when the competition stops. */
  function competitionEndMs(weekStartIso) {
    var p = weekStartIso.split('-');
    return wallToUtcMs(Date.UTC(+p[0], +p[1] - 1, +p[2] + 5, 23, 59, 59, 999));
  }

  /* Instant of Sunday 23:59:59.999 that closes the given week. */
  function weekDeadlineMs(weekStartIso) {
    var p = weekStartIso.split('-');
    var wallEnd = Date.UTC(+p[0], +p[1] - 1, +p[2] + 6, 23, 59, 59, 999);
    return wallToUtcMs(wallEnd);
  }
  function weekRangeLabel(weekStartIso) {
    var p = weekStartIso.split('-');
    var a = new Date(Date.UTC(+p[0], +p[1] - 1, +p[2]));
    var b = new Date(Date.UTC(+p[0], +p[1] - 1, +p[2] + 6));
    var f = function (d) {
      return new Intl.DateTimeFormat('en-GB', {
        day: 'numeric', month: 'short', timeZone: 'UTC'
      }).format(d).toUpperCase();
    };
    return f(a) + ' – ' + f(b);
  }

  /* ===================================================================
     4. State
     =================================================================== */
  var sb = null;
  var state = {
    profile: null,
    leagues: [],
    leagueId: null,
    week: currentWeekStart(),
    board: [],
    history: [],
    open: {},          // profile ids whose activity feed is expanded
    openWeeks: {},
    feeds: {},
    feedDay: {},          // which day groups a viewer has opened
    chip: {},             // one-line summaries for the folded week strip
    weekBoxTouched: false,// once you open or shut it yourself, we stop deciding
    session: [],       // items logged inside the currently open modal
    pendingCode: null,
    view: 'live',
    statsRange: 'week',
    wins: [],
    champions: [],
    raceTo: 5,            // the target a "first to N" run is counted against
    muscles: [],
    bodyView: 'front',
    bodyPick: null,       // the region a thumb is currently holding
    duels: [],
    restDone: false
  };
  var LS = {
    league: 'ironleague.league',
    race: 'ironleague.raceto'
  };
  try {
    var savedRace = Number(localStorage.getItem(LS.race));
    if (savedRace === 3 || savedRace === 5 || savedRace === 10) state.raceTo = savedRace;
  } catch (e) {}

  /* ===================================================================
     5. Boot
     =================================================================== */
  function configured() {
    return CFG.SUPABASE_URL && CFG.SUPABASE_ANON_KEY &&
      CFG.SUPABASE_URL.indexOf('PASTE') === -1 &&
      CFG.SUPABASE_ANON_KEY.indexOf('PASTE') === -1 &&
      /^https?:\/\//.test(CFG.SUPABASE_URL);
  }
  function show(id) {
    ['#boot', '#setup', '#onboard', '#app'].forEach(function (s) {
      $(s).hidden = (s !== id);
    });
  }

  function readInviteCode() {
    var h = location.hash || '';
    var m = h.match(/join\/([A-Za-z0-9]{4,10})/) || h.match(/join=([A-Za-z0-9]{4,10})/);
    if (!m) {
      var q = new URLSearchParams(location.search);
      if (q.get('join')) m = [null, q.get('join')];
    }
    if (m) {
      try { sessionStorage.setItem('ironleague.invite', m[1].toUpperCase()); } catch (e) {}
      history.replaceState(null, '', location.pathname + location.search);
      return m[1].toUpperCase();
    }
    try { return sessionStorage.getItem('ironleague.invite'); } catch (e) { return null; }
  }

  async function boot() {
    if (CFG.APP_NAME) { $('#brandName').textContent = CFG.APP_NAME; }
    $('#version').textContent = 'IRON LEAGUE ' + APP_VERSION + ' · ' + TZ;

    if (!configured()) { show('#setup'); return; }
    if (typeof supabase === 'undefined') {
      show('#setup');
      $('#setupErr').textContent = 'Could not load vendor/supabase.js — is the whole folder uploaded?';
      return;
    }

    sb = supabase.createClient(CFG.SUPABASE_URL, CFG.SUPABASE_ANON_KEY, {
      auth: { persistSession: true, autoRefreshToken: true, storageKey: 'ironleague.auth' }
    });

    state.pendingCode = readInviteCode() || (CFG.DEFAULT_LEAGUE_CODE || '').toUpperCase() || null;

    try {
      var got = await sb.auth.getSession();
      if (!got.data.session) {
        var anon = await sb.auth.signInAnonymously();
        if (anon.error) throw anon.error;
      }
    } catch (e) {
      failSetup(e);
      return;
    }

    try { await loadProfile(); } catch (e) { toast(niceError(e), true); }

    if (!state.profile) { startOnboarding(); return; }
    await enterApp();
  }

  /* The setup screen doubles as the "cannot reach the server" screen. */
  function failSetup(e) {
    var msg = niceError(e);
    show('#setup');
    if (/connection|reload the page/i.test(msg)) {
      $('#setupTitle').innerHTML = 'NO<br><span class="accent">SIGNAL</span>';
      $('#setupIntro').textContent = 'Iron League could not reach the server.';
      $('#setupSteps').hidden = true;
      $('#setupHelp').hidden = true;
      $('#retryBtn').hidden = false;
    }
    $('#setupErr').textContent = msg;
  }
  $('#retryBtn').addEventListener('click', function () { location.reload(); });

  async function loadProfile() {
    var u = await sb.auth.getUser();
    if (!u.data || !u.data.user) return;
    var r = await sb.from('profiles').select('*').eq('user_id', u.data.user.id).maybeSingle();
    if (r.error) throw r.error;
    state.profile = r.data;
  }

  /* ===================================================================
     6. Onboarding
     =================================================================== */
  /* 'invite'  = arrived through a friend's link, league already known
     'join'    = default: type the code you were given
     'create'  = deliberately starting a new group                        */
  var onboardMode = 'join';

  function setOnboardMode(m) {
    onboardMode = m;
    $('#joinWrap').hidden      = (m !== 'join');
    $('#newLeagueWrap').hidden = (m !== 'create');
    $('#joinPreview').hidden   = (m !== 'invite');
    $('#modeToggle').hidden    = (m === 'invite');
    $('#modeToggle').textContent = (m === 'create')
      ? 'Actually, I have a league code'
      : 'No code? Start a brand new league';
    $('#enterBtn').textContent = (m === 'create') ? 'CREATE MY LEAGUE' : 'ENTER THE ARENA';
  }

  async function startOnboarding() {
    show('#onboard');
    var code = state.pendingCode;
    if (code) {
      var pv = await sb.rpc('league_preview', { p_code: code });
      var l = pv.data && pv.data[0];
      if (l) {
        $('#joinPreview').innerHTML =
          '<div class="jc-k">YOU ARE JOINING</div>' +
          '<div class="jc-n">' + esc(l.name) + '</div>' +
          '<div class="muted small mono">' + l.members + '/' + l.max_members +
          ' FIGHTERS · CODE ' + esc(l.code) + '</div>' +
          (l.members >= l.max_members
            ? '<div class="err small">This league is full.</div>' : '');
        setOnboardMode('invite');
        $('#nameInput').focus();
        return;
      }
      state.pendingCode = null;
      try { sessionStorage.removeItem('ironleague.invite'); } catch (e) {}
    }
    setOnboardMode('join');
    $('#nameInput').focus();
  }

  $('#modeToggle').addEventListener('click', function () {
    setOnboardMode(onboardMode === 'create' ? 'join' : 'create');
    $('#onboardErr').textContent = '';
  });

  /* Check the code as it is typed, so nobody discovers a typo after signing up. */
  var codeCheckTimer;
  $('#joinCode').addEventListener('input', function () {
    var v = this.value.trim().toUpperCase();
    this.value = v;
    var hint = $('#codeHint');
    clearTimeout(codeCheckTimer);
    hint.style.color = '';
    if (v.length < 6) {
      hint.className = 'muted small';
      hint.textContent = 'The 6 characters your friend sent you.';
      return;
    }
    hint.className = 'muted small';
    hint.textContent = 'Checking…';
    codeCheckTimer = setTimeout(async function () {
      try {
        var pv = await sb.rpc('league_preview', { p_code: v });
        var l = pv.data && pv.data[0];
        if (!l) {
          hint.className = 'err small';
          hint.textContent = 'No league with that code.';
        } else if (l.members >= l.max_members) {
          hint.className = 'err small';
          hint.textContent = l.name + ' is full (' + l.members + '/' + l.max_members + ').';
        } else {
          hint.className = 'small';
          hint.style.color = 'var(--green)';
          hint.textContent = '✓ ' + l.name + ' — ' + l.members + '/' + l.max_members + ' fighters';
        }
      } catch (e) { /* leave the hint as it is */ }
    }, 350);
  });

  $('#restoreToggle').addEventListener('click', function () {
    var b = $('#restoreBox'); b.hidden = !b.hidden;
    if (!b.hidden) $('#restoreInput').focus();
  });

  $('#enterForm').addEventListener('submit', async function (ev) {
    ev.preventDefault();
    var btn = $('#enterBtn');
    var name = $('#nameInput').value.trim();
    $('#onboardErr').textContent = '';
    if (name.length < 2) { $('#onboardErr').textContent = 'Name must be at least 2 characters.'; return; }
    btn.disabled = true; btn.textContent = 'ENTERING…';
    try {
      var p = await sb.rpc('create_profile', { p_name: name });
      if (p.error) throw p.error;
      state.profile = p.data;

      if (onboardMode === 'create') {
        var lname = ($('#firstLeague').value || '').trim();
        if (lname.length < 2) throw new Error('Give your new league a name.');
        var c = await sb.rpc('create_league', { p_name: lname.slice(0, 28) });
        if (c.error) throw c.error;
        state.leagueId = c.data.id;
      } else {
        var code = state.pendingCode || $('#joinCode').value.trim().toUpperCase();
        if (!code) {
          throw new Error('Enter the league code your friend sent you, ' +
                          'or tap "Start a brand new league" below.');
        }
        var j = await sb.rpc('join_league_by_code', { p_code: code });
        if (j.error) throw j.error;
        state.leagueId = j.data.id;
      }
      try { sessionStorage.removeItem('ironleague.invite'); } catch (e) {}
      state.pendingCode = null;
      localStorage.setItem(LS.league, state.leagueId);
      await enterApp();
    } catch (e) {
      $('#onboardErr').textContent = niceError(e);
    } finally {
      btn.disabled = false;
      btn.textContent = (onboardMode === 'create') ? 'CREATE MY LEAGUE' : 'ENTER THE ARENA';
    }
  });

  $('#restoreBtn').addEventListener('click', async function () {
    var code = $('#restoreInput').value.trim();
    $('#onboardErr').textContent = '';
    if (!code) return;
    try {
      var r = await sb.rpc('restore_profile', { p_code: code });
      if (r.error) throw r.error;
      state.profile = r.data;
      toast('Welcome back, ' + r.data.display_name);
      await enterApp();
    } catch (e) {
      $('#onboardErr').textContent = niceError(e);
    }
  });

  /* ===================================================================
     7. Entering the app
     =================================================================== */
  async function enterApp() {
    show('#app');
    try {
      var w = await sb.rpc('current_week_start');
      if (!w.error && w.data) state.week = w.data;
    } catch (e) { /* keep the locally computed week */ }

    await loadLeagues();

    // An invite arriving while already signed in: join it now.
    if (state.pendingCode) {
      var known = state.leagues.filter(function (l) { return l.code === state.pendingCode; })[0];
      if (known) { state.leagueId = known.id; }
      else {
        try {
          var j = await sb.rpc('join_league_by_code', { p_code: state.pendingCode });
          if (j.error) throw j.error;
          state.leagueId = j.data.id;
          toast('Joined ' + j.data.name);
          await loadLeagues();
        } catch (e) { toast(niceError(e), true); }
      }
      try { sessionStorage.removeItem('ironleague.invite'); } catch (e) {}
      state.pendingCode = null;
      localStorage.setItem(LS.league, state.leagueId);
    }

    buildExerciseSelect();
    buildPointsTable();
    buildComboRules();
    loadRestState();

    if (!state.leagues.length) { renderMe(); switchView('me'); tick(); return; }

    var saved = localStorage.getItem(LS.league);
    var ok = state.leagues.filter(function (l) { return l.id === state.leagueId; })[0] ||
             state.leagues.filter(function (l) { return l.id === saved; })[0];
    state.leagueId = (ok || state.leagues[0]).id;
    localStorage.setItem(LS.league, state.leagueId);

    renderHeader();
    tick();
    await refreshAll();
    startAutoRefresh();
  }

  function league() {
    return state.leagues.filter(function (l) { return l.id === state.leagueId; })[0] || null;
  }

  async function loadLeagues() {
    var r = await sb.rpc('my_leagues');
    if (r.error) { toast(niceError(r.error), true); return; }
    state.leagues = r.data || [];
  }

  function renderHeader() {
    var l = league();
    if (!l) return;
    var lskin = skinClass(l.badge && l.badge.skin);
    $('#leagueBtn').className = 'leaguepill' + (lskin ? ' skin ' + lskin : '');
    $('#lgBadge').innerHTML = leagueBadgeHtml(l, 'sm');
    $('#lgName').textContent = l.name;
    $('#lgName').style.color = nameColor(l.badge && l.badge.text) || '';
    $('#lgMeta').textContent = l.members + '/' + l.max_members + ' · CODE ' + l.code;
    $('#weekLabel').textContent = weekRangeLabel(state.week);
  }

  /* ===================================================================
     8. Countdown to Sunday 23:59
     =================================================================== */
  var lastWeekSeen = state.week;
  function tick() {
    var wk = currentWeekStart();
    var rest = isRestDay();
    var ms = (rest ? weekDeadlineMs(wk) : competitionEndMs(wk)) - Date.now();
    if (ms < 0) ms = 0;
    var s = Math.floor(ms / 1000);
    var d = Math.floor(s / 86400), h = Math.floor(s % 86400 / 3600),
        m = Math.floor(s % 3600 / 60), sec = s % 60;
    var pad = function (n) { return String(n).padStart(2, '0'); };
    var t = $('#cdTimer');
    t.textContent = (d > 0 ? d + 'D ' : '') + pad(h) + ':' + pad(m) + ':' + pad(sec);
    t.classList.toggle('urgent', s < 3600 * 6 && !rest);
    document.querySelector('.cd-label').textContent =
      rest ? 'REST DAY · OPENS IN' : 'LEAGUE CLOSES IN';
    $('#countdown').classList.toggle('resting', rest);

    if (state.view === 'duel') tickDuel();

    if (wk !== lastWeekSeen) {          // a new week just started
      lastWeekSeen = wk;
      state.week = wk;
      state.feeds = {};
      state.open = {};
      rollOver();
      toast('New week. Everyone back to zero.');
    }
  }
  setInterval(tick, 1000);

  async function rollOver() {
    if (!sb || !state.leagueId) return;
    try {
      var w = await sb.rpc('current_week_start');
      if (!w.error && w.data) state.week = w.data;
    } catch (e) { /* keep local value */ }
    refreshAll();
  }

  /* ===================================================================
     9. Live leaderboard
     =================================================================== */
  async function refreshAll() {
    /* allSettled, not all: one widget failing must never blank the table,
       which is the only thing on this screen that really matters. */
    var jobs = [loadBoard(), loadHistory(), loadCombo(), loadBounty(),
                loadStreaks(), loadRaid()];
    var res = await Promise.allSettled(jobs);
    res.forEach(function (x) {
      if (x.status === 'rejected' && window.console) console.error('widget failed', x.reason);
    });
    renderHeader();
  }

  async function loadBoard() {
    if (!state.leagueId) return;
    var r = await sb.rpc('league_leaderboard', { p_league: state.leagueId, p_week: state.week });
    if (r.error) { toast(niceError(r.error), true); return; }
    state.board = r.data || [];
    renderBoard();
  }

  function ranked(rows) {
    var out = [], rank = 0, prev = null;
    for (var i = 0; i < rows.length; i++) {
      var pts = Number(rows[i].points) || 0;
      if (prev === null || pts !== prev) { rank = i + 1; prev = pts; }
      out.push({ row: rows[i], rank: pts > 0 ? rank : null });
    }
    return out;
  }

  /* Divisions are a live cut of the current table: the top ten are Gold,
     the next ten Silver, the rest Bronze. Log something and you climb. */
  function withTiers(rows) {
    if (rows.length < TIER_MINIMUM) return null;     // one division is not a division
    var sorted = rows.slice().sort(function (a, b) {
      var d = Number(b.points) - Number(a.points);
      if (d) return d;
      return new Date(a.joined_at) - new Date(b.joined_at);
    });
    var out = [], i = 0;
    TIERS.forEach(function (t) {
      var members = sorted.slice(i, i + t.size);
      i += t.size;
      if (members.length) out.push({ tier: t, rows: members });
    });
    if (i < sorted.length && out.length) {          // more people than tiers allow for
      out[out.length - 1].rows = out[out.length - 1].rows.concat(sorted.slice(i));
    }
    return out;
  }

  /* `lead` is the top of whichever division this row is in, not the top of
     the league — ranks run straight through now, so it can no longer be
     worked out from the number. Each division stays its own race. */
  function boardRow(p, rank, tierKey, lead) {
    var me = state.profile ? state.profile.id : null;
    var pts = Number(p.points) || 0;
    lead = (lead === undefined ? rank === 1 : lead) && pts > 0;
    var cls = 'row' + (tierKey
        ? (lead ? ' lead-' + tierKey.toLowerCase() : '')
        : (rank && rank <= 3 && pts > 0 ? ' r' + rank : '')) +
      (p.profile_id === me ? ' me' : '');
    var open = !!state.open[p.profile_id];
    var bn = bannerOf(p.lifetime);
    var skin = skinClass(p.banner);
    return '<div class="' + cls + (bn ? ' bnr bnr-' + bn.tier : '') +
           (skin ? ' skin ' + skin : '') +
           '" data-id="' + p.profile_id + '">' +
      '<button class="rowbtn" type="button" data-toggle="' + p.profile_id + '">' +
        '<span class="rank">' + (rank && pts > 0 ? rank : '–') + '</span>' +
        avatarHtml(p.avatar, { name: p.display_name, tier: tierKey }) +
        '<span class="who"><span class="nm"' +
          (nameColor(p.name_color) ? ' style="color:' + nameColor(p.name_color) + '"' : '') +
          '>' + esc(p.display_name) +
          ((p.pinned_badges || []).length
            ? '<span class="wornrow">' + p.pinned_badges.map(function (k) {
                return '<span class="worn">' + iconSvg(badgeArt(k)) + '</span>';
              }).join('') + '</span>' : '') +
          (p.profile_id === me ? ' <span class="muted" style="font-size:11px">(YOU)</span>' : '') +
        '</span>' +
        '<span class="sub">' +
          (bn ? '<b class="rankchip rank-' + bn.tier + '">' + bn.name + '</b> · ' : '') +
          p.entries + (Number(p.entries) === 1 ? ' entry' : ' entries') +
          (Number(p.bonus) > 0 ? ' · <b class="cbadge">+' + num(p.bonus) + ' combo</b>' : '') +
        '</span></span>' +
        '<span class="pts">' + num(pts) + '<small>PTS</small></span>' +
        '<span class="chev">' + (open ? '▲' : '▼') + '</span>' +
      '</button>' +
      (open ? '<div class="feed" data-feed="' + p.profile_id + '">' +
                (state.feeds[p.profile_id] ? feedHtml(state.feeds[p.profile_id]) :
                 '<div class="muted small" style="padding:8px 0">Loading…</div>') +
              '</div>' : '') +
    '</div>';
  }

  function renderBoard() {
    var box = $('#board');
    if (!state.board.length) {
      box.innerHTML = '<div class="empty">Nobody here yet. Share your link!</div>';
      return;
    }
    var groups = withTiers(state.board);
    if (groups) {
      var mine = state.profile ? state.profile.id : null;
      var seen = 0, numbering = null;
      box.innerHTML = groups.map(function (g) {
        var here = g.rows.some(function (p) { return p.profile_id === mine; });
        if (g.tier.rank !== numbering) { seen = 0; numbering = g.tier.rank; }
        return '<div class="divhead t-' + g.tier.key.toLowerCase() +
                 (here ? ' is-mine' : '') + '">' +
                 '<span class="divhead-c">' + iconSvg(g.tier.icon) + '</span>' +
                 '<span class="divhead-t"><b>' + g.tier.name + ' DIVISION</b>' +
                   '<i>' + g.tier.sub + '</i></span>' +
                 '<span class="divhead-n">' + g.rows.length + '</span>' +
               '</div>' +
               g.rows.map(function (p, i) {
                 seen += 1;
                 return boardRow(p, p.points > 0 ? seen : null, g.tier.key, i === 0);
               }).join('');
      }).join('') +
      '<p class="hint">The top five are numbered straight through; every ' +
      'division below counts from one. Pass the person above you and you ' +
      'take their place.</p>';
      return;
    }
    var me = state.profile ? state.profile.id : null;
    var html = ranked(state.board).map(function (r) {
      return boardRow(r.row, r.rank, null);
    }).join('');
    box.innerHTML = html;
  }

  function feedHtml(rows) {
    if (!rows.length) return '<div class="muted small" style="padding:8px 0">Nothing logged this week.</div>';
    var me = state.profile ? state.profile.id : null;

    /* A busy week is forty lines long. Group by day and collapse all but the
       most recent, so opening someone's feed shows a week at a glance. */
    var days = [], byDay = {};
    rows.forEach(function (w) {
      var when = new Date(w.created_at);
      var key = new Intl.DateTimeFormat('en-CA', { timeZone: TZ }).format(when);
      if (!byDay[key]) { byDay[key] = { key: key, when: when, rows: [], pts: 0 }; days.push(byDay[key]); }
      byDay[key].rows.push(w);
      byDay[key].pts += Number(w.points) || 0;
    });
    days.sort(function (a, b) { return b.key < a.key ? -1 : 1; });

    return days.map(function (d, i) {
      var open = state.feedDay[d.key] !== undefined ? state.feedDay[d.key] : i === 0;
      var label = new Intl.DateTimeFormat('en-GB',
        { weekday: 'long', day: 'numeric', month: 'short', timeZone: TZ }).format(d.when);
      return '<div class="fday' + (open ? ' open' : '') + '">' +
        '<button type="button" class="fday-h" data-day="' + d.key + '">' +
          '<span class="fday-x">' + (open ? '▴' : '▾') + '</span>' +
          '<span class="fday-n">' + esc(label.toUpperCase()) + '</span>' +
          '<span class="fday-c">' + d.rows.length + '</span>' +
          '<span class="fday-p">+' + num(d.pts) + '</span>' +
        '</button>' +
        (open ? d.rows.map(function (w) {
          var ex = exercise(w.exercise_key);
          var u = unitFor(w.exercise_key, w.mode);
          var hh = new Intl.DateTimeFormat('en-GB',
            { hour: '2-digit', minute: '2-digit', hourCycle: 'h23', timeZone: TZ })
            .format(new Date(w.created_at));
          return '<div class="fitem">' +
            '<span class="fx">' + esc(ex ? ex.name : w.exercise_key) +
              '<span class="famt"> · ' + num(w.amount) + ' ' + u.short + ' · ' + hh + '</span></span>' +
            '<span class="fpts">+' + num(w.points) +
              (Number(w.boost) > 1 ? '<span class="boost">&times;' + w.boost + '</span>' : '') +
            '</span>' +
            (w.profile_id === me && !isRestDay()
              ? '<button class="del" data-edit="' + w.id + '" title="Edit">✎</button>' +
                '<button class="del" data-del="' + w.id + '" title="Delete">✕</button>'
              : '') +
          '</div>';
        }).join('') : '') +
      '</div>';
    }).join('');
  }

  async function loadFeed(profileId) {
    var r = await sb.from('workouts').select('*')
      .eq('league_id', state.leagueId)
      .eq('profile_id', profileId)
      .eq('week_start', state.week)
      .order('created_at', { ascending: false });
    if (r.error) { toast(niceError(r.error), true); return; }
    state.feeds[profileId] = r.data || [];
    var box = document.querySelector('[data-feed="' + profileId + '"]');
    if (box) box.innerHTML = feedHtml(state.feeds[profileId]);
  }

  document.addEventListener('click', function (e) {
    var h = e.target.closest('[data-day]'); if (!h) return;
    var k = h.getAttribute('data-day');
    var open = h.parentNode.classList.contains('open');
    state.feedDay[k] = !open;
    var feed = h.closest('.feed');
    var pid = feed && feed.getAttribute('data-feed');
    if (pid && state.feeds[pid]) feed.innerHTML = feedHtml(state.feeds[pid]);
  });

  $('#board').addEventListener('click', async function (e) {
    var ed = e.target.closest('[data-edit]');
    if (ed) {
      e.stopPropagation();
      openEdit(ed.getAttribute('data-edit'));
      return;
    }
    var del = e.target.closest('[data-del]');
    if (del) {
      e.stopPropagation();
      if (!confirm('Delete this entry?')) return;
      var target = findEntry(del.getAttribute('data-del'));
      var r = await sb.from('workouts').delete()
                .eq('group_id', target ? target.group_id : del.getAttribute('data-del'))
                .select();
      if (r.error) { toast(niceError(r.error), true); return; }
      if (!r.data || !r.data.length) {
        toast('That week is over — its entries are locked.', true); return;
      }
      state.feeds = {};
      toast('Entry deleted');
      await refreshAll();
      Object.keys(state.open).forEach(function (id) { if (state.open[id]) loadFeed(id); });
      return;
    }
    var btn = e.target.closest('[data-toggle]');
    if (!btn) return;
    var id = btn.getAttribute('data-toggle');
    state.open[id] = !state.open[id];
    renderBoard();
    if (state.open[id] && !state.feeds[id]) loadFeed(id);
  });

  /* ===================================================================
     10. Hall of fame
     =================================================================== */
  async function loadHistory() {
    if (!state.leagueId) return;
    var r = await sb.rpc('weekly_history', { p_league: state.leagueId });
    if (r.error) { toast(niceError(r.error), true); return; }
    state.history = r.data || [];
    renderHall();

    var w = await sb.rpc('league_wins', { p_league: state.leagueId });
    if (!w.error) state.wins = w.data || [];
    var c = await sb.rpc('league_champions', { p_league: state.leagueId });
    if (!c.error) state.champions = c.data || [];
    renderWins();
  }

  /* Cut the run of weekly champions into races to N.
     Somebody reaching N ends that run there: the weeks after it belong to the
     next one, and everybody starts the next one on zero. Doing it here rather
     than in the database is what lets the target be a switch on the phone —
     change it and every past run is re-cut on the spot, with nothing stored
     and nothing to migrate. */
  function cutRuns(champions, target) {
    var runs = [], tally = {}, weeks = 0;
    (champions || []).forEach(function (c) {
      tally[c.profile_id] = (tally[c.profile_id] || 0) + 1;
      weeks++;
      if (tally[c.profile_id] >= target) {
        runs.push({ winner: c, weeks: weeks, ended: c.week_start });
        tally = {}; weeks = 0;
      }
    });
    return { done: runs, tally: tally, weeks: weeks, n: runs.length + 1 };
  }

  /* Crowns. One per week won, counted from the same finished weeks the hall
     below is built from, so the two can never tell different stories. The
     race target is a local choice — nothing about "first to five" needs to be
     agreed with the server, and making it a league setting would mean one
     person deciding it for everybody. */
  function renderWins() {
    var box = $('#wins');
    var rows = state.wins || [];
    var weeks = rows.length ? Number(rows[0].weeks) : 0;
    if (!weeks) {
      $('#winsWeeks').textContent = 'NOT STARTED';
      box.innerHTML = '<div class="empty">No week has finished yet.<br>' +
        'The first crown is handed out on Sunday at 23:59.</div>';
      return;
    }
    var target = state.raceTo;
    var run = cutRuns(state.champions, target);
    var lead = Math.max.apply(null, [0].concat(rows.map(function (r) {
      return run.tally[r.profile_id] || 0; })));

    /* A shut section still has to be worth reading, so the summary carries
       the only two numbers that matter: which run, and where you are in it. */
    var me = rows.filter(function (r) { return r.mine; })[0];
    $('#winsWeeks').textContent = 'RUN ' + run.n + ' · YOU ' +
      ((me && run.tally[me.profile_id]) || 0) + '/' + target;

    /* Runs already taken, most recent first. This is the answer to "what
       happens when somebody gets there": their name goes up here for good,
       the board resets to zero, and the next run starts the following week. */
    var past = run.done.length
      ? '<div class="runs">' + run.done.slice().reverse().map(function (r, i) {
          return '<span class="run"><i>RUN ' + (run.done.length - i) + '</i>' +
            avatarHtml(r.winner.avatar, { name: r.winner.display_name, size: 'sm' }) +
            '<b>' + esc(r.winner.display_name) + '</b>' +
            '<em>' + r.weeks + (r.weeks === 1 ? ' week' : ' weeks') + '</em></span>';
        }).join('') + '</div>'
      : '';

    box.innerHTML = past +
      '<div class="runhead"><span>RUN ' + run.n + ' · FIRST TO ' + target + '</span>' +
        '<i>' + (run.weeks
          ? run.weeks + (run.weeks === 1 ? ' week in' : ' weeks in')
          : 'starts with the next week') + '</i></div>' +
      /* Everybody who has ever won a week, plus you. A league of thirty
         renders twenty-eight rows of "0 / 5" if you list all of it, and the
         names that matter get lost — but somebody who took a week in an
         earlier run has earned their place on the board even while they are
         on zero in this one. */
      (function () {
        var inIt = rows.filter(function (r) {
          return (run.tally[r.profile_id] || 0) > 0 || Number(r.wins) > 0 || r.mine; });
        var rest = rows.length - inIt.length;
        return inIt.map(function (r) {
          var here = run.tally[r.profile_id] || 0;
          var life = Number(r.wins);
          var pct = Math.min(100, here / target * 100);
          return '<div class="win' + (r.mine ? ' me' : '') +
              (here && here === lead ? ' lead' : '') + '">' +
            '<span class="win-w">' + avatarHtml(r.avatar,
                { name: r.display_name, size: 'sm' }) +
              '<b>' + esc(r.display_name) + '</b>' +
              '<i>' + (life ? crowns(life) + ' all time' : 'no crown yet') + '</i></span>' +
            '<span class="win-n">' + here + '<em>/' + target + '</em></span>' +
            '<span class="win-bar"><span style="width:' + pct.toFixed(1) + '%"></span></span>' +
          '</div>';
        }).join('') + (rest > 0
          ? '<p class="restline">and ' + rest + ' others yet to win a week</p>'
          : '');
      })() +
      '<p class="hint">' + (lead
        ? (target - lead) + (target - lead === 1 ? ' week' : ' weeks') +
          ' from the front. Reach ' + target + ' and the run is yours — the board ' +
          'clears and run ' + (run.n + 1) + ' starts the Monday after.'
        : 'Everyone starts on zero. First to ' + target + ' takes the run.') + '</p>';
  }

  /* Five crowns is a row of five; twelve is a crown and a number. Nobody can
     count fourteen little pictures anyway. */
  function crowns(n) {
    if (!n) return '';
    if (n <= 4) return new Array(n + 1).join('<i class="cr">👑</i>');
    return '<i class="cr">👑</i><b class="crx">x' + n + '</b>';
  }

  window.__cutRuns__ = cutRuns;   // the run maths, reachable from a test
  window.__state__ = state;
  window.__BODY__ = BODY;
  window.__renderWins__ = renderWins;
  window.__fixFor__ = fixFor;
  window.__EXNAME__ = function (k) { var e = exercise(k); return e ? e.name : k; };

  $('#winsRace').addEventListener('click', function (e) {
    var b = e.target.closest('[data-race]'); if (!b) return;
    state.raceTo = Number(b.getAttribute('data-race'));
    try { localStorage.setItem(LS.race, String(state.raceTo)); } catch (err) {}
    Array.prototype.forEach.call(this.querySelectorAll('button'), function (x) {
      x.classList.toggle('on', x === b);
    });
    renderWins();
  });

  function renderHall() {
    var box = $('#hall');
    if (!state.history.length) {
      box.innerHTML = '<div class="empty">No finished week yet.<br>' +
        'The first champion is crowned on Sunday at 23:59.</div>';
      return;
    }
    var weeks = [], byWeek = {};
    state.history.forEach(function (r) {
      if (!byWeek[r.week_start]) { byWeek[r.week_start] = []; weeks.push(r.week_start); }
      byWeek[r.week_start].push(r);
    });
    box.innerHTML = weeks.map(function (wk, wi) {
      var rows = byWeek[wk].slice().sort(function (a, b) { return b.points - a.points; });
      var win = rows[0];
      var open = !!state.openWeeks[wk];

      /* Most improved: biggest gain on your own previous week. You have to
         have been there the week before, so nobody wins it by appearing. */
      var prevWk = weeks[wi + 1], climber = null;
      if (prevWk) {
        var prev = {};
        byWeek[prevWk].forEach(function (r) { prev[r.profile_id] = Number(r.points); });
        rows.forEach(function (r) {
          if (prev[r.profile_id] === undefined) return;
          var gain = Number(r.points) - prev[r.profile_id];
          if (gain > 0 && (!climber || gain > climber.gain)) {
            climber = { row: r, gain: gain };
          }
        });
      }
      return '<div class="week">' +
        '<button class="week-top" type="button" data-week="' + wk + '">' +
          '<span class="crown">👑</span>' +
          '<span><span class="week-when">' + weekRangeLabel(wk) + '</span>' +
            '<span class="week-who" style="display:block">' +
              avatarHtml(win.avatar, { name: win.display_name, size: 'sm' }) +
              esc(win.display_name) + '</span></span>' +
          '<span class="week-pts">' + num(win.points) + '<br>' +
            '<span class="muted" style="font-size:10px;letter-spacing:.14em">PTS</span></span>' +
          '<span class="week-chev">' + (open ? '▲' : '▼') + '</span>' +
        '</button>' +
        (climber ? '<div class="climber"><span class="cl-i">📈</span>' +
            '<span class="cl-t">MOST IMPROVED</span>' +
            '<span class="cl-n">' + avatarHtml(climber.row.avatar,
                 { name: climber.row.display_name, size: 'sm' }) +
              esc(climber.row.display_name) + '</span>' +
            '<span class="cl-g">+' + num(climber.gain) + '</span></div>' : '') +
        (open ? '<div class="week-body">' + rows.map(function (r, i) {
            return '<div class="mini"><span class="mn">' + (i + 1) + '. ' + esc(r.display_name) +
                   '</span><span class="mp">' + num(r.points) + ' pts · ' + r.entries + '</span></div>';
          }).join('') + '</div>' : '') +
      '</div>';
    }).join('');
  }

  $('#hall').addEventListener('click', function (e) {
    var b = e.target.closest('[data-week]');
    if (!b) return;
    var wk = b.getAttribute('data-week');
    state.openWeeks[wk] = !state.openWeeks[wk];
    renderHall();
  });

  /* ===================================================================
     11. Leagues / profile tab
     =================================================================== */
  function shareUrl(code) {
    return location.origin + location.pathname + '#/join/' + code;
  }

  function renderMe() {
    var box = $('#leagueList');
    if (!state.leagues.length) {
      box.innerHTML = '<div class="empty">You are not in a league yet.<br>' +
        'Create one below, or open a friend\'s invite link.</div>';
    } else {
      box.innerHTML = state.leagues.map(function (l) {
        var full = l.members >= l.max_members;
        return '<button class="lg-card' + (l.id === state.leagueId ? ' active' : '') +
          '" type="button" data-league="' + l.id + '">' +
          leagueBadgeHtml(l, 'md') +
          '<span><span class="t">' + esc(l.name) + '</span>' +
          '<span class="s">CODE ' + esc(l.code) +
          (state.profile && l.owner_id === state.profile.id ? ' · YOURS' : '') + '</span></span>' +
          '<span class="cap' + (full ? ' full' : '') + '">' + l.members + '/' + l.max_members + '</span>' +
        '</button>';
      }).join('');
    }
    var l = league();
    if (l) {
      var cur = badgeOf(l);
      var chosen = l.badge || {};
      $('#optCrest').innerHTML = leagueBadgeHtml(l, 'sm') +
        (chosen.emblem ? ' ' + esc(cur.emblem.replace(/-/g, ' '))
                       : ' Auto — from the league code');
      $('#optBanner').textContent = chosen.skin
        ? (skinName(chosen.skin) || chosen.skin) : 'Nothing yet';
      $('#optMetal').textContent =
        (chosen.shape ? cur.shape : cur.shape + ' (auto)') + ' · ' +
        (chosen.color ? cur.color : cur.color + ' (auto)');
    }
    $('#shareLink').value = l ? shareUrl(l.code) : '—';
    if (state.profile) {
      $('#renameInput').value = state.profile.display_name;
      $('#bwProfile').value = state.profile.bodyweight
        ? Math.round(fromKg(state.profile.bodyweight) * 10) / 10 : '';
      paintUnits();
      paintForm();
      $('#restoreCode').textContent = state.profile.restore_code;
      buildAvatarGrid();
      paintNameColors();
    }
    paintLeagueRules();
    buildShop();
    if ($('#sectEmblem').open) buildAvatarGridsOnce();
    if (navigator.share) $('#shareBtn').hidden = !l;
  }

  $('#leagueList').addEventListener('click', async function (e) {
    var b = e.target.closest('[data-league]');
    if (!b) return;
    state.leagueId = b.getAttribute('data-league');
    localStorage.setItem(LS.league, state.leagueId);
    state.feeds = {}; state.open = {}; state.openWeeks = {};
    renderHeader(); renderMe();
    switchView('live');
    await refreshAll();
  });

  $('#copyBtn').addEventListener('click', async function () {
    var v = $('#shareLink').value;
    try {
      await navigator.clipboard.writeText(v);
      toast('Link copied — paste it in your group chat');
    } catch (e) {
      $('#shareLink').select(); document.execCommand('copy');
      toast('Link copied');
    }
  });
  $('#shareBtn').addEventListener('click', function () {
    var l = league(); if (!l) return;
    navigator.share({
      title: l.name,
      text: 'Join my calisthenics league "' + l.name + '" — weekly leaderboard, no mercy.',
      url: shareUrl(l.code)
    }).catch(function () {});
  });

  $('#joinBtn').addEventListener('click', async function () {
    var code = $('#joinCodeInput').value.trim();
    if (!code) return;
    try {
      var j = await sb.rpc('join_league_by_code', { p_code: code });
      if (j.error) throw j.error;
      $('#joinCodeInput').value = '';
      state.leagueId = j.data.id;
      localStorage.setItem(LS.league, state.leagueId);
      await loadLeagues();
      renderMe(); renderHeader();
      toast('Joined ' + j.data.name);
      switchView('live');
      await refreshAll();
    } catch (e) { toast(niceError(e), true); }
  });

  $('#createToggle').addEventListener('click', function () {
    var w = $('#createWrap');
    w.hidden = !w.hidden;
    this.textContent = w.hidden ? 'SHOW CREATE OPTION' : 'HIDE';
    if (!w.hidden) $('#newLeagueInput').focus();
  });

  $('#createBtn').addEventListener('click', async function () {
    var name = $('#newLeagueInput').value.trim();
    if (name.length < 2) { toast('Give the league a name.', true); return; }
    if (state.leagues.length && !confirm(
      'This creates a SEPARATE league with its own leaderboard.\n\n' +
      'To add friends to "' + league().name + '", close this and send them ' +
      'your invite link instead.\n\nCreate a new league anyway?')) return;
    try {
      var c = await sb.rpc('create_league', { p_name: name });
      if (c.error) throw c.error;
      $('#newLeagueInput').value = '';
      state.leagueId = c.data.id;
      localStorage.setItem(LS.league, state.leagueId);
      await loadLeagues();
      renderMe(); renderHeader();
      buildExerciseSelect();
      toast('League created — code ' + c.data.code);
      await refreshAll();
    } catch (e) { toast(niceError(e), true); }
  });

  $('#renameBtn').addEventListener('click', async function () {
    var n = $('#renameInput').value.trim();
    if (n.length < 2) { toast('Names must be at least 2 characters.', true); return; }
    try {
      var r = await sb.rpc('rename_profile', { p_name: n });
      if (r.error) throw r.error;
      state.profile = r.data;
      toast('Name updated');
      await refreshAll();
    } catch (e) { toast(niceError(e), true); }
  });

  function buildPointsTable() {
    $('#pointsTable').innerHTML = CATEGORIES.map(function (cat) {
      var list = EXERCISES.filter(function (e) { return e.cat === cat; });
      if (!list.length) return '';
      return '<div class="pcat">' + cat + '</div>' + list.map(function (ex) {
        return '<div class="pex">' +
          '<div class="pex-n">' + esc(ex.name) + '</div>' +
          '<div class="pex-v">' + esc(ex.variants) + '</div>' +
          ex.modes.map(function (m) {
            return '<div class="pex-r">' + m.label + '</div>';
          }).join('') +
        '</div>';
      }).join('');
    }).join('');
  }

  /* ===================================================================
     10b. Duels — 24h head to head. A duel never stores its own workouts;
     it simply reads what you already logged for the league in its window,
     so nothing is ever entered twice.
     =================================================================== */
  function hhmmss(ms) {
    if (ms < 0) ms = 0;
    var s = Math.floor(ms / 1000), pad = function (n) { return String(n).padStart(2, '0'); };
    return pad(Math.floor(s / 3600)) + ':' + pad(Math.floor(s % 3600 / 60)) + ':' + pad(s % 60);
  }

  async function loadDuels() {
    if (!state.leagueId) return;
    var r = await sb.rpc('my_challenges');
    if (r.error) { toast(niceError(r.error), true); return; }
    state.duels = r.data || [];
    renderDuels();
  }

  function renderDuels() {
    var open = state.duels.filter(function (d) {
      return d.status === 'LIVE' || d.status === 'PENDING';
    })[0];
    var past = state.duels.filter(function (d) {
      return d.status === 'FINISHED' || d.status === 'CANCELLED' || d.status === 'EXPIRED';
    });

    $('#duelStart').hidden = !!open;
    $('#duelActive').innerHTML = open ? (open.status === 'LIVE' ? liveDuel(open) : pendingDuel(open)) : '';
    $('#duelPastHead').hidden = !past.length;
    $('#duelPast').innerHTML = past.map(pastDuel).join('');
    tickDuel();
  }

  function liveDuel(d) {
    var me = Number(d.me_points), foe = Number(d.foe_points);
    return '<div class="duel live">' +
      '<div class="duel-top"><span class="duel-tag">DUEL LIVE</span>' +
        '<span class="duel-clock" data-ends="' + d.ends_at + '">--:--:--</span></div>' +
      '<div class="duel-body">' +
        '<div class="duel-side' + (me > foe ? ' lead' : '') + '">' +
          '<div class="duel-av">' + avatarHtml(d.me_avatar, { size: 'lg' }) + '</div>' +
          '<div class="duel-name">YOU</div>' +
          '<div class="duel-pts">' + num(me) + '</div></div>' +
        '<div class="duel-vs">VS</div>' +
        '<div class="duel-side' + (foe > me ? ' lead' : '') + '">' +
          '<div class="duel-av">' + avatarHtml(d.foe_avatar, { size: 'lg' }) + '</div>' +
          '<div class="duel-name">' + esc(d.foe_name || '—') + '</div>' +
          '<div class="duel-pts">' + num(foe) + '</div></div>' +
      '</div>' +
      '<div class="duel-note"><b>Log once.</b> Everything you log normally counts ' +
        'for your league week <b>and</b> for this duel. There is nothing extra to enter.</div>' +
    '</div>';
  }

  function pendingDuel(d) {
    var left = new Date(d.created_at).getTime() + 24 * 3600e3 - Date.now();
    return '<div class="duel">' +
      '<div class="duel-top"><span class="duel-tag">WAITING FOR AN OPPONENT</span>' +
        '<span class="duel-clock" data-ends="' +
          new Date(new Date(d.created_at).getTime() + 24 * 3600e3).toISOString() +
        '">--:--:--</span></div>' +
      '<div style="padding:12px 13px">' +
        '<p class="muted small">Send this code to someone in ' + esc(d.league_name) +
          '. The first one to enter it starts a 24 hour duel with you.</p>' +
        '<div class="duel-code">' + esc(d.code) + '</div>' +
        '<div class="copyrow">' +
          '<button class="btn ghost sm" style="flex:1" data-duelcopy="' + esc(d.code) + '">COPY CODE</button>' +
          '<button class="btn ghost sm" style="flex:1" data-duelcancel="' + d.id + '">CANCEL</button>' +
        '</div>' +
        (left < 0 ? '' : '<p class="muted small" style="margin-top:10px">Unclaimed codes expire after 24 hours.</p>') +
      '</div>' +
    '</div>';
  }

  function pastDuel(d) {
    var me = Number(d.me_points), foe = Number(d.foe_points);
    var cls = 'd', label = 'DRAW';
    if (d.status === 'CANCELLED') { cls = 'd'; label = 'CANCELLED'; }
    else if (d.status === 'EXPIRED') { cls = 'd'; label = 'NO TAKER'; }
    else if (me > foe) { cls = 'w'; label = 'WON'; }
    else if (foe > me) { cls = 'l'; label = 'LOST'; }
    var settled = d.status === 'FINISHED';
    return '<div class="duel-row">' +
      '<span class="duel-res ' + cls + '">' + label + '</span>' +
      '<span class="dr">' + (settled ? 'vs ' + avatarText(d.foe_avatar) +
        esc(d.foe_name || '—') : esc(d.league_name)) + '</span>' +
      '<span class="ds">' + (settled ? num(me) + ' – ' + num(foe) : '') + '</span>' +
    '</div>';
  }

  /* the live/pending clock, driven by the one-second loop */
  function tickDuel() {
    Array.prototype.forEach.call(document.querySelectorAll('[data-ends]'), function (el) {
      var ms = new Date(el.getAttribute('data-ends')).getTime() - Date.now();
      el.textContent = hhmmss(ms);
      if (ms <= 0) el.textContent = 'OVER';
    });
  }

  $('#duelCreateBtn').addEventListener('click', async function () {
    this.disabled = true;
    try {
      var r = await sb.rpc('create_challenge', { p_league: state.leagueId });
      if (r.error) throw r.error;
      toast('Duel code ' + r.data.code + ' — send it to someone');
      await loadDuels();
    } catch (e) { toast(niceError(e), true); }
    this.disabled = false;
  });

  $('#duelAcceptBtn').addEventListener('click', async function () {
    var code = $('#duelCodeInput').value.trim();
    if (!code) return;
    try {
      var r = await sb.rpc('accept_challenge', { p_code: code });
      if (r.error) throw r.error;
      $('#duelCodeInput').value = '';
      $('#duelPreview').textContent = '';
      toast('Duel on. 24 hours.');
      await loadDuels();
    } catch (e) { toast(niceError(e), true); }
  });

  /* show who is behind a code before committing to it */
  var duelPeekTimer;
  $('#duelCodeInput').addEventListener('input', function () {
    var v = this.value.trim().toUpperCase();
    this.value = v;
    var out = $('#duelPreview');
    clearTimeout(duelPeekTimer);
    out.className = 'muted small';
    if (v.length < 6) { out.textContent = ''; return; }
    duelPeekTimer = setTimeout(async function () {
      var r = await sb.rpc('challenge_preview', { p_code: v });
      var c = r.data && r.data[0];
      if (!c) { out.className = 'err small'; out.textContent = 'No duel with that code.'; return; }
      if (c.status !== 'PENDING') {
        out.className = 'err small';
        out.textContent = 'That duel is ' + c.status.toLowerCase() + '.';
        return;
      }
      out.style.color = 'var(--green)';
      out.textContent = '✓ ' + avatarText(c.challenger_avatar) +
        c.challenger_name + ' is waiting — ' + c.league_name;
    }, 350);
  });

  $('#duelActive').addEventListener('click', async function (e) {
    var copy = e.target.closest('[data-duelcopy]');
    if (copy) {
      var code = copy.getAttribute('data-duelcopy');
      try { await navigator.clipboard.writeText(code); } catch (err) {}
      toast('Code ' + code + ' copied');
      return;
    }
    var can = e.target.closest('[data-duelcancel]');
    if (can) {
      if (!confirm('Cancel this duel code?')) return;
      var r = await sb.rpc('cancel_challenge', { p_id: can.getAttribute('data-duelcancel') });
      if (r.error) { toast(niceError(r.error), true); return; }
      toast('Duel cancelled');
      await loadDuels();
    }
  });

  /* ===================================================================
     10c. Weekly bounty — one side quest, always the same weekday.
     Completion is read from the logs, so there is nothing to claim.
     =================================================================== */
  async function loadBounty() {
    if (!state.leagueId) return;
    var r = await sb.rpc('current_bounty', { p_league: state.leagueId });
    if (r.error) { $('#bountyCard').hidden = true; return; }
    var b = r.data && r.data[0];
    if (!b) { $('#bountyCard').hidden = true; return; }

    var day = new Date(b.on_date + 'T12:00:00Z');
    var dayLabel = new Intl.DateTimeFormat('en-GB',
      { weekday: 'long', day: 'numeric', month: 'short', timeZone: 'UTC' })
      .format(day).toUpperCase();

    /* A bounty counts on its day only — never before, never after. Say so. */
    var today = iso(wallNow());
    var when  = b.on_date > today ? 'soon' : (b.on_date === today ? 'today' : 'past');
    var tag   = when === 'today' ? 'BOUNTY · TODAY ONLY'
              : when === 'soon'  ? 'BOUNTY · ' + dayLabel
              :                    'BOUNTY · CLOSED';
    var rule  = when === 'today' ? 'Counts today only, until midnight.'
              : when === 'soon'  ? 'Counts on ' + dayLabel + ' only — not before, not after.'
              :                    'This one is over.';

    state.chip.bounty = { on: b.mine, urgent: when === 'today' && !b.mine,
      t: 'BOUNTY ' + (b.mine ? 'DONE' : when === 'today' ? 'TODAY'
                    : when === 'soon' ? dayLabel.slice(0, 3) : 'CLOSED') };
    paintWeekChips();
    $('#bountyCard').hidden = false;
    $('#bountyCard').className = 'bounty' + (b.mine ? ' got' : '') +
      (when === 'today' ? ' now' : '') + (when === 'past' ? ' shut' : '');
    $('#bountyCard').innerHTML =
      '<div class="bo-top">' +
        '<span class="bo-tag">' + tag + '</span>' +
        '<span class="bo-pts">' + (b.mine ? '✓ +' + num(b.points) : '+' + num(b.points)) + '</span>' +
      '</div>' +
      '<div class="bo-name">' + esc(b.name) + '</div>' +
      '<div class="bo-desc">' + esc(b.descr) + '</div>' +
      '<div class="bo-rule">' + rule + '</div>' +
      '<div class="bo-foot">' +
        (b.winners > 0
          ? b.winners + (b.winners === 1 ? ' has ' : ' have ') + 'done it' +
            (b.first_name ? ' · first ' + esc(avatarText(b.first_avatar)) +
              esc(b.first_name) + ' 🩸' : '')
          : when === 'past' ? 'Nobody got it' : 'Nobody has claimed it yet') +
      '</div>';
  }

  /* ===================================================================
     11a. Daily combo
     =================================================================== */
  function comboReward(n) {
    for (var i = 0; i < COMBO_TIERS.length; i++) {
      if (n >= COMBO_TIERS[i].n) return COMBO_TIERS[i].pts;
    }
    return 0;
  }

  async function loadCombo() {
    if (!state.leagueId) return;
    if (isRestDay()) {
      $('#comboCard').hidden = false;
      $('#comboCard').className = 'combo rest';
      $('#comboCard').innerHTML =
        '<div class="combo-top"><span class="combo-t">REST DAY</span>' +
          '<span class="combo-p">LEAGUE CLOSED</span></div>' +
        '<div class="combo-hint">Saturday night closed the week — nothing can be ' +
        'added, edited or deleted today. One <b>stretching session</b> is the only ' +
        'thing that still counts. Recover, and come back Monday.</div>';
      return;
    }
    $('#comboCard').className = 'combo';
    var r = await sb.rpc('my_combo_today', { p_league: state.leagueId });
    if (r.error) return;
    var got = {};
    (r.data || []).forEach(function (x) { got[x.category] = Number(x.points); });
    var hit = COMBO_CATS.filter(function (c) { return (got[c] || 0) >= COMBO_MIN; });
    var earned = comboReward(hit.length);
    var next = null;
    for (var i = COMBO_TIERS.length - 1; i >= 0; i--) {
      if (COMBO_TIERS[i].n > hit.length) { next = COMBO_TIERS[i]; break; }
    }
    state.chip.combo = { on: !!earned,
      t: 'COMBO ' + hit.length + '/' + COMBO_CATS.length +
         (earned ? ' +' + earned : '') };
    paintWeekChips();
    $('#comboCard').hidden = false;
    $('#comboCard').innerHTML =
      '<div class="combo-top"><span class="combo-t">DAILY COMBO</span>' +
        '<span class="combo-p' + (earned ? ' on' : '') + '">' +
          (earned ? '+' + earned + ' TODAY' : 'NO BONUS YET') + '</span></div>' +
      '<div class="combo-pips">' + COMBO_CATS.map(function (c) {
        var v = got[c] || 0, done = v >= COMBO_MIN;
        return '<span class="pip' + (done ? ' done' : '') + '">' + c +
               '<i>' + num(Math.min(v, COMBO_MIN)) + '/' + COMBO_MIN + '</i></span>';
      }).join('') + '</div>' +
      '<div class="combo-hint">' + (next
        ? (next.n - hit.length) + ' more group' + (next.n - hit.length > 1 ? 's' : '') +
          ' today for +' + next.pts
        : 'Maximum combo reached today. ') + '</div>';
  }

  function buildComboRules() {
    $('#comboRules').innerHTML =
      '<p class="muted small">Score at least <b>' + COMBO_MIN + ' points</b> in ' +
      'different muscle groups on the <b>same day</b> and the bonus is added ' +
      'automatically. Only the highest tier counts.</p>' +
      '<div class="ptable">' + COMBO_TIERS.slice().reverse().map(function (t) {
        return '<div class="pex" style="display:flex;justify-content:space-between">' +
          '<span>' + t.n + ' groups in one day</span>' +
          '<b style="color:var(--red);font-family:var(--display)">+' + t.pts + '</b></div>';
      }).join('') + '</div>' +
      '<p class="muted small" style="margin-top:8px">Groups: ' + COMBO_CATS.join(' · ') +
      '. Recovery does not count.</p>';
  }

  /* ===================================================================
     11b. Personal stats
     =================================================================== */
  var CAT_COLOR = { PUSH: '#ff2e2e', PULL: '#ff8a1f', LEGS: '#ffc93c',
                    CORE: '#26d07c', CARDIO: '#3aa8ff', RECOVERY: '#9b7bff' };

  async function loadStats() {
    if (!state.leagueId) return;
    $('#statsWho').textContent = state.profile ? state.profile.display_name : '';
    var r = await sb.rpc('my_stats', {
      p_league: state.leagueId, p_all: state.statsRange === 'all'
    });
    if (r.error) { toast(niceError(r.error), true); return; }
    renderStats(r.data || []);

    var rec = await sb.rpc('my_duel_record', { p_league: state.leagueId });
    renderDuelRecord(!rec.error && rec.data && rec.data[0]);

    var all = state.statsRange === 'all' ? r
            : await sb.rpc('my_stats', { p_league: state.leagueId, p_all: true });
    if (!all.error) {
      renderMilestones((all.data || []).reduce(function (a2, x) {
        return a2 + Number(x.total_points); }, 0));
    }

    var mus = await sb.rpc('my_muscles', {
      p_league: state.leagueId, p_all: state.statsRange === 'all' });
    if (!mus.error) { state.muscles = mus.data || []; renderBody(); }
  }

  /* ===================================================================
     11b-2. The body
     =================================================================== */
  /* Colour is the whole point: a figure you have to read the numbers off is
     just a table with a drawing next to it. Cold where there is nothing,
     hot where there is work, and the same five steps for every region so two
     muscles the same colour really have had the same attention. */
  /* The scale reads as a week being filled, so the words are about how much of
     the week is done rather than how hard you went. `col` fills the plate,
     `ink` writes the number: a plate dark enough to read as empty against the
     figure is too dark to read as text against the panel. */
  var BODY_STEPS = [
    { at: 0,   col: '#333947', ink: '#828b9c', name: 'nothing yet' },
    { at: 1,   col: '#2f5d7c', ink: '#5b9ec9', name: 'barely started' },
    { at: 25,  col: '#2f8f86', ink: '#3fc0b3', name: 'a quarter of a week' },
    { at: 50,  col: '#4fae54', ink: '#5fce65', name: 'half a week' },
    { at: 80,  col: '#a8bf33', ink: '#c2d94a', name: 'nearly a full week' },
    /* Three shades above full, because plenty of people do two or three
       weeks' worth of pushing in one week. With a single "full" colour their
       whole figure went one flat red and stopped telling them anything. */
    { at: 100, col: '#d8a72e', ink: '#f0bd3c', name: 'a full week done' },
    { at: 160, col: '#e07a24', ink: '#ff9440', name: 'well past a full week' },
    { at: 250, col: '#e2542e', ink: '#ff6a3d', name: 'two and a half weeks over' }
  ];

  function bodyStep(pct) {
    var out = BODY_STEPS[0];
    BODY_STEPS.forEach(function (s2) { if (pct >= s2.at) out = s2; });
    return out;
  }

  function renderBody() {
    var fig = $('#bodyFig');
    if (!fig) return;
    var rows = state.muscles || [];
    var by = {};
    rows.forEach(function (m) { by[m.key] = m; });

    /* The art has two figures. Anyone still stored as 'neutral' — the default
       before the illustration arrived — is drawn as the male one until they
       pick, rather than being shown nothing. */
    var form = (state.profile && state.profile.body_form) || 'masc';
    var art = BODY[form] || BODY.masc;
    var view = state.bodyView === 'back' ? 'back' : 'front';
    var weeks = rows.length ? Number(rows[0].weeks) : 0;
    var filled = rows.filter(function (m) { return Number(m.pct) >= 100; }).length;
    $('#bodyWeeks').textContent = !rows.length ? ''
      : filled + ' / ' + rows.length + ' FILLED' +
        (state.statsRange === 'all' ? ' · ' + weeks + 'W' : '');

    if (!rows.length) {
      fig.innerHTML = '';
      $('#bodyRead').innerHTML = '';
      $('#bodyFix').innerHTML = '';
      $('#bodyBalance').innerHTML = '';
      $('#muscleCount').textContent = '';
      $('#bodyList').innerHTML = '<div class="empty">Log something and the figure ' +
        'starts filling in.</div>';
      return;
    }

    /* One gradient over the whole figure, drawn last and deaf to clicks. Flat
       vector muscles on a flat body read as a diagram; a single top-light
       makes it read as a body, and costs one path. */
    /* The rest of the body first — head, hands, feet, the parts nothing is
       scored on — then the regions over it, so a highlight always sits on top
       of the figure rather than under an ankle. */
    fig.innerHTML =
      '<svg viewBox="' + art.box[view] + '" class="bsvg" ' +
        'role="img" aria-label="Muscle coverage, ' + view + ' view">' +
        art.inert[view].map(function (d) {
          return '<path class="bskin" d="' + d + '"/>';
        }).join('') +
        art[view].map(function (pl) {
          var m = by[pl.m] || { pct: 0, name: pl.m };
          var on = state.bodyPick === pl.m;
          return '<path class="bpart' + (on ? ' on' : '') + '" data-m="' + pl.m +
                 '" fill="' + bodyStep(Number(m.pct)).col + '" d="' + pl.d + '">' +
                 '<title>' + esc(m.name + ' · ' + num(m.pct) + '% of a week') +
                 '</title></path>';
        }).join('') +
      '</svg>';

    /* The readout under the figure: whatever is being held, or the weakest
       region, which is the one worth naming when nobody is pointing. */
    var pick = by[state.bodyPick];
    var weakest = rows.reduce(function (a, b) {
      return Number(b.pct) < Number(a.pct) ? b : a; });
    var shown = pick || weakest;
    var st = bodyStep(Number(shown.pct));
    $('#bodyRead').innerHTML =
      '<span class="br-l">' + (pick ? esc(pick.name) : 'WEAKEST LINK') + '</span>' +
      '<span class="br-n" style="color:' + st.ink + '">' + num(shown.pct) + '%</span>' +
      '<span class="br-s">' + esc(pick ? st.name : shown.name + ' · ' + st.name) +
        ' · ' + num(shown.points) + ' of ' + num(shown.target) + ' pts</span>';

    /* What to actually do about it. Suggesting the region is half an answer;
       the other half is which exercise and how much of it, so each chip is
       priced: enough to move that region ten points from where it is now.
       Tapping one opens the log sheet already set to it. */
    $('#bodyFix').innerHTML = fixFor(shown, weakest);

    /* Balance is the weakest region and nothing else. Averaging would let a
       pair of enormous arms hide a back nobody has trained. */
    var bal = Number(weakest.pct);
    $('#bodyBalance').innerHTML =
      '<div class="balance"><span class="bal-l">BALANCE</span>' +
        '<span class="bal-bar"><span style="width:' + Math.min(100, bal).toFixed(1) +
          '%;background:' +
          bodyStep(bal).col + '"></span></span>' +
        '<span class="bal-n" style="color:' + bodyStep(bal).ink + '">' +
          num(bal) + '%</span></div>' +
      '<p class="hint"><b>100% is one full week of that muscle.</b> A full week ' +
        'for you is <b>' + num(rows.reduce(function (a, m) {
            return a + Number(m.target); }, 0)) + ' points</b> across the ' +
        'fourteen, and it grows with you — with your grade, and with what your ' +
        'own finished weeks actually look like. Over 100 keeps counting. Your ' +
        'balance is the one you have filled least: 40% on all fourteen earns ' +
        'NO WEAK LINK, all fourteen full is FULLY FORGED.</p>';
    $('#muscleCount').textContent = num(bal) + '% WEAKEST';
    $('#bodyList').innerHTML =
      rows.slice().sort(function (a, b) { return Number(b.pct) - Number(a.pct); })
        .map(function (m) {
          var st2 = bodyStep(Number(m.pct));
          var full = Number(m.pct) >= 100;
          return '<button type="button" class="mrow' + (full ? ' full' : '') +
              (state.bodyPick === m.key ? ' on' : '') + '" data-m="' + m.key + '">' +
            '<span class="mr-n">' + esc(m.name) + '</span>' +
            '<span class="mr-bar"><span style="width:' +
              Math.min(100, Number(m.pct)).toFixed(1) +
              '%;background:' + st2.col + '"></span></span>' +
            '<span class="mr-p" style="color:' + st2.ink + '">' + num(m.pct) + '%</span>' +
          '</button>';
        }).join('');
  }

  /* ---- what to do about a lagging region --------------------------------
     A percentage tells you where you are behind and nothing about the way
     out, so each region carries the exercises that actually train it, priced
     in the units you would type in.

     The amount is what it takes to move that region ten points from where it
     is now, which is the whole reason the curve is worth having: the same
     +10 costs a handful of reps on something untouched and hundreds on
     something already full, and the chips say so out loud.

     Gym lifts and team sports are left out. Both are real training and both
     score, but a suggestion has to be something the person reading it can go
     and do, and neither a rack nor a pitch is a given. */
  /* The most of a thing worth printing. These are weekly numbers, not one
     session, so they are generous — but past them the suggestion is
     arithmetic rather than advice and the honest answer is that the region is
     a long way off and something smaller should be picked. */
  var FIX_CAP = { reps: 400, seconds: 900, minutes: 40, km: 20, flat: 6 };

  function fixFor(m, weakest) {
    if (!m) return '';
    /* what is left of this week's dose */
    var need = Math.max(0, Number(m.target) - Number(m.points));
    if (need <= 0) {
      var done = weakest && Number(weakest.pct) < 100 ? weakest : null;
      return '<div class="fix"><span class="fix-l">' +
        esc((m.name || '').toUpperCase()) + ' IS FULL FOR THE WEEK</span>' +
        (done
          ? '<button type="button" class="fix-b wide" data-jump="' + done.key + '">' +
              '<b>' + esc(done.name) + ' is at ' + num(done.pct) + '%</b>' +
              '<i>that is where the week is missing</i></button>'
          : '<span class="fix-none">All fourteen full. Nothing left to even out.</span>') +
      '</div>';
    }

    var picks = EXERCISES.filter(function (e) {
      return e.cat !== 'GYM' && e.cat !== 'SPORT' &&
             (e.muscles[m.key] || 0) >= 0.2 && e.modes.length &&
             e.modes[0].rate > 0;
    }).map(function (e) {
      var mo = e.modes[0];
      var per = mo.rate * e.muscles[m.key];          // region points per unit
      return { ex: e, mode: mo, amount: need / per, share: e.muscles[m.key] };
    }).filter(function (x) {
      return x.amount > 0 && isFinite(x.amount) &&
             x.amount <= (FIX_CAP[x.mode.mode] || 150);
    });

    /* Prefer the ones that get there in a sane amount rather than the ones
       with the biggest share: four hundred crunches is technically the answer
       and nobody is doing it. */
    picks.sort(function (a, b) { return effort(a) - effort(b); });
    picks = picks.slice(0, 3);

    /* Nothing under the caps means the region is a very long way off. Say
       that rather than printing a number nobody will act on. */
    if (!picks.length) {
      var send = weakest && weakest.key !== m.key ? weakest : null;
      return '<div class="fix"><span class="fix-l">' +
        esc((m.name || '').toUpperCase()) + ' NEEDS A WHOLE WEEK OF WORK</span>' +
        (send
          ? '<button type="button" class="fix-b wide" data-jump="' + send.key + '">' +
              '<b>' + esc(send.name) + ' is at ' + num(send.pct) + '%</b>' +
              '<i>start with the emptiest one</i></button>'
          : '<span class="fix-none">Chip away at it — anything counts.</span>') +
      '</div>';
    }

    return '<div class="fix"><span class="fix-l">TO FILL ' +
        esc((m.name || '').toUpperCase()) + ' THIS WEEK</span>' +
      picks.map(function (x) {
        return '<button type="button" class="fix-b" data-fix="' + x.ex.key + '">' +
          '<b>' + esc(x.ex.name) + '</b>' +
          '<i>' + fixAmount(x) + '</i></button>';
      }).join('') + '</div>';
  }

  /* How much of a chore a suggestion is.

     Quantity alone gets this wrong, and wrong in a way that would have
     shipped: the first version answered a neglected lower back with "10 dragon
     flags", which is the fewest reps and also a move most people cannot do
     once. So the amount is weighed twice — by unit, because a rep and a
     minute of holding are not the same errand, and by difficulty, because a
     rep of something priced at six times its neighbours is six times the ask.
     An exercise's own rate is the difficulty: that is what the rate is for. */
  var FIX_UNIT = { reps: 1, seconds: 0.5, minutes: 25, km: 200, flat: 40 };
  var FIX_NORM = null;

  /* The middle rate for each menu group and unit, worked out once. Comparing
     across groups would say a pull-up is harder than a plank, which is true
     and useless: the question is whether this is a hard one OF ITS KIND. */
  function fixNorm(cat, mode) {
    if (!FIX_NORM) {
      var bag = {};
      EXERCISES.forEach(function (e) {
        e.modes.forEach(function (mo) {
          if (!mo.rate) return;
          (bag[e.cat + '|' + mo.mode] = bag[e.cat + '|' + mo.mode] || []).push(mo.rate);
        });
      });
      FIX_NORM = {};
      Object.keys(bag).forEach(function (k) {
        var v = bag[k].sort(function (a, b) { return a - b; });
        FIX_NORM[k] = v[Math.floor(v.length / 2)];
      });
    }
    return FIX_NORM[cat + '|' + mode] || 1;
  }

  function effort(x) {
    var norm = fixNorm(x.ex.cat, x.mode.mode);
    var hard = Math.max(1, x.mode.rate / norm);
    return x.amount * (FIX_UNIT[x.mode.mode] || 1) * hard;
  }

  function fixAmount(x) {
    var a = x.amount, u = x.mode;
    var step = u.mode === 'km' ? 0.5 : u.mode === 'minutes' ? 0.5 : 5;
    var n = Math.max(step, Math.round(a / step) * step);
    var unit = u.short || (u.mode === 'reps' ? 'reps' : u.mode);
    return num(n) + ' ' + unit;
  }

  /* Straight into the log sheet with that exercise already chosen. */
  $('#bodyFix').addEventListener('click', function (e) {
    var j = e.target.closest('[data-jump]');
    if (j) { pickMuscle(j.getAttribute('data-jump')); return; }
    var b = e.target.closest('[data-fix]'); if (!b) return;
    modal.key = b.getAttribute('data-fix');
    openModal();
  });

  /* Tap a plate or a row to hold a region; tap it again to let go. Both
     lists drive the same selection, so pointing at the figure highlights the
     row and the other way round. */
  function pickMuscle(key) {
    state.bodyPick = state.bodyPick === key ? null : key;
    renderBody();
  }
  $('#bodyFig').addEventListener('click', function (e) {
    var t = e.target.closest('[data-m]'); if (!t) return;
    pickMuscle(t.getAttribute('data-m'));
  });
  $('#bodyList').addEventListener('click', function (e) {
    var t = e.target.closest('[data-m]'); if (!t) return;
    pickMuscle(t.getAttribute('data-m'));
  });
  $('#bodyViewPick').addEventListener('click', function (e) {
    var b = e.target.closest('[data-bview]'); if (!b) return;
    state.bodyView = b.getAttribute('data-bview');
    Array.prototype.forEach.call(this.querySelectorAll('button'), function (x) {
      x.classList.toggle('on', x === b);
    });
    /* A region that is not on this side cannot stay selected, or the readout
       would name something the figure is no longer showing. */
    var here = (BODY.masc[state.bodyView] || []).some(function (pl) {
      return pl.m === state.bodyPick; });
    if (!here) state.bodyPick = null;
    renderBody();
  });

  function renderStats(rows) {
    var total = rows.reduce(function (a, r) { return a + Number(r.total_points); }, 0);
    var entries = rows.reduce(function (a, r) { return a + Number(r.entries); }, 0);
    var days = rows.reduce(function (a, r) { return Math.max(a, Number(r.active_days)); }, 0);
    $('#statPoints').textContent = num(total);
    $('#statSub').textContent = entries + (entries === 1 ? ' entry' : ' entries') +
      (days ? ' · ' + days + (days === 1 ? ' active day' : ' active days') : '') +
      (league() ? ' · ' + league().name : '');

    /* share of points per muscle group */
    var byCat = {};
    rows.forEach(function (r) {
      byCat[r.category] = (byCat[r.category] || 0) + Number(r.total_points);
    });
    var cats = CATEGORIES.filter(function (c) { return byCat[c] > 0; });
    $('#statBar').innerHTML = total > 0 ? cats.map(function (c) {
      return '<span style="width:' + (byCat[c] / total * 100).toFixed(2) + '%;background:' +
        CAT_COLOR[c] + '" title="' + c + '"></span>';
    }).join('') : '';
    $('#statLegend').innerHTML = cats.map(function (c) {
      return '<span><i style="background:' + CAT_COLOR[c] + '"></i>' + c +
             ' ' + num(byCat[c]) + '</span>';
    }).join('');

    if (!rows.length) {
      $('#statList').innerHTML = '<div class="empty">Nothing logged ' +
        (state.statsRange === 'all' ? 'yet.' : 'this week yet.') + '</div>';
      return;
    }
    $('#statList').innerHTML = rows.map(function (r) {
      var ex = exercise(r.exercise_key);
      var u = unitFor(r.exercise_key, r.mode);
      return '<div class="srow">' +
        '<span class="sdot" style="background:' + (CAT_COLOR[r.category] || '#666') + '"></span>' +
        '<span class="sname">' + esc(ex ? ex.name : r.exercise_key) +
          '<span class="ssub">' + num(r.total_amount) + ' ' + u.short +
          ' · ' + r.entries + 'x</span></span>' +
        '<span class="spts">' + num(r.total_points) + '</span>' +
      '</div>';
    }).join('');
  }

  /* Duel record is lifetime and does not follow the week/all-time toggle,
     so it says so. Hidden entirely until the first duel has been settled. */
  function renderDuelRecord(rec) {
    var box = $('#duelRecord');
    if (!rec || Number(rec.played) === 0) { box.hidden = true; return; }
    box.hidden = false;
    box.innerHTML =
      '<span class="dr-t">DUELS<i>all time</i></span>' +
      '<span class="dr-c w">' + rec.won + '<i>W</i></span>' +
      '<span class="dr-c l">' + rec.lost + '<i>L</i></span>' +
      '<span class="dr-c d">' + rec.drawn + '<i>D</i></span>' +
      (Number(rec.best_streak) > 1
        ? '<span class="dr-s">best run ' + rec.best_streak + '</span>' : '');
  }

  function renderMilestones(lifetime) {
    var next = null, done = [];
    MILESTONES.forEach(function (m) {
      if (lifetime >= m.at) done.push(m); else if (!next) next = m;
    });
    var last = done.length ? done[done.length - 1] : null;
    var from = last ? last.at : 0;
    var pct = next ? Math.min(100, (lifetime - from) / (next.at - from) * 100) : 100;
    /* The ladder is the point of this tab, so it leads with where you stand
       and how far the next grade is — the full list is there to scroll, not
       to read. Only nearby grades get shown; 25 pips would be noise. */
    var idx = last ? MILESTONES.indexOf(last) : -1;
    var near = MILESTONES.slice(Math.max(0, idx - 1), idx + 4);
    $('#milestones').innerHTML =
      '<div class="ms-hero rank-' + (last ? last.tier : 'i') + '">' +
        '<span class="ms-badge">' +
          iconSvg(!last ? 'star-formation'
                  : last.tier === 'vi' ? 'crown'
                  : last.tier === 'v'  ? 'laurels-trophy'
                  : last.tier === 'iv' ? 'trophy'
                  : 'star-formation') + '</span>' +
        '<span class="ms-id">' +
          '<b>' + (last ? last.name : 'UNRANKED') + '</b>' +
          '<i>' + num(lifetime) + ' lifetime points</i>' +
        '</span>' +
      '</div>' +
      '<div class="ms-bar"><span style="width:' + pct.toFixed(1) + '%"></span></div>' +
      '<p class="ms-next">' + (next
        ? '<b>' + num(Math.max(0, next.at - lifetime)) + '</b> to ' + next.name
        : 'Every grade taken. Nothing left above you.') + '</p>' +
      '<div class="ms-steps">' + near.map(function (m) {
        return '<span class="ms-step' + (lifetime >= m.at ? ' on' : '') +
               (m === next ? ' nxt' : '') + '">' +
               '<b>' + m.name + '</b><i>' + num(m.at) + '</i></span>';
      }).join('') + '</div>' +
      '<details class="ms-all"><summary>All ' + MILESTONES.length + ' grades</summary>' +
        '<div class="ms-pips">' + MILESTONES.map(function (m) {
          return '<span class="ms-pip' + (lifetime >= m.at ? ' on' : '') + '">' +
                 '<i>' + num(m.at) + '</i>' + m.name + '</span>';
        }).join('') + '</div></details>';
  }

  $('#statsRange').addEventListener('click', function (e) {
    var b = e.target.closest('[data-range]'); if (!b) return;
    state.statsRange = b.getAttribute('data-range');
    Array.prototype.forEach.call(this.querySelectorAll('button'), function (x) {
      x.classList.toggle('on', x === b);
    });
    loadStats();
  });

  /* ===================================================================
     11c. Light / dark
     =================================================================== */
  function applyTheme(pref) {
    var root = document.documentElement;
    if (pref === 'auto') root.removeAttribute('data-theme');
    else root.setAttribute('data-theme', pref);
    try { localStorage.setItem('ironleague.theme', pref); } catch (e) {}
    var dark = pref === 'dark' || (pref === 'auto' &&
      !window.matchMedia('(prefers-color-scheme: light)').matches);
    var meta = document.querySelector('meta[name="theme-color"]');
    if (meta) meta.setAttribute('content', dark ? '#08090c' : '#f4f5f7');
    Array.prototype.forEach.call(document.querySelectorAll('#themeRow button'), function (b) {
      b.classList.toggle('on', b.getAttribute('data-theme') === pref);
    });
  }
  function savedTheme() {
    try { return localStorage.getItem('ironleague.theme') || 'dark'; } catch (e) { return 'dark'; }
  }
  /* A short list of animals, because the pin is a garnish, not a second
     identity. Whatever someone picked before still shows even if it is not
     on this list. */
  var PIN_EMOJI = ('🦍🦊🐺🦁🐯🐻🐼🦅🦉🐗🦈🐍🦂🐢🐊🦖🐙🦑🐝🦋🐉🐴🦌🐘🦏🐆🦇🐸🐬🦭')
    .match(/\p{Extended_Pictographic}\uFE0F?/gu) || [];

  function myAvatar() {
    return parseAvatar(state.profile && state.profile.avatar);
  }

  /* The emblem grids are ~110 inline SVGs. Building them on every render of
     the settings tab cost enough DOM work to be felt, so they are built the
     first time the section is opened and only patched for selection after. */
  var avGridsBuilt = false;

  function paintAvatarPreview() {
    var a = myAvatar();
    $('#avPreview').innerHTML = avatarHtml(buildAvatar(a), { size: 'lg' });
  }

  function markAvatarSelection() {
    var a = myAvatar();
    function mark(sel, attr, val) {
      Array.prototype.forEach.call(document.querySelectorAll(sel), function (b) {
        b.classList.toggle('on', b.getAttribute(attr) === val);
      });
    }
    mark('#avatarGrid [data-icon]', 'data-icon', a.icon);
    mark('#avColors [data-color]', 'data-color', a.color || 'clear');
    mark('#avPins [data-pin]', 'data-pin', a.emoji || '');
  }

  function buildAvatarGrid() {
    paintAvatarPreview();
    if (!avGridsBuilt) return;      // grids not on screen yet; nothing to mark
    markAvatarSelection();
  }

  function buildAvatarGridsOnce() {
    if (avGridsBuilt) return;
    avGridsBuilt = true;
    $('#avatarGrid').innerHTML = GI_AVATARS.map(function (n) {
      return '<button type="button" class="av-opt" data-icon="' + n +
             '" aria-label="' + esc(n.replace(/-/g, ' ')) + '">' + iconSvg(n) + '</button>';
    }).join('');
    $('#avColors').innerHTML = AV_COLORS.map(function (c) {
      return '<button type="button" class="sw' + (c.key === 'clear' ? ' sw-clear' : '') +
             '" data-color="' + c.key + '" style="--sw:' + c.css + '" aria-label="' + c.name +
             '"><span></span><small>' + c.name + '</small></button>';
    }).join('');
    $('#avPins').innerHTML = PIN_EMOJI.map(function (e) {
      return '<button type="button" class="av-opt" data-pin="' + e + '">' + e + '</button>';
    }).join('');
    markAvatarSelection();
    showAvPane(myAvatar().emoji ? 'emoji' : 'icon');
  }

  function showAvPane(kind) {
    $('#avIconPane').hidden = kind !== 'icon';
    $('#avEmojiPane').hidden = kind === 'icon';
    Array.prototype.forEach.call($('#avKind').querySelectorAll('button'), function (b) {
      b.classList.toggle('on', b.getAttribute('data-kind') === kind);
    });
  }

  $('#avKind').addEventListener('click', function (e) {
    var b = e.target.closest('[data-kind]'); if (!b) return;
    showAvPane(b.getAttribute('data-kind'));
  });

  async function saveAvatar(v) {
    try {
      var r = await sb.rpc('set_avatar', { p_avatar: v });
      if (r.error) throw r.error;
      state.profile = r.data;
      paintAvatarPreview();
      markAvatarSelection();
      await refreshAll();
    } catch (e) { toast(niceError(e), true); }
  }

  /* Picking one mark clears the other — an avatar is never both. */
  $('#avatarGrid').addEventListener('click', function (e) {
    var b = e.target.closest('[data-icon]'); if (!b) return;
    var a = myAvatar();
    saveAvatar(buildAvatar({ icon: b.getAttribute('data-icon'), color: a.color }));
  });
  $('#avColors').addEventListener('click', function (e) {
    var b = e.target.closest('[data-color]'); if (!b) return;
    var a = myAvatar();
    if (!a.icon) { toast('Pick an emblem first'); return; }
    saveAvatar(buildAvatar({ icon: a.icon, color: b.getAttribute('data-color') }));
  });
  $('#avPins').addEventListener('click', function (e) {
    var b = e.target.closest('[data-pin]'); if (!b) return;
    saveAvatar(b.getAttribute('data-pin'));      // an animal replaces the emblem
  });
  $('#avatarClear').addEventListener('click', function () { saveAvatar(''); });
  /* Built on first open, so the settings tab does not pay for ~110 inline
     SVGs every time it renders. */
  $('#sectEmblem').addEventListener('toggle', function () {
    if (this.open) buildAvatarGridsOnce();
  });
  $('#weekBox').addEventListener('toggle', function () { state.weekBoxTouched = true; });
  $('#sectBadges').addEventListener('toggle', function () { if (this.open) loadBadges(); });

  /* ---- name colours ---------------------------------------------------- */
  function paintNameColors() {
    var mine = (state.profile && state.profile.name_color) || '';
    $('#nameColors').innerHTML = NAME_COLORS.map(function (c) {
      return '<button type="button" class="nc' + (c.key === mine ? ' on' : '') +
             '" data-nc="' + c.key + '" style="--nc:' + c.css + '">' + c.name + '</button>';
    }).join('');
    var l = league(), owner = l && state.profile && l.owner_id === state.profile.id;
    $('#leagueTextBox').hidden = !owner;
    if (owner) {
      var cur = (l.badge && l.badge.text) || '';
      $('#lgNameColors').innerHTML = NAME_COLORS.map(function (c) {
        return '<button type="button" class="nc' + (c.key === cur ? ' on' : '') +
               '" data-lnc="' + c.key + '" style="--nc:' + c.css + '">' + c.name + '</button>';
      }).join('');
    }
  }
  $('#nameColors').addEventListener('click', async function (e) {
    var b = e.target.closest('[data-nc]'); if (!b) return;
    try {
      var r = await sb.rpc('set_name_color', { p_color: b.getAttribute('data-nc') });
      if (r.error) throw r.error;
      state.profile = r.data; paintNameColors(); await refreshAll();
    } catch (err) { toast(niceError(err), true); }
  });
  $('#lgNameColors').addEventListener('click', function (e) {
    var b = e.target.closest('[data-lnc]'); if (!b) return;
    saveBadge({ text: b.getAttribute('data-lnc') });
  });

  /* The three CHANGE buttons under "This league's identity" used to be
     placeholders that did nothing at all. Everything they promised now lives
     in the shop, so they open it at the right shelf instead of duplicating
     three pickers in two places. */
  document.addEventListener('click', function (e) {
    var b = e.target.closest('[data-goshop]'); if (!b) return;
    var shop = $('#sectShop'); if (!shop) return;
    shop.open = true;
    var head = document.getElementById(b.getAttribute('data-goshop'));
    if (head && head.scrollIntoView) {
      head.scrollIntoView({ behavior: 'smooth', block: 'start' });
    }
  });

  /* Two separate sets on purpose: a league is a place and a player is a
     person, so they should not be able to wear the same thing. Leagues get
     the wide, architectural looks; players get the tighter, personal ones. */
  var LEAGUE_SKINS = [
    { key: 'standard',  name: 'STANDARD' },  { key: 'stadium',   name: 'STADIUM' },
    { key: 'goldrush',  name: 'GOLD RUSH' }, { key: 'neon',      name: 'NEON ARENA' },
    { key: 'tactical',  name: 'OVERDRIVE' }, { key: 'varsity',   name: 'VARSITY' },
    { key: 'holo',      name: 'PRISM' },     { key: 'luxury',    name: 'DOMINION' },
    { key: 'jungle',    name: 'JUNGLE' },    { key: 'sandstorm', name: 'SANDSTORM' },
    { key: 'abyss',     name: 'ABYSS' },     { key: 'circuit',   name: 'CIRCUIT' },
    { key: 'aurora',    name: 'AURORA' },    { key: 'magma',     name: 'MAGMA' },
    { key: 'steelwork', name: 'STEELWORK' }, { key: 'nightops',  name: 'NIGHT OPS' }
  ];
  var PLAYER_SKINS = [
    { key: 'carbon',    name: 'CARBON' },    { key: 'ember',     name: 'INFERNO' },
    { key: 'frost',     name: 'FROSTBITE' }, { key: 'velocity',  name: 'VELOCITY' },
    { key: 'blueprint', name: 'BLUEPRINT' }, { key: 'grunge',    name: 'GLITCH' },
    { key: 'obsidian',  name: 'OBSIDIAN' },  { key: 'inverted',  name: 'CLEAN SLATE' },
    { key: 'venom',     name: 'VENOM' },     { key: 'bloodline', name: 'BLOODLINE' },
    { key: 'glacier',   name: 'GLACIER' },   { key: 'brass',     name: 'BRASS' },
    { key: 'static',    name: 'STATIC' },    { key: 'orchid',    name: 'ORCHID' },
    { key: 'moss',      name: 'MOSS' },      { key: 'ash',       name: 'ASH' }
  ];
  var ALL_SKINS = LEAGUE_SKINS.concat(PLAYER_SKINS);
  function skinName(key) {
    for (var i = 0; i < ALL_SKINS.length; i++) {
      if (ALL_SKINS[i].key === key) return ALL_SKINS[i].name;
    }
    return null;
  }
  function skinClass(key) {
    for (var i = 0; i < ALL_SKINS.length; i++) if (ALL_SKINS[i].key === key) return 'sk-' + key;
    return '';
  }

  /* ---- shop -------------------------------------------------------------
     The cosmetics that already exist, shown as things you can pick rather
     than buried in a picker. Everything is free for now; the price slot is
     there so it has somewhere to go later. */
  /* Every banner is CSS, not a picture: nothing to download, it re-themes
     with the app, and the motion is part of the artwork. Names match the
     mockups so we can talk about them. */
  /* Two separate sets on purpose: a league is a place and a player is a
     person, so they should not be able to wear the same thing. Leagues get
     the wide, architectural looks; players get the tighter, personal ones. */
  /* Every crest control changes one field and saves the whole badge, so a
     partial write can never drop the other three. Ownership is checked on the
     server too — this is the button, not the rule. */
  async function saveBadge(patch) {
    var l = league();
    if (!l || !state.profile || l.owner_id !== state.profile.id) {
      toast('Only the person who created the league can change its crest'); return;
    }
    var cur = badgeOf(l);
    var badge = {
      shape:  patch.shape  !== undefined ? patch.shape  : cur.shape,
      color:  patch.color  !== undefined ? patch.color  : cur.color,
      emblem: patch.emblem !== undefined ? patch.emblem : cur.emblem,
      skin:   patch.skin   !== undefined ? patch.skin   : ((l.badge && l.badge.skin) || null),
      text:   patch.text   !== undefined ? patch.text   : ((l.badge && l.badge.text) || null)
    };
    try {
      var r = await sb.rpc('set_league_badge', { p_league: l.id, p_badge: badge });
      if (r.error) throw r.error;
      await loadLeagues();
      buildShop(); renderMe(); renderHeader();
      toast('League crest updated');
    } catch (err) { toast(niceError(err), true); }
  }

  function buildShop() {
    var l = league();
    var cur = badgeOf(l);
    $('#shopCrests').innerHTML = GI_CRESTS.map(function (n) {
      return '<button type="button" class="shop-c' + (n === cur.emblem ? ' on' : '') +
             '" data-crest="' + n + '" aria-label="' + esc(n.replace(/-/g, ' ')) + '">' +
             iconSvg(n) + '<small>FREE</small></button>';
    }).join('');

    /* Shape and colour were the two CHANGE buttons that never did anything.
       They are drawn as the badge you would actually get, not named in a
       dropdown, because nobody knows what a "heater" is. */
    $('#shopShapes').innerHTML = BADGE_SHAPES.map(function (k) {
      return '<button type="button" class="shop-c shop-badge' +
             (k === cur.shape ? ' on' : '') + '" data-cshape="' + k + '" ' +
             'aria-label="' + esc(k) + ' shape">' +
             badgeHtml({ shape: k, color: cur.color, emblem: cur.emblem }, 'md') +
             '</button>';
    }).join('');
    $('#shopMetals').innerHTML = BADGE_COLORS.map(function (c) {
      return '<button type="button" class="shop-c shop-badge' +
             (c.key === cur.color ? ' on' : '') + '" data-cmetal="' + c.key + '" ' +
             'aria-label="' + esc(c.key) + '">' +
             badgeHtml({ shape: cur.shape, color: c.key, emblem: cur.emblem }, 'md') +
             '</button>';
    }).join('');
    function skins(target, cls, set, chosen) {
      $(target).innerHTML = set.map(function (b) {
        return '<button type="button" class="' + cls + ' skin sk-' + b.key +
               (b.key === chosen ? ' on' : '') +
               '" data-skin="' + b.key + '"><span>' + b.name + '</span>' +
               '<small>FREE</small></button>';
      }).join('');
    }
    skins('#shopBanners', 'shop-b', LEAGUE_SKINS, l && l.badge && l.badge.skin);
    skins('#shopPlayer', 'shop-b shop-b-sm', PLAYER_SKINS,
          state.profile && state.profile.banner);
  }

  $('#shopCrests').addEventListener('click', function (e) {
    var b = e.target.closest('[data-crest]'); if (!b) return;
    saveBadge({ emblem: b.getAttribute('data-crest') });
  });
  $('#shopShapes').addEventListener('click', function (e) {
    var b = e.target.closest('[data-cshape]'); if (!b) return;
    saveBadge({ shape: b.getAttribute('data-cshape') });
  });
  $('#shopMetals').addEventListener('click', function (e) {
    var b = e.target.closest('[data-cmetal]'); if (!b) return;
    saveBadge({ color: b.getAttribute('data-cmetal') });
  });
  $('#shopBanners').addEventListener('click', function (e) {
    var b = e.target.closest('[data-skin]'); if (!b) return;
    saveBadge({ skin: b.getAttribute('data-skin') });
  });

  $('#shopPlayer').addEventListener('click', async function (e) {
    var b = e.target.closest('[data-skin]'); if (!b) return;
    try {
      var r = await sb.rpc('set_banner', { p_banner: b.getAttribute('data-skin') });
      if (r.error) throw r.error;
      state.profile = r.data;
      buildShop(); await refreshAll();
      toast('Your banner is set');
    } catch (err) { toast(niceError(err), true); }
  });

  /* Kilos or pounds. Storage is always kilos; this only changes what is typed
     and what the labels say, so two people in one league can read weights
     differently without touching each other's scores. */
  var LB = 2.20462;
  function useLb() { return state.profile && state.profile.units === 'lb'; }
  function toKg(v) { return useLb() ? v / LB : v; }
  function fromKg(v) { return useLb() ? v * LB : v; }
  function unitName() { return useLb() ? 'LB' : 'KG'; }

  function paintUnits() {
    Array.prototype.forEach.call($('#unitRow').querySelectorAll('button'), function (b) {
      b.classList.toggle('on', b.getAttribute('data-units') === (useLb() ? 'lb' : 'kg'));
    });
  }
  $('#unitRow').addEventListener('click', async function (e) {
    var b = e.target.closest('[data-units]'); if (!b) return;
    try {
      var r = await sb.rpc('set_units', { p_units: b.getAttribute('data-units') });
      if (r.error) throw r.error;
      state.profile = r.data;
      paintUnits(); renderMe();
      toast('Weights now in ' + unitName().toLowerCase());
    } catch (err) { toast(niceError(err), true); }
  });

  function paintForm() {
    /* 'neutral' is what everybody was stored as before there were two real
       bodies to choose between. It draws the male one, so that is the button
       that lights up until they pick. */
    var f = (state.profile && state.profile.body_form) || 'masc';
    if (f === 'neutral') f = 'masc';
    Array.prototype.forEach.call($('#formRow').querySelectorAll('button'), function (b) {
      b.classList.toggle('on', b.getAttribute('data-form') === f);
    });
  }
  $('#formRow').addEventListener('click', async function (e) {
    var b = e.target.closest('[data-form]'); if (!b) return;
    try {
      var r = await sb.rpc('set_body_form', { p_form: b.getAttribute('data-form') });
      if (r.error) throw r.error;
      state.profile = r.data;
      paintForm(); renderBody();
    } catch (err) { toast(niceError(err), true); }
  });

  /* ---- rules a league creator owns -------------------------------------- */
  var DOW = [[1,'MON'],[2,'TUE'],[3,'WED'],[4,'THU'],[5,'FRI'],[6,'SAT'],[7,'SUN']];


  function paintLeagueRules() {
    var l = league(), mine = l && state.profile && l.owner_id === state.profile.id;
    $('#leagueOwnerBox').hidden = !mine;
    $('#deleteLeagueBtn').hidden = !mine;
    if (!l) return;
    var rest = l.rest_dow || [7];
    $('#restDows').innerHTML = DOW.map(function (d) {
      return '<button type="button" class="dow' + (rest.indexOf(d[0]) >= 0 ? ' on' : '') +
             '" data-dow="' + d[0] + '"' + (mine ? '' : ' disabled') + '>' + d[1] + '</button>';
    }).join('');
    /* One day, or none. A rest day closes the league, so it cannot also be
       the day that being behind is worth something. */
    $('#catchDows').innerHTML = DOW.map(function (d) {
      var off = rest.indexOf(d[0]) >= 0;
      return '<button type="button" class="dow catch' +
             (l.catchup_dow === d[0] ? ' on' : '') + (off ? ' off' : '') +
             '" data-catch="' + d[0] + '"' + (mine && !off ? '' : ' disabled') + '>' +
             d[1] + '</button>';
    }).join('');
    $('#seasonWeeks').value = l.season_weeks == null ? '' : String(l.season_weeks);
    if (mine) ruleState(rulesSummary());
    $('#leaveNote').textContent = mine
      ? 'Leaving keeps the league alive for everyone else. Deleting removes it for everybody.'
      : 'Your logs stay in every other league you are in.';
  }

  $('#restDows').addEventListener('click', function (e) {
    var b = e.target.closest('[data-dow]'); if (!b || b.disabled) return;
    b.classList.toggle('on');
    paintCatchAvailability();
    saveRules();
  });

  /* Tapping a catch-up day picks it; tapping the one already on clears it. */
  $('#catchDows').addEventListener('click', function (e) {
    var b = e.target.closest('[data-catch]'); if (!b || b.disabled) return;
    var was = b.classList.contains('on');
    Array.prototype.forEach.call($('#catchDows').querySelectorAll('.dow'),
      function (x) { x.classList.remove('on'); });
    if (!was) b.classList.add('on');
    saveRules();
  });

  /* A day cannot be both, so the rest days grey themselves out of the
     catch-up row as they are picked. */
  function paintCatchAvailability() {
    var rest = Array.prototype.map.call($('#restDows').querySelectorAll('.dow.on'),
      function (b) { return b.getAttribute('data-dow'); });
    Array.prototype.forEach.call($('#catchDows').querySelectorAll('.dow'), function (b) {
      var off = rest.indexOf(b.getAttribute('data-catch')) >= 0;
      b.classList.toggle('off', off);
      b.disabled = off;
      if (off) b.classList.remove('on');
    });
  }

  /* These used to need a SAVE button that sat three blocks lower, next to the
     season-length dropdown, where it read as part of that dropdown. So people
     tapped a rest day, tapped a catch-up day, closed the app and found both
     gone — the changes had never been sent. A toggle that looks like it did
     something has to have done something, so every control here saves itself.
     Debounced, because picking three rest days is three taps and one save. */
  var rulesTimer = null;

  function ruleState(text, bad) {
    var el = $('#rulesState'); if (!el) return;
    el.textContent = text;
    el.classList.toggle('bad', !!bad);
  }

  function saveRules() {
    if (!state.leagueId) return;
    var l = league();
    if (!l || !state.profile || l.owner_id !== state.profile.id) return;
    ruleState('Saving…');
    clearTimeout(rulesTimer);
    rulesTimer = setTimeout(async function () {
      var dows = Array.prototype.map.call($('#restDows').querySelectorAll('.dow.on'),
        function (b) { return Number(b.getAttribute('data-dow')); });
      var picked = $('#catchDows').querySelector('.dow.on');
      var wk = $('#seasonWeeks').value;
      try {
        var r = await sb.rpc('set_league_settings', {
          p_league: state.leagueId, p_rest_dow: dows,
          p_catchup_dow: picked ? Number(picked.getAttribute('data-catch')) : null,
          p_season_weeks: wk === '' ? null : Number(wk)
        });
        if (r.error) throw r.error;
        await loadLeagues();
        ruleState(rulesSummary());
      } catch (err) {
        ruleState(niceError(err), true);
        /* Put the buttons back to what the league actually says, so the screen
           never shows a rule that is not in force. */
        paintLeagueRules();
      }
    }, 500);
  }

  /* What is actually in force, in words, under the buttons. */
  function rulesSummary() {
    var l = league(); if (!l) return '';
    var name = function (n) {
      for (var i = 0; i < DOW.length; i++) if (DOW[i][0] === n) return DOW[i][1];
      return '?';
    };
    var rest = (l.rest_dow || []).map(name);
    return 'Saved · rest ' + (rest.length ? rest.join(' + ') : 'none') +
      ' · catch-up ' + (l.catchup_dow ? name(l.catchup_dow) : 'none') +
      ' · season ' + (l.season_weeks ? l.season_weeks + ' weeks' : 'no end');
  }

  $('#seasonWeeks').addEventListener('change', saveRules);

  $('#leaveBtn').addEventListener('click', async function () {
    var l = league(); if (!l) return;
    if (!confirm('Leave ' + l.name + '? Your logs in your other leagues are untouched.')) return;
    try {
      var r = await sb.rpc('leave_league', { p_league: l.id });
      if (r.error) throw r.error;
      await loadLeagues();
      state.leagueId = state.leagues.length ? state.leagues[0].id : null;
      await refreshAll();
      toast('You left ' + l.name);
    } catch (err) { toast(niceError(err), true); }
  });

  $('#deleteLeagueBtn').addEventListener('click', async function () {
    var l = league(); if (!l) return;
    if (!confirm('Delete ' + l.name + ' for everyone? This cannot be undone.\n\n' +
                 'Each workout still counts in the other leagues it was logged into.')) return;
    if (!confirm('Really delete ' + l.name + '? Type-free final check.')) return;
    try {
      var r = await sb.rpc('delete_league', { p_league: l.id });
      if (r.error) throw r.error;
      await loadLeagues();
      state.leagueId = state.leagues.length ? state.leagues[0].id : null;
      await refreshAll();
      toast(l.name + ' deleted');
    } catch (err) { toast(niceError(err), true); }
  });

  $('#bwSave').addEventListener('click', async function () {
    var v = toKg(parseFloat($('#bwProfile').value));
    try {
      var r = await sb.rpc('set_bodyweight', { p_kg: isFinite(v) ? v : null });
      if (r.error) throw r.error;
      state.profile = r.data;
      toast(isFinite(v) ? 'Bodyweight saved' : 'Bodyweight cleared');
    } catch (e) { toast(niceError(e), true); }
  });

  $('#themeRow').addEventListener('click', function (e) {
    var b = e.target.closest('[data-theme]'); if (!b) return;
    applyTheme(b.getAttribute('data-theme'));
  });
  applyTheme(savedTheme());

  /* ===================================================================
     12. Tabs
     =================================================================== */
  function switchView(v) {
    state.view = v;
    /* Driven off the DOM, so adding a section cannot leave it permanently
       hidden because this list was not updated. */
    Array.prototype.forEach.call(document.querySelectorAll('.view'), function (sec) {
      sec.hidden = sec.id !== 'view-' + v;
    });
    Array.prototype.forEach.call(document.querySelectorAll('.tab'), function (t) {
      t.classList.toggle('is-active', t.getAttribute('data-view') === v);
    });
    if (v === 'me') renderMe();
    if (v === 'stats') { loadStats(); if ($('#sectBadges').open) loadBadges(); }
    if (v === 'duel') { loadDuels(); loadRivalries(); }
    window.scrollTo(0, 0);
  }
  Array.prototype.forEach.call(document.querySelectorAll('.tab'), function (t) {
    t.addEventListener('click', function () { switchView(t.getAttribute('data-view')); });
  });
  $('#leagueBtn').addEventListener('click', function () { switchView('me'); });
  $('#refreshBtn').addEventListener('click', async function () {
    var b = $('#refreshBtn'); b.classList.add('spin');
    state.feeds = {};
    await loadLeagues();
    await refreshAll();
    Object.keys(state.open).forEach(function (id) { if (state.open[id]) loadFeed(id); });
    if (state.view === 'stats') { await loadStats(); await loadBadges(); }
    if (state.view === 'duel') { await loadDuels(); await loadRivalries(); }
    b.classList.remove('spin');
    toast('Refreshed');
  });

  /* ===================================================================
     13. Log workout modal
     =================================================================== */
  var modal = { key: null, mode: null, editing: null };

  function buildExerciseSelect() {
    modal.restOnly = isRestDay();
    renderExList('');
    selectExercise(modal.restOnly ? 'stretch' : 'pushups');
    closeFinder();
  }

  /* Search across name, category and a hand-written alias list, so "abs",
     "erg" and "soccer" all land somewhere sensible. */
  function exMatches(q) {
    var pool = EXERCISES.filter(function (e) {
      return !modal.restOnly || e.cat === 'RECOVERY';
    });
    q = q.trim().toLowerCase();
    if (!q) return pool;
    return pool.filter(function (e) {
      return (e.name + ' ' + e.cat + ' ' + e.aliases).toLowerCase().indexOf(q) >= 0;
    });
  }

  function renderExList(q) {
    var hits = exMatches(q), box = $('#exList');
    if (!hits.length) { box.innerHTML = '<p class="muted small pad8">Nothing matches.</p>'; return; }
    /* Groups stay shut until you open one, so browsing by muscle group is a
       short list of headings rather than a hundred-line scroll. Typing opens
       everything, because then the list is already short. */
    var searching = !!q.trim();
    var shown = CATEGORIES.filter(function (cat) {
      return hits.some(function (e) { return e.cat === cat; });
    });
    /* One group and nothing else — a rest day, say — has nothing to collapse,
       so leave it open rather than showing a heading with a hidden list. */
    var alone = shown.length === 1;
    var html = '';
    shown.forEach(function (cat) {
      var list = hits.filter(function (e) { return e.cat === cat; });
      var open = searching || alone || state.exOpen === cat;
      html += '<div class="exl-g' + (open ? ' open' : '') + '">' +
        '<button type="button" class="exl-h" data-cat="' + cat + '">' +
          iconSvg(GI_CAT[cat]) + '<span>' + cat + '</span>' +
          '<span class="exl-c">' + list.length + '</span>' +
          '<span class="exl-x">' + (open ? '▴' : '▾') + '</span>' +
        '</button>' +
        (open ? list.map(function (e) {
          var m = e.modes[0];
          return '<button type="button" class="exl-i' +
            (e.key === modal.key ? ' on' : '') + '" data-ex="' + e.key + '" role="option">' +
            '<span class="exl-n">' + esc(e.name) + '</span>' +
            '<span class="exl-r">' + esc(m.label) + '</span></button>';
        }).join('') : '') +
      '</div>';
    });
    box.innerHTML = html;
  }

  function openFinder() {
    $('#exFinder').hidden = false;
    $('#exSearch').value = '';
    var cur = exercise(modal.key);
    state.exOpen = cur ? cur.cat : null;      // start where you already are
    renderExList('');
    $('#exSearch').focus();
  }
  function closeFinder() { $('#exFinder').hidden = true; }

  function selectExercise(key) {
    var ex = exercise(key);
    if (!ex) return;
    modal.key = key;
    modal.mode = ex.modes[0].mode;
    $('#exPickName').textContent = ex.name;
    $('#exPickIcon').innerHTML = iconSvg(GI_CAT[ex.cat]);
    $('#gymRow').hidden = ex.cat !== 'GYM';
    if (ex.cat === 'GYM') {
      $('#bwLbl').textContent = 'YOUR BODYWEIGHT (' + unitName() + ')';
      $('#loadLbl').textContent = 'WEIGHT LIFTED (' + unitName() + ')';
      if (!$('#bwInput').value && state.profile && state.profile.bodyweight) {
        $('#bwInput').value = Math.round(fromKg(state.profile.bodyweight) * 10) / 10;
      }
    }
    var row = $('#modeRow');
    if (ex.modes.length > 1) {
      row.hidden = false;
      row.innerHTML = ex.modes.map(function (m, i) {
        return '<button type="button" data-mode="' + m.mode + '" class="' + (i === 0 ? 'on' : '') + '">' +
          (m.mode === 'reps' ? 'REPS' : UNITS[m.mode].label) + '</button>';
      }).join('');
    } else {
      row.hidden = true; row.innerHTML = '';
    }
    applyMode();
  }

  /* A mode may override how its unit is named and stepped
     (a "sprint" is a rep, but nobody calls it that). */
  function unitFor(key, mode) {
    var base = UNITS[mode] || {};
    var ex = exercise(key);
    var m = ex && ex.modes.filter(function (x) { return x.mode === mode; })[0];
    return {
      label: (m && m.unitLabel) || base.label,
      short: (m && m.short) || base.short,
      step:  (m && m.step)  || base.step,
      def:   (m && m.def)   || base.def,
      quick: (m && m.quick) || base.quick
    };
  }

  function applyMode() {
    var u = unitFor(modal.key, modal.mode);
    $('#amountLbl').textContent = u.label;
    $('#amountInput').value = u.def;
    $('#amountInput').step = u.step;
    var ex = exercise(modal.key);
    var m = ex.modes.filter(function (x) { return x.mode === modal.mode; })[0];
    $('#quickRow').innerHTML = u.quick.map(function (q) {
      return '<button type="button" data-q="' + q + '">' + q + ' ' + u.short + '</button>';
    }).join('');
    $('#exVariants').textContent = ex.cat + ' · ' + ex.variants;
    $('#rateLbl').textContent = m ? m.label : '';
    updatePreview();
  }

  function gymInputs() {
    return { bw: toKg(parseFloat($('#bwInput').value)),
             load: toKg(parseFloat($('#loadInput').value)) };
  }
  function updatePreview() {
    var v = parseFloat($('#amountInput').value);
    if (!isFinite(v) || v < 0) v = 0;
    var g = gymInputs();
    $('#ptsPreview').textContent = num(pointsFor(modal.key, modal.mode, v, g.bw, g.load));
  }

  $('#exPick').addEventListener('click', function () {
    if ($('#exFinder').hidden) { openFinder(); } else { closeFinder(); }
  });
  $('#exSearch').addEventListener('input', function () { renderExList(this.value); });
  $('#exList').addEventListener('click', function (e) {
    var h = e.target.closest('[data-cat]');
    if (h) {
      var cat = h.getAttribute('data-cat');
      state.exOpen = state.exOpen === cat ? null : cat;
      renderExList($('#exSearch').value);
      return;
    }
    var b = e.target.closest('[data-ex]'); if (!b) return;
    selectExercise(b.getAttribute('data-ex'));
    closeFinder();
  });
  $('#bwInput').addEventListener('input', updatePreview);
  $('#loadInput').addEventListener('input', updatePreview);
  $('#modeRow').addEventListener('click', function (e) {
    var b = e.target.closest('[data-mode]'); if (!b) return;
    modal.mode = b.getAttribute('data-mode');
    Array.prototype.forEach.call(this.querySelectorAll('button'), function (x) {
      x.classList.toggle('on', x === b);
    });
    applyMode();
  });
  $('#quickRow').addEventListener('click', function (e) {
    var b = e.target.closest('[data-q]'); if (!b) return;
    $('#amountInput').value = b.getAttribute('data-q');
    updatePreview();
  });
  $('#amountInput').addEventListener('input', updatePreview);
  $('#plusBtn').addEventListener('click', function () { bump(1); });
  $('#minusBtn').addEventListener('click', function () { bump(-1); });
  function bump(dir) {
    var u = unitFor(modal.key, modal.mode);
    var v = parseFloat($('#amountInput').value); if (!isFinite(v)) v = 0;
    v = Math.max(0, Math.round((v + dir * u.step) * 100) / 100);
    $('#amountInput').value = v;
    updatePreview();
  }

  function findEntry(id) {
    for (var pid in state.feeds) {
      var rows = state.feeds[pid] || [];
      for (var i = 0; i < rows.length; i++) if (rows[i].id === id) return rows[i];
    }
    return null;
  }

  /* Correcting an entry. Only possible while its week is still running —
     the database refuses anything older, so this can never rewrite history. */
  function openEdit(id) {
    var w = findEntry(id);
    if (!w) { toast('Entry not found — refresh and try again.', true); return; }
    selectExercise(w.exercise_key);
    modal.mode = w.mode;
    var row = $('#modeRow');
    if (!row.hidden) {
      Array.prototype.forEach.call(row.querySelectorAll('button'), function (b) {
        b.classList.toggle('on', b.getAttribute('data-mode') === w.mode);
      });
    }
    applyMode();
    $('#amountInput').value = Number(w.amount);
    updatePreview();

    modal.editing = id;
    state.session = [];
    $('#sessionBox').hidden = true;
    $('#modalErr').textContent = '';
    $('#sheetTitle').textContent = 'EDIT ENTRY';
    $('#addBtn').textContent = 'SAVE CHANGES';
    $('#cancelEditBtn').hidden = false;
    document.querySelector('.sheet-foot').hidden = true;   // CANCEL EDIT replaces DONE
    $('#logModal').hidden = false;
    document.body.style.overflow = 'hidden';
    document.body.classList.add('modal-open');
  }

  /* On a rest day, has this athlete already taken their one session? */
  async function loadRestState() {
    state.restDone = false;
    if (!isRestDay() || !state.leagueId || !state.profile) return;
    var r = await sb.from('workouts').select('created_at')
      .eq('league_id', state.leagueId)
      .eq('profile_id', state.profile.id)
      .eq('week_start', state.week);
    if (r.error || !r.data) return;
    var today = iso(wallNow());
    state.restDone = r.data.some(function (w) {
      return iso(new Date(new Date(w.created_at).getTime() +
             tzOffsetMs(new Date(w.created_at), TZ))) === today;
    });
  }

  function applyMultiLeagueNote() {
    var n = state.leagues.length;
    var el = $('#multiNote');
    el.hidden = n < 2;
    if (n >= 2) {
      el.innerHTML = 'You are in <b>' + n + ' leagues</b>. Log once — this entry ' +
                     'counts in all of them.';
    }
  }

  function applyRestMode() {
    var rest = isRestDay();
    $('#restNote').hidden = !rest;
    if (!rest) { $('#addBtn').disabled = false; return; }
    $('#restNote').innerHTML = state.restDone
      ? '<b>Recovery already logged.</b> That is your one session for today — ' +
        'the league opens again on Monday.'
      : '<b>Rest day.</b> The league closed on Saturday night. One stretching ' +
        'session is all that counts today, and only once.';
    $('#addBtn').disabled = state.restDone;
  }

  function openModal() {
    if (!state.leagueId) { toast('Join or create a league first.', true); switchView('me'); return; }
    modal.editing = null;
    $('#sheetTitle').textContent = 'LOG WORKOUT';
    $('#addBtn').textContent = 'ADD';
    $('#cancelEditBtn').hidden = true;
    document.querySelector('.sheet-foot').hidden = false;
    state.session = [];
    $('#sessionBox').hidden = true;
    $('#sessionList').innerHTML = '';
    $('#sessionTotal').textContent = '0';
    $('#modalErr').textContent = '';
    selectExercise(modal.key || EXERCISES[0].key);
    $('#logModal').hidden = false;
    document.body.style.overflow = 'hidden';
    document.body.classList.add('modal-open');
    applyMultiLeagueNote();
    loadRestState().then(applyRestMode);
  }
  async function closeModal(force) {
    $('#logModal').hidden = true;
    document.body.style.overflow = '';
    document.body.classList.remove('modal-open');
    modal.editing = null;
    if (state.session.length || force === true) {
      state.feeds = {};
      await refreshAll();
      Object.keys(state.open).forEach(function (id) { if (state.open[id]) loadFeed(id); });
      if (state.view === 'stats') loadStats();
      if (state.view === 'duel') loadDuels();
    }
  }
  $('#logBtn').addEventListener('click', openModal);
  $('#cancelEditBtn').addEventListener('click', function () { closeModal(); });
  Array.prototype.forEach.call(document.querySelectorAll('[data-close]'), function (b) {
    b.addEventListener('click', closeModal);
  });
  document.addEventListener('keydown', function (e) {
    if (e.key === 'Escape' && !$('#logModal').hidden) closeModal();
  });

  $('#addBtn').addEventListener('click', async function () {
    var amount = parseFloat($('#amountInput').value);
    $('#modalErr').textContent = '';
    if (!isFinite(amount) || amount <= 0) { $('#modalErr').textContent = 'Enter a number above zero.'; return; }
    var btn = this; btn.disabled = true;
    var label = modal.editing ? 'SAVE CHANGES' : 'ADD';
    btn.textContent = 'SAVING…';
    try {
      if (modal.editing) {
        var target = findEntry(modal.editing);
        var up = await sb.from('workouts').update({
          exercise_key: modal.key, mode: modal.mode, amount: amount
        }).eq('group_id', target ? target.group_id : modal.editing).select();
        if (up.error) throw up.error;
        if (!up.data || !up.data.length) {
          throw new Error('That week is over — its entries are locked.');
        }
        toast('Updated — now ' + num(up.data[0].points) + ' pts');
        await closeModal(true);
        return;
      }
      var g = gymInputs();
      var isGym = (exercise(modal.key) || {}).cat === 'GYM';
      var r = await sb.rpc('log_workout', {
        p_league: state.leagueId,
        p_key: modal.key,
        p_mode: modal.mode,
        p_amount: amount,
        p_bw: isGym && isFinite(g.bw) ? g.bw : null,
        p_load: isGym && isFinite(g.load) ? g.load : null
      });
      if (r.error) throw r.error;
      var row = r.data && r.data[0];
      if (!row) throw new Error('Could not save that entry.');
      state.session.push(row);
      renderSession();
      if (isRestDay()) { state.restDone = true; applyRestMode(); }
      toast('+' + num(row.points) + ' pts' +
            (row.leagues > 1 ? ' · counted in ' + row.leagues + ' leagues' : ''));
      $('#amountInput').value = unitFor(modal.key, modal.mode).def;
      updatePreview();
    } catch (e) {
      $('#modalErr').textContent = niceError(e);
    } finally {
      btn.textContent = label;
      btn.disabled = isRestDay() && state.restDone;   // keep the rest lock on
    }
  });

  function renderSession() {
    var box = $('#sessionBox');
    box.hidden = state.session.length === 0;
    var total = state.session.reduce(function (a, w) { return a + Number(w.points); }, 0);
    $('#sessionTotal').textContent = num(total);
    $('#sessionList').innerHTML = state.session.slice().reverse().map(function (w) {
      var ex = exercise(w.exercise_key), u = unitFor(w.exercise_key, w.mode);
      return '<div class="sitem">' +
        '<span class="sx">' + esc(ex ? ex.name : w.exercise_key) +
        ' <span class="muted">· ' + num(w.amount) + ' ' + u.short + '</span></span>' +
        '<span class="sp">+' + num(w.points) + '</span>' +
        '<button class="del" data-undo="' + w.id + '" title="Remove">✕</button>' +
      '</div>';
    }).join('');
  }

  $('#sessionList').addEventListener('click', async function (e) {
    var b = e.target.closest('[data-undo]'); if (!b) return;
    var id = b.getAttribute('data-undo');
    var entry = state.session.filter(function (w) { return w.id === id; })[0];
    var r = await sb.from('workouts').delete()
              .eq('group_id', entry ? entry.group_id : id);
    if (r.error) { toast(niceError(r.error), true); return; }
    state.session = state.session.filter(function (w) { return w.id !== id; });
    renderSession();
    toast('Removed');
  });

  /* ===================================================================
     14. Keep it live
     =================================================================== */
  var autoTimer = null;
  function startAutoRefresh() {
    if (autoTimer) clearInterval(autoTimer);
    autoTimer = setInterval(function () {
      if (document.hidden || !$('#logModal').hidden) return;
      loadBoard();
      if (state.view === 'duel') loadDuels();
    }, 25000);
  }
  document.addEventListener('visibilitychange', function () {
    if (!document.hidden && state.leagueId) { state.feeds = {}; refreshAll(); }
  });

  /* ===================================================================
     15. PWA plumbing
     =================================================================== */
  /* updateViaCache 'none' so the worker script itself is never read from the
     http cache, and a reload the moment a new worker takes over — otherwise
     the page you are looking at keeps running the release you just replaced
     and the update only shows up two launches later. Guarded so a worker
     claiming a page for the first time does not bounce it. */
  if ('serviceWorker' in navigator) {
    window.addEventListener('load', function () {
      navigator.serviceWorker.register('./sw.js', { updateViaCache: 'none' })
        .then(function (reg) { reg.update(); })
        .catch(function () {});
    });
    var hadController = !!navigator.serviceWorker.controller;
    navigator.serviceWorker.addEventListener('controllerchange', function () {
      if (hadController && !window.__reloading__) {
        window.__reloading__ = true;
        location.reload();
      }
    });
  }
  var deferredPrompt = null;
  window.addEventListener('beforeinstallprompt', function (e) {
    e.preventDefault();
    deferredPrompt = e;
    $('#installBtn').hidden = false;
  });
  $('#installBtn').addEventListener('click', async function () {
    if (!deferredPrompt) return;
    deferredPrompt.prompt();
    await deferredPrompt.userChoice;
    deferredPrompt = null;
    $('#installBtn').hidden = true;
  });

  boot();
})();
